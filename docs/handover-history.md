# handover 历史明细（窗0 – 窗16 · 台账原文 + 窗16 从 §6.3 / §6.4 搬出的流水账）

> **这是 `docs/handover.md` 的历史附件，不是必读文件。** 用途只有一个：
> 要**复现某个窗口的做法**、或**查当时的数字**时，按窗号在这里检索。
> 现行规范、规则、检索入口一律在 **`docs/handover.md`**（§0 检索入口 / §6.5 窗口建议 / §6.6.0 摘要表 / §8 排版法）。
> 维护规则（§8.5）：本文件**只增不改**；新的窗口在 `handover.md` 的 §6.6.0 追加摘要行，明细写进 §6.2.N。
> ⚠ **窗16 追加三块**（都是"只搬不改"）：§6.3 已完成条目的原文、§6.4 逐窗复核流水账原文（正文只留"一句话 + 表"）。
> 检索用法（在仓库根目录跑）：
> - 按窗号：`grep -n '窗9' docs/handover-history.md`
> - 按关键词：`grep -n '裸反引号\|hot:true\|考情' docs/handover-history.md`
> - 需要上下文再 `read` 对应行段（**别整读**，本文件约 4 万字符）。

## 窗号台账历史明细（窗13 重排前的原表，逐字保留）

### 6.6.1 历史明细（**不用读**；只在复现某窗做法 / 查当时数字时按需查）

> ⚠ 以下是窗13 重排前的原表，**逐字保留**（信息零丢失，§8.5 的"历史归位"）。新窗**跳过本节**。

| 窗号 | 日期 | 交付 | 该窗给出的窗口建议 |
| --- | --- | --- | --- |
| **窗13** | 2026-09-21 | **theory 文案体检第 3 批（计网书 10 个模块）** + **修 t10 判据一处误报**：10 条 theory 按 §3.8-16 重写成"三问开篇（2–3 行）→ 关键机制 → 考点提醒 → **真题考情（末尾、实测条条 2 行）**"，**考情块由逐题长注压成 2 行**（`net-mail` **446→154 字符**、`net-switch` **532→154 字符**——旧文案把 9/11 道题逐个解释），删 `net-tcp-cc` 的"观察要点"等用法铺垫，**全部真题题号一道没丢**（`net-window` 的 10 行真题锚点表整表保留），字符 13358→13641；**勘误 4 处数字 + 3 处年份归属**（全部 `RC408.examHistory` 现算 + 回 `考情缓存/` 取证：`net-subnet` 14→**15 年**（大题年份把 2014 错写成 2022）、`net-tcp-conn` 7→**8 年**、`net-tcp-cc` 8→**9 年**（theory+头注释，漏 2026-47）、`net-shannon` 头注释（2010 是假的、漏 2013/2026）；2MSL 掐表只认 **2021-38/2022-39/2024-38**、rwnd 只认 **2025-38**（`2025_解析.txt:202`）、两定理联立锚点 **2017-34**（`关键词索引.md:1618`））；**★ 修一处"检查器误报"**：`tmp_t10_check_static.js` 的 `outsideMath()` **只挖 `\(…\)`、没挖 `$$…$$`**，把 `net-window` 一条**完全正确的**显示公式报成 `strayBackslash=1`——**判据错、不是数据错**（§3.8-17 的第四种形态），修后配离线自测 `tmp_t13_judge_test.js`（**把被检查脚本的函数原文抠出来 `eval`**，5 用例该红会红、该绿会绿），并就地记明**窗12 §6.2.10⑨ 记的 `strayBackslash=0` 与实测对不上、别当基准**；**流程改进**：`tmp_t13_apply.js` 把 §3.8-16 的结构验收**前移到写盘之前**（当场抓到 `net-tcp-cc` 漏写 `## 考点提醒`）；**计数全不变**（条目 96 / ready 45 / 纯理论 36 / 建设中 15 / 火苗 40 / exam-history 50），**`v=49 → 50`**；验证：`tmp_t13_apply.js --verify` 10 模块逐字相等、`readme_audit check` 四模式 **0 不一致**、`doc_check.py` **13 项全绿**（含新的第 12 项自述数字/措辞与第 13 项排版硬约束）、`t11`（提权）**`结构不合格=0 考情块超2行=0`**+10 条考情条 18 格逐条相符+2 张理论区截图目视、`t6`（提权）`traverse=96 bad=0`+`hotTotal=40`+CDP 零异常、`t10`（提权）`strayBackslash=0`+`cjkInMath=0`（`mathNodes 108→104`、`rawBackslashModules` 仍 12 是**预期**）；handover 维护：**§6.2.11 新增** / §6.2.10⑨ 就地勘误 / §3.5-18 / **§3.8-17④ 新增** / §3.8-16 进度 30/45 / §3.6.1 / §6.4 / §6.5 / §6.6；**§6.2.11⑦（同窗追加 · handover 上手成本治理 P0+P2）**：§0 动作清单加**第 6 步"跑三条基线"** + 一条**"不要通读全文（2895 行 ≈157K token）"的硬边界与三条 grep 定位命令** + 索引表补"theory 体检/本窗该做啥"两行 + §0③ 的检查器坑**去重成一行指向 §3.8-17**；`tools/doc_check.py` **新增第 12 项"handover 自述数字 / 过时措辞"**（只扫"当前状态区"、历史节豁免；顺带**现算并打印体检真实进度 30/45（OS 8/8·COA 12/12·NET 10/10·DS 0/15）**），配注入自测 `tmp_t13_doccheck_selftest.js`（**4 个注错 → exit 1 且报对 → 还原后 exit 0**，实测 5/5） | **换窗**。**下一窗首选 = theory 体检第 4 批（最后一批）`ds` 书 15 个模块**（theory 约 16.6K 字符、全库最大，按 §3.7-3 单独一窗；做完即 **45/45 收口**），直接照抄 `tmp_t13_theory.js` + `tmp_t13_apply.js` 这一对（含"写盘前的结构/行宽检查"）、`tmp_t11_theory_harness.js` 只改 `IDS`/`BOOK` 与 2 个 `shot()`。若做模块：`net-routing`（11/18 年 + 大题 4，hot）或 `net-csma`（14/18 年 + 大题 1，hot）；instant 可 2 个/窗 `coa-floatadd`；`ds-prim` 先按 §2.0② 裁定"MST 选择题归谁"。⚠ 每条 theory 与头注释的"N/18、选几、大几"**一律现算**（三批共查出 20+ 处错值，`doc_check` 与四模式对账都查不到）；⚠ 写"某年考的是哪个点"之前先 grep `考情缓存/`；⚠ **"必须为 0"的指标第一次非 0，先分清"数据错"还是"判据错"**（§3.8-17④）。 |
| **窗12** | 2026-09-21 | **theory 文案体检第 2 批（计组书 12 个模块）** + **修"公式显示"第二类 bug**：12 条 theory 按 §3.8-16 重写成"三问开篇（2–3 行）→ 关键机制 → 考点提醒 → **真题考情（末尾、实测条条 2 行）**"，删 4 处背景/用法铺垫、`coa-mainmem` 考情块 6 行→2 行（2561→1292 字符）、**全部真题题号一道没丢**；**勘误 18 处**（12 条 theory 里 **9 条** + **7 个头注释**，全部用 `RC408.examHistory` 现算：coa-mainmem 10 年→**6 年（8 题=6 选+2 大）**、coa-fixadd 14→**13 年**（并补漏登的 **2012-43**）、coa-float754 11→**12 年**（删假锚点"2019 精度"）、coa-io 13→**11 年**（大题 2016-44 从选段移出）、coa-perf 6→**7 年**、coa-disk 补漏登 **2013-44**、coa-pipeline 分母 17→**18**（删假锚点"2014-44"）、两个 cache 模块把"整体 13 年"改成本模块 **8/18** 与 **11/18**），另修 `exam-history.js:177` 的"10 年"（**窗6 漏改的那一行**）与 handover §2.2"窗9 回滚了却没改回"的进位计数制行（9/18 选 4 → **10/18 选 5**）；**用户可见公式 bug ×5 模块**：`coa-fixadd` / `ds-binsearch` / `ds-topo` / `net-shannon` 的数学定界符写成**单反斜杠**（模板字面量求值即裸括号 → KaTeX 收不到）、`net-window` 用 framework **不认的 `\[…\]`**——**推翻窗10"模块源码一直是好的"**（那次普查按双反斜杠匹配）；**三条新防线**：`doc_check.py` **第 11 项"模块 theory 数学定界符"**（含注入测试：塞一处单反斜杠即 exit 1）、t10 拆出 **`strayBackslash`（必须为 0）**、t11 的 `\(` 判据改"成对" / `NaN` 改"按模块声明允许数"；**两处清理**：删 `index.html` 里**把开发契约写在用户界面上**的页脚（违反 §1.7-6）、删 782KB 的 `tmp_handover_corrupt_backup.md`（`tmp_t12_heading_diff.js` 比对无章节缺失）；**计数全不变**（96 / 45 / 36 / 15 / 40 / 50），**`v=48 → 49`**；验证：`doc_check.py` **11 项全绿**、`readme_audit check` 0 不一致、`t6` 96 条 `bad=0`+`hotTotal=40`+CDP 零异常、`t10` 定界符 `$$`+`\(`/`\)`+`cjkInMath=0`+**`strayBackslash=0`**+ds-bst-avl 26/ds-heap 37 不变、`t11` **`结构不合格=0 考情块超2行=0`**+12 条考情条 18 格逐条相符+2 张理论区截图目视；handover 维护：**§6.2.10 新增** / §0③ / §2.2 / §3.5-18 / §3.5-24 / §3.6.1 / **§3.8-17 新增** / §3.8-16 进度 / §6.4 / §6.5 / §6.6 | **换窗**。**下一窗首选 = theory 体检第 3 批**：建议 **`net`（10 个模块 / 13.3K 字符）**，**`ds`（15 个 / 16.6K）最大、留最后单开一窗**；照 `tmp_t12_theory.js` + `tmp_t12_apply.js` 这一对改文案、`tmp_t11_theory_harness.js` 只改 `IDS`/`BOOK` 与 2 个 `shot()` 验证。若做模块：`net-routing`（11/18 年 + 大题 4，hot）或 `net-csma`（14/18 年 + 大题 1，hot）；instant 可 2 个/窗 `coa-floatadd`。⚠ **每个模块的 theory 与头注释数字都要现算**（读 `RC408.examHistory`）——本窗 16 处错值 `doc_check` 与四模式对账全都查不到；⚠ **批量改 theory 用"数据文件 + 锚点写入器 + 统一转义"**（别手写反引号/反斜杠，那是复发 4 次的坑）；⚠ 判据报红先问"它在这批数据上还成立吗"（§3.8-17）；⚠ **开工先跑 §3.5-24 的"文档完整性探针"**（行首计数必须全是 1） |
| **窗11** | 2026-09-21 | **theory 文案体检第 1 批（OS 书 8 个模块）** + **修复 handover 三份重复损坏**：8 条 theory 按 §3.8-16 重写成"三问开篇（2–3 行）→ 关键机制 → 考点提醒 → **真题考情（末尾、≤2 行）**"，删 3 处背景/用法铺垫、保留全部易错点与真题题号；**勘误 4 处**（os-lru `7/17`→`7/18`、`13/17`→`16/18`；os-deadlock"（2023 真题）"→ **2013-32**；os-vm `13 年`→**14/18**、"10 次以上大题"→**大题 12 道**；os-procstate 头注释删缓存里查不到的 "2025-25"）；**新记一条对账盲区**（模块 theory 的年份/分母不在 `tmp_readme_audit.js` 覆盖范围，§3.5-24）；**handover 损坏修复**：文件被写成三份重复（6814 行、文档头出现 3 次），只保留最新副本 A 并补回 §6.2.8 ②③④⑤ 与 `### 6.3` 标题 → **2381 行**，留档 `tmp_handover_corrupt_backup.md`（782KB）+ 可重跑的 `tmp_fix_handover_dup.js`；**新增 `tmp_t11_theory_harness.js` + `tmp_t11_check_static.js`**（理论区专用：三问开篇 / 考情块行数 / 考情条一致性 / 横向溢出 + 理论区截图）；**计数全不变**（条目 96 / ready 45 / 纯理论 36 / 建设中 15 / 火苗 40 / exam-history 50），**`v=47 → 48`**；验证：`doc_check.py` 全绿、`readme_audit check` 0 不一致、`t6` 96 条 `bad=0`+CDP 零异常、`t10` 定界符 `\(`/`\)`+`cjkInMath=0`+26·37·12 不变、`t11` `结构不合格=0 考情块超2行=0`+2 张理论区截图目视；handover 维护：**§6.2.9 新增** / §6.4 / §6.5 / §6.6 / §3.6.1 / §3.8-16 / §3.5-18 / §3.5-24 / §0 | **换窗**。**下一窗首选 = theory 体检第 2 批**（`net` 10 个模块 / `coa` 12 个；**`ds` 15 个最大、留最后单开一窗**），照 `tmp_t11_theory_harness.js` 改 `IDS` 即可验。若做模块：`net-routing`（11/18 年 + 大题 4，hot）或 `net-csma`（14/18 年 + 大题 1，hot）；instant 可 2 个/窗 `coa-floatadd`。⚠ 每条 theory 的"N/18、选几、大几"**一律现算**（读 `RC408.examHistory`），别继承旧文案数字；⚠ theory 里的 Markdown 行内代码**必须写转义反引号**；⚠ **开工先跑一次 §3.5-24 的"文档完整性探针"**（数 `### 6.6 窗号台账` 这类标题的**行首**出现次数，必须 = 1；>1 就是 handover 又被拼接坏了，§6.2.9③） |
| **窗10** | 2026-09-21 | **修全站 KaTeX 公式显示错误（共享内核 bug）+ 数学环境去中文**（用户截图反馈）：根因是 `js/framework.js` 的 `renderMath` 把 KaTeX 定界符写成 `\\(`（JS 无效转义 → 求值为**裸括号**），于是 auto-render 以裸括号为定界符、**KaTeX 拿到不含 `\\(\\)` 的内容**，把 `\log` `\lfloor` `\ge` 当普通字符原样吐出（截图里的"文字显示错误"）；**一行修复 = 全站 45 个模块公式一起恢复**（全库 `\\(`/`\\)` 全部配对，模块源码一直是对的）；顺手修 4 处"数学环境含中文"（coa-muldiv / coa-mainmem×2 / ds-bst-avl，改"中文出数学、式子留数学"）；**验证装桩**（本机拉不到 KaTeX）：修复前 97 个公式环境**识别到 0 个**，修复后 ds-bst-avl 26 / ds-heap 37 / coa-mainmem 12；数学环境含中文 4 → **0**；96 条遍历 `bad=0`、CDP 零异常；**计数全不变**（96/45/36/15/40/50），**`v=46 → 47`**；harness 两坑：`/json/new` 后再 `Page.navigate` 同 url 会让页面跑两遍自检（只剩一行输出）、页面脚本同步执行时 `RC408.books` 未就位（`bad=96`）；**模板字面量裸反引号第 4 次复发**（改用占位符 + `String.fromCharCode(96)` 替换收工）；handover 维护：**§6.2.8 新增** / §6.4（窗10 复核）；新增 `tmp_t10_harness.js` + `tmp_t10_check_static.js` 留档 | **换窗**。**下一窗首选 = "theory 文案体检"（用户拍板）**：按 **§3.8-16 的三问 + 四块标准**重写 45 个模块理论区（先答"为解决什么问题 / 怎么实现 / 记住什么"，再按"定位 → 关键机制 → 考点提醒 → **真题考情 ≤2 行**"组织；**删推导与背景铺垫、留结论与易错陷阱、保留全部真题题号**）；**用户选择单开一窗**，建议分批（按书）做。若做模块：`net-routing`（11/18 年 + 大题 4，hot）或 `net-csma`（14/18 年 + 大题 1，hot）；instant 可 2 个/窗 `coa-floatadd`；`ds-prim` 先按 §2.0② 裁定 MST 选择题归谁。⚠ 改 `framework.js`/css **必须递增 v**；公式类问题**先查定界符**（§6.2.8）。 |
| **窗9** | 2026-09-21 | **零新模块 · 纯数据对账窗**（§3.9 双轨表的「纯文档/数据对账」轨）：清理 §2.0 挂了 3 个窗口的"**全库选择题重复登记**"遗留——新增 `tmp_dup_audit.js`（唯一数据源 `js/exam-history.js`，展开 **428 条逐题记录**后按"年份-题号"哈希），查出**选择题重复 6 处**（§2.0② 需裁决）+ **大题重复 12 处**（§2.0① 合规）；6 处选择题**逐条回 `考情缓存/` 原文裁决 = 4 删 + 2 保留**：**2010-4**（2010_解析.txt:94-98 是 RL 型平衡旋转 → 归 ds-bst-avl，哈夫曼实为 2010-6）、**2013-9**（:117-122 是 AOE 网关键路径 → 归 ds-topo；2013 全年解析无"散列"命中）、**2016-13**（2016_解析.txt:224-229 是"short 补码→unsigned"、无进制换算 → 归 coa-fixadd）、**2020-6**（2020_真题.txt:40-47 是"修改递归 DFS 算法"→ 归 ds-dfs-bfs）；**2021-13 / 2022-15 保留**（题干本身跨两考点，进 `ACCEPTED_SEL` 白名单）；**顺带查出第 7 处误登**：`ds-dijkstra` 的 2014 `42·43` → **43 是路由表续问**（归 net-subnet）。**新维度**：`tmp_readme_audit.js` 新增 **`years` 模式**（原来只比 `N/18` 计数、没人查"选 …；大 …"的**年份清单**）→ 查出 3 处：ds-hash 10→**9 年**、coa-baseconv 11/18→**10/18（选 5）**、**主存行/BST 行的选段多列了但属大题的年份**（`2010·15`/`2021·15`/`2026·8`）已删（**年数不变**）。**两道新防线**：`tmp_dup_audit.js` 的 **`check` 模式**（回归，exit 1，实测 0 违规）+ **`tools/doc_check.py` 新增"模块 JS 语法"项**（45 个模块 `node --check`，专治窗8/窗9 两次"裸反引号截断 theory"事故；实测注入即报 `ds-hash.js:15`）。**计数全不变**：火苗 **40**、ready 45 / 纯理论 36 / 建设中 15 / 条目 96 / v=47。**真 bug 1 个**（我自己在 `ds-hash.js` theory 写裸反引号 → `SyntaxError`，当场修 + 补防线）；**判断错 1 处并翻案留档**（2022-15 一度被过严裁决删掉 → 回滚，§6.2.7④）；**`years` 解析连撞 5 轮假红**（§3.6.1 新增两条倾向）。handover 维护：§2.0（口径段改写 + 裁决判据）/ §2.1（散列表 10→9）/ §2.2（进位计数制 11→10）/ §3.5-18（留档清单加窗9 两个脚本）/ §3.6.1（十窗归因 + **2 条新倾向**）/ **§6.2.7 新增** / §6.3-3（转"漏登"）/ §6.4（窗9 复核 + 环境第 4 次复现 0x5）/ §6.5 / §6.6；README 四处；模块 theory 两处（`coa-baseconv` / `ds-hash`）；`tools/doc_check.py` 一处 | **换窗**。下一窗**换回"做模块"轨（1 个/窗）**：首选 `net-routing`（11/18 年 + 大题 4，**会标 hot**，stepper 距离向量逐轮迭代表，可复用 `net-*` 家族表格+逐帧快照）或 `net-csma`（14/18 年 + 大题 1，hot，即时计算器 + 二进制指数退避时间轴）；instant 可 2 个/窗：`coa-floatadd`。⚠ **`ds-prim` 现在正是好时机**（重复排查刚做完），但**必须先按 §2.0② 裁定"MST 选择题归谁"再动手**。⚠ **"漏登"排查仍未做**（本窗只做了反方向的"重复"），单开一窗。⚠ 提醒下一窗：改考频/清单前后跑 `node tmp_readme_audit.js check`（一条命令跑 overview/t5chk/years/comments，全 0 不一致才 exit 0）｜写完模块**立刻跑 `tools/doc_check.py`**（现在会 `node --check` 全部模块，裸反引号当场暴露）｜**裁决"该归谁"时先抄该条目自己的登记规则**（§3.6.1 窗9 第一条新倾向）｜跑浏览器前先 `chrome --version` 坐实要提权（`0x5`） |
| **窗8** | 2026-09-21 | 新增 `js/modules/net-mail.js`（电子邮件与 FTP，前缀 `_ml`）与 `js/modules/net-switch.js`（以太网交换机自学习，前缀 `_swc`）——**同书（计网）/ 同范式（生命线 + 报文箭头 + 逐帧快照）/ 同模式（stepper）/ 零 CSS 与框架改动**；net-mail = 邮件 4 条生命线 + FTP 2 条生命线 + **区间带**（"SMTP 全程 · 推"/"收信 · 拉"/"控制连接 21 全程保持"/"数据连接传完即关"）+ 协议一览表（端口·谁主动·推还是拉）+ 三场景（邮件 / FTP 主动 / FTP 被动）；net-switch = **帧事件时序图**（一个事件 = 1 入向 + N 出向箭头，**行内纵向错开**，`ROWH=84` 支持一行 4 条）+ **转发表实时增长** + 四条规则表 + 冲突域/广播域表，拓扑 = **端口 1 一段 Hub 共享段（A、B）+ 端口 2（C）+ 端口 3（D）**（这条拓扑正是"过滤丢弃"分支可达的前提、也是 2021-47 真题原型）；考情从零核定 **net-mail 9/18（选 8 + 大 1）**、**net-switch 11/18（选 9 + 大 3）**，**都标 `hot:true`**（火苗 **38 → 40**）、**ready 43 → 45、纯理论 38 → 36**、exam-history 48 → 50、README 总览 43 → 45 行、v=45 → 46；**考情勘误 2 处 + 补登 5 题**（README 交换机行 9 → **11** 年；旧 net-switch note 的 **2011-47 / 2013-38 两条误登**；旧 net-mail note **漏登 5 年**含大题 2023-47(1)）；**两道大题靠"渲染卷面目视"核到**（2021 真题 p8–p9 是乱码字形码、关键词索引零命中；2023 解析无文本层）；**真 bug 1 个（编译期自伤）**：net-switch 的 theory 模板字符串里用裸反引号写 Markdown 行内代码 → 整个模块 `SyntaxError`（一 require 即暴露）；**测试脚本零返工**——九窗以来第一次"零断言红"（冒烟 18696 项一次全过、harness 160 帧 0 违规一次跑通）；handover 维护：§1.5 / §2.4 / §2.6-2 / §3.1-4 / §3.5-13 / §3.5-18 / §3.6.1（九窗归因 + 1 条复发倾向）/ **§3.8-15 新增** / §4.1 / §4.4 / §4.5 / §5 / §6.1 / **§6.2.6 新增** / §6.3-8 + §6.3-11 结案 / §6.4 / §6.5 / §6.6；README 四处 | **换窗**。**下一窗首选走"轻量轨"**：§2.0 的"**全库选择题重复登记**"系统排查（连挂 3 窗的遗留、本窗又实测出两处跨条目误登，做法 = 以 `exam-history.js` 为唯一数据源列重复清单 + 按 §2.0② 逐条裁决 + `tmp_readme_audit.js` 三模式回归）；**若做模块（1 个/窗）**：`net-routing`（11/18 年 + 大题 4，**会标 hot**，stepper 距离向量逐轮迭代表）或 `net-csma`（14/18 年 + 大题 1，即时计算器 + 退避时间轴）；instant 可 2 个/窗：`coa-floatadd`。⚠ **`ds-prim` 与 ds-kruskal 同考同一批 MST 真题，必须先决定"选择题归谁"，建议排在"重复登记排查"之后**。⚠ 提醒下一窗：改数字先跑 `node tmp_readme_audit.js overview\|comments\|t5chk`｜**新增 README 总览行必须同步 `OV`（现 45 行）**｜**考情不许继承旧 note、索引/grep 会漏题，拿不准就 `--render` 卷面目视**｜冒烟改 `tmp_net_smoke.js`、harness 改 `tmp_net_harness.js`（**跑前先 `chrome --version` 坐实要提权**）｜**设计拓扑先确认"要演示的分支真的可达"（§3.8-15）**｜theory 里 Markdown 行内代码必须用转义反引号 |
| **窗7** | 2026-09-21 | 新增 `js/modules/net-dns.js`（DNS 域名解析过程，前缀 `_dns`）与 `js/modules/net-http.js`（HTTP 请求与持久连接，前缀 `_ht`）——**同书（计网）/ 同范式（时序箭头：生命线 + 报文箭头 + 逐帧快照）/ 同模式（stepper）/ 零 CSS 与框架改动**；net-dns = 五条生命线 + 四场景（递归+迭代 / 全部迭代 / 全部递归 / 缓存命中）+ "谁发了几条查询"对照表；net-http = 每个 RTT 一行 + 三种连接方式 + **计时起点（含不含建连）**口径切换 + 真题锚点卡；考情从零核定 **3/18（全选）** 与 **5/18（选 4 + 大 1）**、**都不标 hot**（火苗 38 不变）、**ready 41 → 43、纯理论 40 → 38**、exam-history 46 → 48、v=44 → 45；**考情勘误 2 处 + 补登 1 题**（2010-40 是纯 DNS 题却记在 net-http；2011-47(3) 官方答案 **6 RTT** 而非 7、且旧 note 漏登这道大题小问 → 全部回 `考情缓存/` 原文改正）；**真 bug 1 个**（net-http 一行 3 条箭头 → 标签压盖；Node 冒烟 14124 条断言全绿漏网，**逐帧几何断言** `labelOverlap=16` 抓到）→ 新经验 **§3.8-14**；handover 维护：§2.4 / §2.6-2 / §3.1-4 / §3.5-13 / §3.5-18 / §3.6.1（八窗归因 + 2 条新倾向）/ **§3.8-14** / §4.1 / §4.4 / §4.5 / §5 / **§6.2.5 新增** / §6.3-8 + §6.3-10 结案 / §6.4 / §6.5 / §6.6；README 三处 | **换窗**；下一窗首选**同家族 2 个**：`net-mail`（邮件·FTP）+ `net-switch`（交换机自学习），可直接复用本窗"生命线 + 报文箭头 + 逐帧快照"范式、零内核改动；次选 `ds-extsort` 败者树｜`coa-floatadd`｜`ds-prim`。**"全库选择题重复登记"排查仍单排一窗、不要与模块同窗**。⚠ 提醒下一窗：**考情不许继承旧 note，一律回缓存复核**（本窗靠它抓出 2 处勘误 + 1 处漏登）｜改数字先跑 `node tmp_readme_audit.js overview\|comments\|t5chk`｜冒烟改 `tmp_net_smoke.js`、harness 改 `tmp_net_harness.js`（自带逐帧几何断言）｜版面"同一行纵向堆叠"先定每行上限再反算行高（§3.8-14） |
| **窗6** | 2026-09-21 | **三批交付**。**(A) 数据对账**：README「真题考情分析」四表（当时在 §五，窗6 第三批重编号为 **§二**）按 `js/exam-history.js` 全部重算（26 行 17 年分母 → 18 年、13 行分子纠错、平台状态 13 行改写、新增维护口径注记）；README 模块总览 12 行错值；handover §2 十处错值；`exam-history.js` 注释 28 处；**`ds-kruskal` 补 `hot:true`**（5/18 年 + 大题 2017-42/2018-42，按口径本应标而漏标）；**9 个无 exam-history 条目的考点派 2 个子代理回 `考情缓存/` 核定**（链表 4/18、复杂度 9/18、机器级代码 14/18、指令格式 11/18、中断 16/18、CSMA/CD 14/18、交换机 9/18、路由 11/18、应用层 17/18），纠正"复杂度=每年第 1 题""文件系统大题 5 道"；**勘误 `coa-mainmem` 的"10/18 年"**（题数当年数，实为 6 年 / 8 题 = 6 选 + 2 大，仍靠"大题 ≥2"标 hot）；**改写窗3 的"2023 无法核对"**（2023 真题有图像层、渲染 8 页目视可读，补核 2023-21/36/44/47）。**(B) 新增 2 个模块**：`js/modules/coa-baseconv.js`（进位计数制与转换，前缀 `_bc`）与 `js/modules/coa-muldiv.js`（乘除运算电路，前缀 `_md`）——**同书 / 同范式（instant 计算器）/ 零 CSS 与框架改动**；考情从零核定 **11/18**（选 6 + 大 5）与 **7/18**（选 1 + 大 6）⟹ 均标 `hot:true`，**火苗 36 → 38、ready 39 → 41、纯理论 42 → 40**、exam-history 44 → 46、**v=43 → 44**；**勘误 1 处**：旧 note"2024-13 直接考进制转换"错（实为 **2024-12**）；**真 bug 1 个**（`coa-muldiv` 的 `err` 文案把 Markdown `**` 渲进 HTML，冒烟抓到并修）；**harness 坑 1 个**（生成脚本里带转义的正则退化成字符类 → 假阳性，改 `indexOf`）；handover 维护：§1.5 / §2.2 / §3.1-4 / §3.5-10⑤ / §3.5-12⑥⑦⑧ / **§3.5-24 新增** / §3.5-18 / §3.6.1 / §3.9 / §4.4 / §4.5 / §5 / §6.1 / §6.2.2 / **§6.2.3 新增** / §6.3-3 结案 + §6.3-8 / §6.4 / §6.5 / §6.6；README 六处 | **换窗**；下一窗按 §3.7-2 家族合批 **2 个/窗**："时序箭头"家族（`net-dns` / `net-http` / `net-mail`，复用 net-tcp-conn 的报文箭头 + 两端状态机）｜"树+SVG"家族 `ds-extsort` 败者树｜instant 家族 `coa-floatadd`｜"图+SVG"家族 `ds-prim`。**§6.3-3 的 README 遗留本窗已清完、不再挂账**；**"全库选择题重复登记"排查仍单排一窗、不要与模块同窗**。⚠ 提醒下一窗：改考频数字先跑 `node tmp_readme_audit.js overview\|comments\|t5chk`｜子代理考情提示词可直接复用｜**2023 可渲染目视，别写"无法核对"**｜冒烟务必带 §3.8-11/12/13 + "HTML 不许有未渲染 Markdown" ｜ **(C) 文档分工**：按用户 2026-09-21 原则把 README 去内部化（六块开发内容搬进 handover：目录结构 / 快照架构 / UI 部件 API / 开发指南 / 定制入口 / 约束 / 建设结论，README 23698 → 13218 字符），**handover 新增 §0 新窗最快上手（入口 + "我要干什么→去哪节"索引 + 三条常犯错）**、§1.7-6 分工原则、§2.6 / §4.6 / §4.7 / §1.2 补全，`doc_check.py` 检查项 10 → 9（全绿）、§3.5-13 同步 |
| **窗5** | 2026-09-20 | 新增 `js/modules/coa-mainmem.js`（主存与 CPU 连接／存储器芯片扩展，前缀 `_mm`，**零 CSS/框架改动**）：**地址总线 + 芯片矩阵（行 = 字扩展、列 = 位扩展）+ 译码器 + 地址空间分区表四区同屏**；逐片摆放（淡入）→ 数据线分段（第 c 列管一段 D 位）→ 地址线并联 + 高位选片 → `⌈log₂行数⌉` 线—行数线译码器 + 逐行点亮片选 → **逐地址自检"唯一命中"** → 地址访问演示（高位译码 → 行 → 片内地址）；三条硬公式实时代入 + 片选/片内地址位数；9 个预设（含 2009-15 / 2010-15 / 2016-16 / 2021-15 四道真题同款）；**考情从零核定 ~~10/18 年（10 选 + 2 大）~~ → **⚠ 窗6 勘误：6/18 年（8 题 = 6 选 + 2 大），原写的是"题数"** → 标 `hot:true`、火苗 34 → 35、ready 38 → 39、纯理论 43 → 42**（该条目原本是纯理论卡，不是 wip）；**旧记录勘误 1 处**：2015-16 不是芯片扩展题（实为页式虚存+TLB+Cache 直写的访存次数，渲染卷面目视确认）；**3 个真 bug 全部由浏览器几何断言抓到**（`s.rows` 当标量用 → `[object Object]`；行标签超宽压邻列；画布右边界算小）；**handover 维护**：§1.5、§2.2、§3.3-21 新增、§3.5-9⑤ 新增（PowerShell 改中文文件会整篇变乱码）+ §3.5-18 清单换窗5 那对 + §3.5-20⑦、§3.6.1 六窗归因 + 三条新倾向、§3.8-12/§3.8-13 新增、§4.4/§4.5/§5、§6.2.1 新增、§6.3-7、§6.4 全部同步；README 五处；v=41 → 42 | **换窗**；下一窗首选 **README「真题考情分析」专项清理**（§6.3-3：27 行仍用 17 年分母 + 5 行"规划中"其实已上线，以 `exam-history.js` 为唯一数据源写脚本重算，风险最低收益最确定）；若继续做模块则按家族合批 2 个：instant 计算器家族（进制转换/乘除/浮点加减）｜时序箭头家族（DNS/HTTP/邮件）｜`ds-extsort` 败者树。**`coa-mainmem` 已完成、勿再列入待办**；**"全库选择题重复登记"排查另排一窗，别与首选那件事同窗做** |
| **窗4** | 2026-09-20 | 新增 `js/modules/ds-heap.js`（堆与优先队列，前缀 `_hp`，**零 CSS/框架改动**：槽位 = 数组下标 ⇒ 边静态、无需相似变换）：完全二叉树 ⇄ 数组双视角 + 自底向上建堆（Floyd）→ 插入上浮 → 删除堆顶下沉的三段连演，交换沿弧线错开滑动 + 交换前幽灵 + 数组行弧形箭头，堆序逐结点检查 + 已取出序列（堆排序输出视角）+ 优先队列/复杂度卡，7 个预设；**考情从零核定 8/18 年（选 7 + 大 1）→ 标 `hot:true`、火苗 33 → 34**（**用户新口径"大题可对应多个知识点、选择题尽量单一"落库为 §2.0，窗4 是第一个应用**：2022-42 因标准解法之一是大根堆而登记为大题；2018-11 / 2024-9 按单选口径从 ds-sort 移出）；`--ask` 把 2020-9 锚成 2020-8 已按卷面纠正；**handover 维护**：§1.5 火苗 33→34、§2.0 登记口径、§2.1、§3.1-5 补注 + §3.1-6 新增、§3.3-20 补注、§3.5-12③ 就地改写 + ⑤（缓存可信度普查：11 个无文本层文件 + 页脚年份错）、§3.5-18/§3.5-21⑤、§3.6.1 五窗归因、§3.8-3④ + §3.8-11 新增、§4.2/§4.4/§4.6/§5、§6.2~§6.5 全部同步；README 五处；v=40 → 41 | **换窗**；下一窗首选 §6.3-7 主存与 CPU 连接（芯片扩展，新范式单开一窗），或按家族合批 2 个：instant 计算器家族（进制转换/乘除/浮点加减）｜时序箭头家族（DNS/HTTP/邮件）；`ds-heap` 已完成、勿再列入待办。**遗留可选任务**：按 §2.0 新口径做一次"全库选择题重复登记"的系统排查（专开一窗，未做）。**⚠ 该建议里的"§6.3-7 首选"窗5 已完成；"coa-radix / coa-float-add" 这两个 id 并不存在（实际是 `coa-baseconv` / `coa-floatadd`，且都是纯理论卡），窗5 已就地改正** |
| **窗3** | 2026-09-20 | **重写** `js/modules/net-window.js`（滑动窗口 停等/GBN/SR，用户要求"更准确、更直观"；前缀仍 `_sw`，**零 CSS/框架改动**）：序号空间改按 mod 2ⁿ 且**窗口跨环自动拆两段**、"轮"驱动主循环（连发一窗 → 确认依次回来 → 才超时）、GBN 真·后退 N 帧整段重发、**新增 ACK 丢失建模（`A2:1` 语法）**、接收方不再凭空确认、内置信道利用率卡（由 U 反推最小窗口与序号位数）+ 9 个真题预设；**考情勘误 11 年 → 10 年**（删误登 2013-35·36 / 2021-35·37 / 2026-35 并把 2026-35 补进 net-csma；补漏登 2012-36 / 2018-36 / 2020-36；2023 无法核对（**⚠ 窗6 实测：2023 真题有图像层、渲染可读，此判断已失效，见 §3.5-12⑧**）、2025 只有解析故不登记）；**handover 维护**：§2.4 滑动窗口行、§3.2-5 就地改写、§3.3-19/20 新增、§3.5-16 补注 + §3.5-20/21 新增、§3.6.1 四窗归因、§3.8-10 新增、§6.2/§6.3/§6.4 全部同步；README 模块总览第 34 行 + 平台状态行；v=39 → 40 | **换窗**；下一窗仍建议 `ds-heap`（窗2 已代查清考情，可直接复用"树 + SVG"家族的固定槽位 + 内联关键帧）——**⚠ 窗4 实测：窗2 代查的那份堆考情有 2 处误记（2022-42、2010-11），且 7 年而非 8 年、全为选择，见 §6.2 窗4 ②**；并列出"重写已有模块"这类活的标准顺序 |
| **窗2** | 2026-09-20 | `ds-bst-avl`（二叉排序树 + AVL 旋转）模块 1 个（前缀 `_av`，**零 CSS/框架改动**：固定槽位布局 + 内联 `@keyframes` + 边用相似变换跟随，做法沉淀为 §3.1-5）；BST/AVL 考情**从零核定 13/18 年**（新发现 2021-6；纠正缓存把 2019-4 错标成"2020-47 计算机网络"，写进 §3.5-12④）；**handover 维护**：§1.5 火苗 32→33、§2.1 BST/AVL 行、§3.5-9②/§3.5-10（harness 三大坑）/§3.5-12④（缓存年份污染）/§3.5-13、§3.6.1（三窗归因）、§3.8-9（动画关键帧断言法）、§4.4/§5/§6.2/§6.3/§6.4 全部计数；顺手补 README 漏登的 `ds-btree.js` | **换窗**；下一窗建议 `ds-heap`（堆与优先队列，考情已代查清），可直接复用本窗的固定槽位 + 内联关键帧做法——**⚠ 该"代查清的考情"窗4 实测有 2 处误记（2022-42 / 2010-11），正确结论是 7/18 年、全为选择，见 §6.2 窗4 ②** |
| **窗1** | 2026-09-20 | `ds-btree`（B 树与 B+ 树）模块 1 个 + B 树/B+ 树考情勘误 3 处（9/18 → **12/18**）；**handover 两轮维护**：① 窗号制度（§1.7-4 + §6.6 台账）；② handover 第一优先级（§1.7-5）+ **§3.6 改为按模型分小节、通用经验拆到 §3.8 / §3.5-16~19** + 修掉 6 处过时或互相矛盾的条目（§1.5 火苗结案、§4.5 分类计数、§3.2-11 Bash 前提、§3.4-22 "已解决却像没解决"、§3.4-25 force push 叙述、§3.5-11 与 §3.8-7 两套说法） | **换窗**；下一窗建议 `ds-bst-avl`（AVL 旋转），"确认只靠内联过渡即可与另一'树+SVG'模块合批"——**该判断窗2 实测为错**：`transition` 在本框架里永不播放，必须用内联 `@keyframes`（见 §3.1-5） |
| 窗0 及更早 | ≤2026-09-20 | 编号制度建立前的各窗（平台搭建、理论卡批次、栈序列/io/磁盘、专注模式、ds-sort 希尔、ds-expression、ds-dfs-bfs + 真题缓存工具 + 文档自检工具…），交付明细见 §6.2/§6.3 历史条目 | 无编号制度，未记录（其"本窗"叙述一律按窗0 对待） |

## §6.3 已完成条目的原文（窗16 从 §6.3 搬出；只搬不改）

1. ~~排序模块补希尔排序动画~~ **✅ 已完成（窗0）：ds-sort 增至七种算法**。
   下拉新增"希尔排序"；theory 补「希尔排序（缩小增量排序）」小节并勘误考情
   （原写"17 年每年必考"，exam-history 实测 **18/18 年**，已改）；
   快照新增 `gap` 字段，渲染新增**分组标签**（当前增量下同色同组，SVG 增量行）+ 图例说明；
   增量序列 ⌊n/2⌋ 逐趟减半至 1；每趟表按"第 k 趟（增量 d=…）"记录，中途无元素保证最终位置
   （反推特征已写进考点提醒）。Node 冒烟 7 算法全帧无 undefined/NaN、趟结果与手工推演一致、
   非法输入友好报错；浏览器实测连步/跳帧/每趟表/截图版面正常。
2. ~~表达式求值~~ **✅ 已完成（2026-09-20）：`ds-expression.js`（前缀 _ex）**，目录条目已转 ready。
   **年份勘误（PDF 原文核实）**：表达式题实为 2012-2、2014-2、2024-2（选择）+ 2017-41（表达式树大题）；
   原记录的 13-2 是栈序列题、2022-2/2026-2 均非表达式题（2026-2 为双向链表）。
   ds-stack-queue 原错挂的 2012-2 已移归 ds-expression；其漏登的 2026-42 大题（PDF 核实为
   入栈序列+k<j<i 非法型+卡特兰数递推）已补进 exam-history，现为 **10/18、大题 2**。
3. ~~README「真题考情分析」一节明显过时~~ **✅ 已完成（窗6，见 §6.2.2）**：该节四张表已**全部按
   `js/exam-history.js` 重算**——26 行 17 年分母 → **18 年**；"平台状态"列 5 行"🚧 规划中但其实已上线"
   （coa-io / coa-pipeline / os-file-alloc / os-deadlock / os-sched）改成实际的 ✅；另有 5 行"已有纯理论卡却写规划中"
   改成 📖；确实未做的 3 行（ds-linkedlist / ds-complexity / coa-assembly）改写成 `🚧 <id>（建设中）`；
   模块总览表 12 行错值、handover §2 十处错值、`exam-history.js` 注释 28 处一并改完；
   并新增「**本表的维护口径**」注记（**唯一数据源 = `js/exam-history.js`**、"N/18 年"是**年份数不是题数**、
   **※** 表示该行没有 exam-history 条目）。**窗6 已把这 9 个 ※ 考点的年数全部回缓存核定**。
   **仍未做的**：**"漏登"**（某考点实际考过、却没登记进 `exam-history.js`）**尚未系统性排查**——
    窗9 已做完的是**反方向**的"重复登记"排查（见 **§6.2.7**）。**对账脚本只能查"已登记的是否自洽"，
   查不出"该登记却没登记"**；要做需回 `考情缓存/` 逐年逐题过一遍，属"专开一窗"的活。
   > 历史（窗1~窗5 的续改进度，保留备查）：窗1 改 B 树行 + 模块总览第 36 行；窗2 改 BST/AVL 行 + 第 37 行；
   > 窗3 改滑动窗口行 + 第 34 行；窗4 新增堆行 + 第 38 行；窗5 新增主存行 + 第 39 行。
   > **火苗数与理论卡计数以 `tools/doc_check.py` 的实测输出为准**（现在是火苗 **36**、纯理论 **42**），别照抄旧数字。

4. ~~DFS/BFS 遍历~~ **✅ 已完成（窗0）：`ds-dfs-bfs.js`（前缀 `_fb`）**，目录条目已转 ready。
   快照逐动作推送：起点入栈/入队 → 每访问一个顶点一帧（DFS 沿"第一个未访问邻点"深入 / BFS 出队后
   批量入队）→ 无未访问邻点时退栈回溯一帧 → 每个分量结束一帧 → 完成帧；快照全量携带
   `visited/tree/container/dist/adj/edges/nodes/comps/compVerts/peak` 副本，布局只由顶点集决定（不随进度变）。
   渲染：SVG 圆形布局（有向图带箭头、双向边分两侧弧线）+ 邻接表（顶点表/边表/度数）+ 工作栈（当前路径）
   或队列（待扩展层）芯片 + 访问序列 + 遍历树/森林边 + 分量列表 + 结论（复杂度 O(n+e)/O(n²)、
   BFS 层号=无权图单源最短路径）+ 考点提醒；非法输入（空/格式错/自环/顶点>8/起点不在图中）均给友好报错。
   验证：Node 冒烟 30 个随机图 + 固定场景（DFS/BFS 序列、生成树边数、有向退栈=逆拓扑序、
   起点为 F 的层号、V1/V10 命名、非连通森林）全过；浏览器实测连步/跳帧/自动播放/重置/切算法/
   切起点/三预设/非法输入卡片/专注模式进出，DFS/BFS/有向各模式逐帧无 undefined·NaN，
   全站 96 条遍历零报错，SVG 全帧几何断言（文字不越 viewBox、标签不压节点圆）零违规。
5. ~~B 树与 B+ 树~~ **✅ 已完成（窗1）：`ds-btree.js`（前缀 `_bt`）**，目录条目已转 ready。
   插入分裂（溢出红闪 → 中间关键字上升 → 向上传播 → 根分裂树高 +1）与删除
   （借位蓝 / 合并琥珀 / 前驱顶替 / 根空了降高）逐帧；静态 B+ 树对照图 + 5 条辨析表。
   验证：Node 冒烟 44 场景 / 2044 帧 / 89372 项断言全过（含"每帧都是合法 m 阶 B 树""高度与关键字数
   四项公式自洽""叶结点同层""关键字集合 = 前 n 个插入 − 前 m 个删除"等不变量）；浏览器实测 31 项
   自检全绿（逐帧无 undefined/NaN、SVG 几何零越界、控制面板/预设/非法输入、全站 96 条遍历零报错、
   零 JS 异常）；锁定 3 个真 bug 并复验（见 §3.6.1）。
6. ~~AVL 旋转~~ **✅ 已完成（窗2）：`ds-bst-avl.js`（前缀 `_av`）**，目录条目已转 ready。
   查找路径 → 插入到叶 → 自底向上定位最小不平衡子树 → LL/RR/LR/RL 旋转（结点滑动 + 幽灵 + 边同步变形）；
   可切「普通 BST」对照（默认有序序列 1..10：BST 高 10 / AVL 高 4）。
   **窗0 担心的"旋转过渡要动 style.css"已彻底解决，且窗1 设想的"内联 transition"其实走不通**——
   每帧 `stage.innerHTML=''` 重建 DOM，CSS `transition` 永远不会播放，**必须用内联 `@keyframes`**；
   完整做法见 **§3.1-5**。验证：Node 冒烟 44 场景 / 1532 帧 / 132310 项断言全过；
   浏览器 96 条遍历零异常、8 预设 + 7 例非法输入 + 逐帧几何断言全过；锁定 1 个真 bug 并复验（§6.2-④）。
7. ~~主存与 CPU 连接/芯片扩展~~ **✅ 已完成（窗5）：`js/modules/coa-mainmem.js`（前缀 `_mm`）**，目录条目转 ready。
   四区同屏（① 地址总线 ② 芯片矩阵：行 = 字扩展、列 = 位扩展 ③ 译码器 + 片选线 ④ 地址空间分区表），
   逐片摆放 → 数据线分段 → 地址线并联 + 高位选片 → `⌈log₂行数⌉` 线—行数线译码器 + 逐行点亮片选
   → 逐地址自检"唯一命中" → 地址访问演示；9 个预设（含 4 道真题同款）；**零 CSS/框架改动**。
   验证：Node 冒烟 67 场景 / ≈1280 帧 / 96427 项断言全过；浏览器 96 条遍历零异常、
   **几何越界 0 + 压盖 0**（547 帧逐帧断言）、5 例非法输入界面报错卡、15 张原分辨率截图目视；
   **锁定 3 个真 bug**（`s.rows` 当标量用、行标签超宽压邻列、画布右边界算小）并复验，见 §6.2.1。
   ⚠ 旧记录把 **2015-16** 当成本考点（实为"页式虚存+TLB+Cache 直写的访存次数"），窗5 已勘误（§6.2.1③）。
8. 其余 wip：Prim、基数、外部排序、浮点加减、（**窗6 已完成**：进制转换 → `coa-baseconv`、乘除 → `coa-muldiv`，均 ready + hot；
   **窗7 已完成**：DNS → `net-dns`、HTTP → `net-http`，均 ready、**不标 hot**——见 §6.2.5；
   **窗8 已完成**：邮件·FTP → `net-mail`、以太网交换机自学习 → `net-switch`，均 ready 且 **hot**
   ——见 §6.2.6；
   **窗15 已完成**：路由算法 → `net-routing`（ready + hot）；**窗16 已完成**：CSMA/CD 与最小帧长 →
   `net-csma`（ready + hot，从纯理论卡转做成交互模块）——见 §6.2.13 / §6.2.14）
   动态分区、位示图、分层模型、编码调制等
   （注：其中概念型已全部转纯理论卡，此列表仅剩"值得做动画"的过程型；
   按 §3.7 分批更划算——Prim 归"图+SVG"批、
   ~~邮件·FTP 与 DNS/HTTP 同属"时序箭头"批~~ **该批窗7+窗8 已做完四个模块**
   （`net-dns` / `net-http` / `net-mail` / `net-switch` 的"生命线 + 报文箭头 + 逐帧快照"范式已完全铺好，
   **下一窗要再做时序箭头类，直接照抄 `tmp_net_smoke.js` + `tmp_net_harness.js` 改 SCENES 与选择器即可**））
9. ~~堆与优先队列~~ **✅ 已完成（窗4）：`js/modules/ds-heap.js`（前缀 `_hp`）**，目录条目转 ready。
   快照逐动作推送：init（按层序填入）→ scan（选定本轮筛选根，紫圈）→ cmp（选较大/较小孩子、双亲与孩子比较）
   → swap（沿弧线错开滑动 + 虚线幽灵 + 数组行弧形箭头）→ stop（本轮结束）→ settled（建堆完成，O(n)）
   → place（插入落位）/ take（取出堆顶）→ pop（末尾顶上、n−1）→ 再向下筛选 → done；
   快照全量携带 `arr`（1 基，元素 {v,id}）/`n`/`pos`/`from`/`heapOK`/`heapFull`/`taken` 等。
   渲染：完全二叉树（结点右上角标下标）+ 数组行（含空槽虚线，**容量 vs 堆大小**同屏对照）+ 交换弧线标注 +
   堆序逐结点检查 + 已取出序列 + 优先队列/复杂度卡。
   验证：Node 冒烟 87 场景 / 3677 帧 / 769916 项断言全过；浏览器 96 条遍历零异常、485 帧几何 + 压盖 +
   结点相碰断言全零违规、5 例非法输入界面报错卡、11 张截图目视；锁定 1 个真 bug（input 缺 `default`，
   冒烟漏网、浏览器抓到）并复验（见 §6.2 窗4 ⑤）。**考情按用户新口径最终定稿：8/18 年（选 7 + 大 1）→ `hot:true`。**
   ⚠ **与 ds-sort 的分工（handover 明确要求）**：ds-heap 讲"堆的结构与调整"（建堆筛选/插入/删除堆顶/判堆），
   ds-sort 讲"排序的过程与性质"，两处 theory 都已写明这条分工。**考情上按 §2.0 的新口径划清**：
   问"堆的结构调整"的选择题（2018-11 建堆过程、2024-9 两次删除）**只归 ds-heap**（已从 ds-sort 移出）；
   只问排序性质、堆排序仅作选项的（2012-10/2016-11/2017-11/2023-10）**只归 ds-sort**。
   ⚠ **旧文勘误**：原 §6.3-9（窗2 代查）写的"8 年…大 2022-42、2010-11 是堆排序选项"里，
   **2010-11 那句是错的**（卷面无堆排序选项，已删）；而 **2022-42 经用户口径拍板后又回到了"算"**——
   最终结论：**8/18 年（选 7 + 大 1）、标 `hot:true`**，详见 §6.2 窗4 ②。
10. ~~DNS 域名解析过程 / HTTP 请求与持久连接~~ **✅ 已完成（窗7）：`js/modules/net-dns.js`（前缀 `_dns`）
    与 `js/modules/net-http.js`（前缀 `_ht`）**，两条目录项均由纯理论卡转 ready。
    两者同属"**时序箭头**"家族（与 `net-tcp-conn` 同范式），**零 CSS / 框架改动**；
    考情从零核定 **3/18（全选）** 与 **5/18（选 4 + 大 1）**，**都不标 hot**；
    验证：Node 冒烟 14124 项断言全过 + 浏览器 96 条遍历 0 报错 + 100 帧几何断言 0 违规（详见 §6.2.5）。
11. ~~电子邮件与 FTP / 以太网交换机自学习~~ **✅ 已完成（窗8）：`js/modules/net-mail.js`（前缀 `_ml`）
    与 `js/modules/net-switch.js`（前缀 `_swc`）**，两条目录项均由纯理论卡转 ready。
    两者同属"**时序箭头**"家族（与 `net-dns` / `net-http` 同范式），**零 CSS / 框架改动**；
    考情从零核定 **9/18（选 8 + 大 1）** 与 **11/18（选 9 + 大 3）**，**都标 `hot:true`（火苗 38 → 40）**；
    **顺带勘误两处**：README 交换机行 9 → 11 年、旧 note 的 2011-47 / 2013-38 两条误登，另补登 5 道邮件题；
    验证：Node 冒烟 18696 项断言全过 + 浏览器 96 条遍历 0 报错 + 160 帧几何断言 0 违规（详见 §6.2.6）。

## §6.4 逐窗复核流水账原文（窗16 从 §6.4 搬出；只搬不改）

- 静态资源版本：**v=53**（**窗16** 递增一次：新增 `js/modules/net-csma.js` 并在 `index.html` 加 script 标签、
  改 `js/app.js` / `js/exam-history.js`，`v=52 → 53`；
  **窗15** 递增一次：新增 `js/modules/net-routing.js` + 改 `app.js` / `exam-history.js`，`v=51 → 52`；
  **窗14** 递增一次：改了 15 个 `ds-*.js` 的 theory（+ 6 个头注释）
  与 `exam-history.js`（删去 `ds-stack-queue` 误登的 2024-42），`v=50 → 51`；
  窗13 是 **v=49 → 50**（10 个 `net-*.js` 的 theory + 2 个头注释）；
  窗12 是 **v=48 → 49**（12 个 `coa-*.js` 的 theory + 7 个头注释、4 个模块的公式定界符、`index.html` 删页脚）；
  窗11 是 v=47 → 48（8 个 `os-*.js` 的 theory 文案）；
  窗10 是 v=46 → 47（framework 定界符修复）；窗8 是 v=45 → 46（新增两个 `net-*` script 标签）；
  窗7 是 v=44 → 45；窗6 是 v=42 → 43（数据对账批）→ 44（两个新模块批））；改任何 js/css 后继续递增
- 预览服务器：`python serve.py 8642`（后台运行，端口占用时先杀旧进程）。
  ⚠ 长会话中途该后台服务可能已挂掉，此时页面只加载一半脚本（表现为"模块少了几个/目录树空"），
  用 `Invoke-WebRequest http://127.0.0.1:8642/index.html` 探活；浏览器实测建议直接用 `file://` 打开，
  本项目按"双击 index.html 即可运行"设计，不依赖服务器。
  （**窗2** 实测 8642 全程活着，但**仍然用 `file://`**——少一个失败面。）
- 浏览器自动化测试：**首选 §3.5-10 的 headless Chrome + CDP 方案**（Node 内置 fetch/WebSocket，
  本机无 playwright；若环境里有 ZCode in-app browser 或 playwright 也可直接用），
  流程是"复位筛选→切书→逐条打开→断言 stage/theory 无'输入有误/is not defined'"+ 新模块逐帧渲染，
  每次改动后建议跑一遍（注意 §3.8-4：同一个自检只能跑一次）。
  **窗2 复核**：96 条目（ready 37 / 纯理论 43 / 建设中 16）零报错、零 JS 异常、零 console.warn。
  **窗3 复核**：同样 96 条目（数字未变）零报错、零 JS 异常、零 console.warn、零 `Runtime.exceptionThrown`；
  net-window 逐帧 ≈260 帧几何断言 + 自动压盖断言零违规（做法见 §3.8-10、脚本模板见 §3.5-21）。
  **窗4 复核**：96 条目（ready 38 / 纯理论 43 / 建设中 15）零报错、零 JS 异常、零 console.warn、
  零 `Runtime.exceptionThrown`；ds-heap 485 帧几何 + 压盖 + 结点相碰断言零违规。
  **窗5 复核**：96 条目（ready 39 / 纯理论 42 / 建设中 15）零报错、零 JS 异常、零 console.warn、
  零 `Runtime.exceptionThrown`；coa-mainmem **547 帧几何越界 0 + 压盖 0**、5 例非法输入在界面上确实弹
  "输入有误"卡、连步/跳帧/播放/重置/改字长/改 lookup/专注模式进出全过、15 张原分辨率截图逐张目视。
  ⚠ **窗5 把"几何断言"的价值说清了**：本窗 3 个真 bug **全部**是它抓的
  （`[object Object]`、文字超宽压邻列、画布右边界越界），**Node 冒烟一条都没抓到**——
  它是"版面/类型串味"的唯一自动防线，**别只跑不读输出**（本窗第一轮 12032 条红、压盖 5548 条，
  如果当成"噪声"略过就会带着一堆版面缺陷交付）。
  **窗6 复核**：96 条目（ready 41 / 纯理论 40 / 建设中 15）零报错、零 JS 异常、零 console.warn、
   零 `Runtime.exceptionThrown`；**本窗无新模块**，故只跑"全站遍历 + 火苗/考情条断言 + 2 张原分辨率截图"
   （精简 harness 见 §6.2.2⑦），另用 `node tmp_readme_audit.js overview|comments` 做数据对账（0 不一致）。
  **窗7 复核**：96 条目（ready 43 / 纯理论 38 / 建设中 15）零报错、零 JS 异常、零 console.warn、
   零 `Runtime.exceptionThrown`；两个新模块 **100 帧逐帧几何断言 0 违规**（越界 / 报文标签两两压盖 /
   标签压生命线标题 / 标题互相压盖）、考情条均 18 格、6 张原分辨率截图逐张目视
   （**本窗唯一真 bug 就是这套几何断言抓的**：一行三箭头标签压盖 → §3.8-14）。
   **窗7 环境实测（与窗4/5 一致）**：workspace-write 下 `chrome --version` 直接报
   `crashpad_client_win.cc OpenProcess: 拒绝访问 (0x5)` ——**先跑这条一行命令坐实"必须提权"**，
   再给 harness 那一条命令加 `sandbox_permissions: danger-full-access`（本窗 2 轮都这么走，没有白跑）。
   **窗8 复核**：96 条目（ready 45 / 纯理论 36 / 建设中 15）零报错、零 JS 异常、零 console.warn、
   零 `Runtime.exceptionThrown`；四个 net 模块 **160 帧逐帧几何断言 0 违规**（越界 / 报文标签两两压盖 /
   标签压生命线标题·**区间带标签·行标签** / 标题互压）、两新模块考情条均 18 格
   （"考过 9 年·共 9 题" / "考过 11 年·共 12 题"）+ **🔥 徽标就位**、8 张原分辨率截图逐张目视
   （**本窗没有版面 bug**：几何断言一次全过；**唯一的真 bug 是编译期就炸的反引号**，见 §3.6.1 窗8 段）。
   **窗8 环境实测（与窗4/5/7 完全一致，第三次复现）**：workspace-write 下 `chrome --version` 报
   `0x5` ⟹ 给 harness 那一条命令加 `sandbox_permissions: danger-full-access`，**1 轮即跑通、没有白跑**。
   **窗9 复核（纯数据窗，零新模块）**：96 条目（**ready 45 / 纯理论 36 / 建设中 15，与窗8 完全一致**）
   零报错、零 JS 异常、零 console.warn/error、零 `Runtime.exceptionThrown`；
   harness 只跑"全站遍历 + 考情条/火苗断言 + 4 张原分辨率截图"那一栏（纯数据窗够用），
   实测 `traverse=96 bad=0`、`coa-baseconv` 考情条 **"考过 10 年 · 共 10 题"**（与改后数据一致）、
   `hotTotal=40`；**本窗共跑 2 轮**（第 2 轮是因为改完 `exam-history.js` 必须复验——
   **顺序教训：先把数据/文档改完再开浏览器，能省掉一轮**）。
   **窗9 另新增一条"非浏览器"防线**：`tools/doc_check.py` 现在会对 45 个模块跑 `node --check`
   （见 §6.2.7⑥），**写完模块先跑它**。
   **窗10 复核（修全站公式显示 bug）**：96 条目遍历 `bad=0`、零 JS 异常、零 console.warn/error、
   零 `Runtime.exceptionThrown`；**装桩验证 KaTeX 定界符与 97 个公式环境**（判据表见 §6.2.8③）；
   `tools/doc_check.py` 全绿（含 45 个模块 `node --check`）；`v=46 → 47`。
   **窗11 复核（theory 文案体检 · OS 批 8 个模块）**：`tmp_t6_harness.js` 96 条目遍历 `bad=0`、
    零 JS 异常、零 console.warn/error、零 `Runtime.exceptionThrown`；`tmp_t10_harness.js` 定界符仍是
    `\(` / `\)`、`cjkInMath=0`、ds-bst-avl 26 / ds-heap 37 / coa-mainmem 12（与窗10 一模一样）；
    **新增 `tmp_t11_theory_harness.js`（理论区专用 harness）**：8 个 OS 模块
    **三问开篇齐、考情块在最后且实测 2 行、考情条 18 格且"考过 N 年 · 共 M 题"与 `exam-history` 逐条相符、
    横向溢出 0px**（`结构不合格=0 考情块超2行=0`），另出 2 张**理论区原分辨率截图**目视
    ——**窗10 的教训正是"理论区排版九个窗口没人看过"，本窗把它变成可回归的一栏**；`v=47 → 48`。
   **窗12 复核（theory 文案体检 · COA 批 12 个模块 + 修公式定界符）**：`tmp_t6_harness.js` 96 条目遍历
   `bad=0`、零 JS 异常、零 console.warn/error、零 `Runtime.exceptionThrown`、`hotTotal=40`、4 张截图；
   `tmp_t11_theory_harness.js`（**IDS 已换成 12 个 coa 模块**）→ **`结构不合格=0 考情块超2行=0 数学环境共=9`**、
   考情条均 18 格且数字逐条与 `exam-history` 相符、横向溢出 0px、2 张理论区截图；
   `tmp_t10_harness.js` → 定界符 `$$` + `\(`/`\)`、`cjkInMath=0`、**`strayBackslash=0`**（新判据）、
   `mathNodes=108`、`renderMathCalls=131`、ds-bst-avl **26** / ds-heap **37**（与窗10/11 一致）、
   `coa-muldiv` / `coa-mainmem` 变成 **0 / 0**（改写后不再用 LaTeX，**这是预期的、不是回归**）；
   `tools/doc_check.py` **新增第 11 项"模块 theory 数学定界符"全绿**（含注入测试：塞一处单反斜杠即 exit 1）；
   `v=48 → 49`。
   **窗14 复核（theory 文案体检 · DS 批 15 个模块，最后一批）**：`tmp_t11_theory_harness.js`
   （**IDS 已换成 15 个 ds 模块、BOOK='ds'**）→ **`结构不合格=0 考情块超2行=0 数学环境共=169`**、
   15 条考情条均 18 格且数字逐条与 `exam-history` 相符（`ds-stack-queue` 已按删掉 2024-42 后的
   **9 年 · 共 9 题** 报出）、横向溢出 0px、`traverse=96 bad=0`、`ERRORS: none`、CDP 零异常、
   2 张理论区截图（`ds-bst-avl` / `ds-sort`）逐张目视；
   `tmp_t10_harness.js` → 定界符 `$$` + `\(`/`\)`、`cjkInMath=0`、**`strayBackslash=0`**、
   `mathNodes=180`、`renderMathCalls=131`；`tmp_t6_harness.js` → `traverse=96 bad=0`、
   `hotTotal=40`（**删掉一处大题登记后仍然 40**）、CDP 零异常；**`v=50 → 51`**。
   **窗14 环境实测（第 8 次复现）**：workspace-write 下 `chrome --version` 无输出
   ⟹ 给 harness 命令各加 `sandbox_permissions: danger-full-access` + 一句理由，
   **3 次提权调用全部 1 轮跑通**（跑前照例 `Stop-Process chrome` + 删 `tmp_chrome_profile`）。
   **窗13 复核（theory 文案体检 · NET 批 10 个模块 + 修 t10 判据误报）**：`tmp_t11_theory_harness.js`
   （**IDS 已换成 10 个 net 模块**）→ **`结构不合格=0 考情块超2行=0 数学环境共=2`**、10 条考情条均 18 格
   且数字逐条与 `exam-history` 相符、横向溢出 0px、`traverse=96 bad=0`、CDP 零异常、2 张理论区截图目视；
   `tmp_t6_harness.js` → `traverse=96 bad=0`、`hotTotal=40`、CDP 零异常、4 张截图；
   `tmp_t10_harness.js` → 定界符 `$$` + `\(`/`\)`、`cjkInMath=0`、**`strayBackslash=0`**
   （**修正判据之后**；修正前它误报 `net-window[\frac,…]`，见 §6.2.11④）、`mathNodes=104`（108→104 是
   `net-shannon` 去掉 4 处简单行内公式所致，**预期**）、`renderMathCalls=131`、ds-bst-avl **26** / ds-heap **37**（与窗10/11/12 一致）；
   **`v=49 → 50`**。
   **窗13 环境实测（第 7 次复现）**：workspace-write 下 `chrome --version` 无输出（与窗12 表现相同）
   ⟹ 给 harness 那三条命令各加 `sandbox_permissions: danger-full-access` + 一句理由，
   **3 次提权调用全部 1 轮跑通**（跑前照例 `Stop-Process chrome` + 删 `tmp_chrome_profile`）。
   **窗12 环境实测（第 6 次复现）**：workspace-write 下 `chrome --version` **无任何输出**
   （窗7/8/9 记的 0x5 报错行这次没打，但同样是"起来又被杀"）⟹ 加
   `sandbox_permissions: danger-full-access` + 一句理由，**3 次提权调用全部 1 轮跑通**。
   **窗9 环境实测（与窗4/5/7/8 完全一致，第四次复现）**：workspace-write 下 `chrome --version` 报
   `crashpad_client_win.cc: OpenProcess: 拒绝访问 (0x5)` ⟹ 给 harness 那一条命令加
   `sandbox_permissions: danger-full-access` + 一句理由，**1 轮即跑通、没有白跑**。
   ⚠ **窗9 新增一条 python 侧的同类限制**：`subprocess(capture_output=True)` 在沙箱下同样走命名管道、
   报 `PermissionError: [WinError 5]` ——**要给"检查子进程输出"的脚本改用"重定向到临时文件再读"**。
   ⚠ **窗4 修正了本条的"遍历 96 条 0 异常"口径**：它确实能抓到"默认视图弹报错卡"这类问题
  （ds-heap 的 `default` 缺失就是它报的 `stage err ds-heap`）——**别把它当成走过场**；
  但它**只看得见默认输入**，所以 §3.8-11 的冒烟断言仍然必须补上。
  **窗4 环境实测（与 §3.5-10 有出入，务必按新的来）**：headless Chrome 在 **workspace-write 沙箱下
  会半途被杀**（`/json/version` 能返回 200、紧接着 Chrome 进程消失、9222 再也连不上，
  Node 侧表现为 `/json/new` 后无声无息地结束），**必须提权到 danger-full-access 才跑得完**；
  前一次残留的 `tmp_chrome_profile` 会让下一次连 9222 都起不来，**重跑前先杀 chrome + 删 profile**。
  **窗5 复现并细化**：不提权时**表现完全一样**——`CDP ok: Chrome/153…` 打印出来后进程静默结束、
  连截图一行都不输出（本窗第一次跑就是这样）。**判据：只有看到 `shot: 01_…` 与 `==== 完成 ====` 才算跑完**；
  提权的正确写法是给**那一条 pwsh 命令**加 `sandbox_permissions: danger-full-access` + 一句理由，
  命令本身不用改（本窗连跑 6 轮都靠这一招）。

## §7 开场提示词模板原文（窗16 降级为 skill 指针时搬出；只搬不改）

> 用法：复制下面 ```text 里的整段到新窗口，只填"本次任务"一节。
> ⚠ 本模板**只写"接手时必须照做的动作与去哪查"**，不重复正文内容、不写死窗号/条目号
> （规则与细节一律按 §1.7 的正文，避免模板过时）。
> ⚠ **给出"换窗"建议的窗口，必须在回复里把本段整段贴出来**（§3.7-6③，用户 2026-09-21 要求）。

```text
先读 docs/handover.md 并严格遵守。**第一件事：读 §3.5.0 的 8 条闸门并照着念一遍，然后按 §3.9 流水线开工**
（本流程以"节约成本"为第一优先：能省则省、但不许跳过闸门的"跳过条件"之外的步骤）。

重点：
· **先读 §0「新窗最快上手」**（§0.1 总导航 + §0.2 三分钟进场 + **§0.3 问题检索表** + §0.4 任务检索表），再按下面的节展开
· **不要通读全文**（全文三千余行 / 18 万字符，开工用 §0.2 第 6 步的探针现算）：按 §0.3 / §0.4 定位，或 `grep -n` 后只读那一小段；
  要改 handover 本身，先读 **§8 handover 排版法**（含四条机器硬约束）
· 铁律与规则：快照模式（§1.3）、模块契约（§4.2）、命名前缀（§3.1-3）、火苗口径（§1.5）、
  协作与 handover 优先级（§1.7）、Git 规则（§1.6：未经我要求不 commit/push）
· 先读 §6.5【窗口建议】与 §6.6 窗号台账（**本窗号 = §6.6.0 摘要表最上一行 + 1**，收尾一律署这个号），
  按 §3.7 再判一次本窗批次与是否换窗
· 先读 §3.8（通用验证方法论）；再看 §3.6（模型专属坑，按模型分小节）——
  有你模型的小节就照做，没有就自己新开 `#### 3.6.N <模型名>`（只写自己特有的失误倾向）
· 查真题只走本地缓存（§3.5-12）：tools/exam_index.py --ask 关键词 / grep 考情缓存/；不转抄、不凭记忆、不开 PDF
· **改任何文档里的考频/计数数字前，先跑 §3.5-24 的对账脚本**（窗6 留档的 `tmp_readme_audit.js`：
  `check` 一条命令跑 **overview / comments / t5chk / years 四个模式**，必须全 0 不一致才 exit 0）

━━━ 本次任务 ━━━
任务：〔默认：按序推进，即按 §6.5 的窗口建议做；要改做别的就写明〕
本窗批次：〔做模块填 1 或 2 个（按 §3.7 选：同书 + 同范式 + 不动 framework/css）；
  做 theory 体检就填"哪一本书"——§3.8-16 要求分批、每批一本书（**窗11 OS 批、窗12 COA 批、
  窗13 NET 批、窗14 DS 批均已完成，45/45 收口；再做就是新增模块时的增量维护**）〕
补充要求：〔可选：真题预设、界面细节、范围限制等；没有就删掉这行〕
━━━━━━━━━━━━━━

验收（按 §3.9 双轨表选一种）：
〔A. 做模块〕① 接线五件套（模块文件 / app.js 转 ready / exam-history / index.html 的 v+1 / README）；
  ② Node 冒烟全过（含 §3.8-11 默认输入、§3.8-12 量纲、§3.8-13 类型串味）；
  ③ 浏览器实测新模块 + 收尾全站遍历一遍零报错。
〔B. 纯文档/数据对账（零新模块）〕① 对账脚本 0 不一致（`tmp_readme_audit.js check`，四模式）；
  ② `tools/doc_check.py` 全绿；③ 全站 96 条遍历零报错 + CDP 零异常（`tmp_t6_harness.js`，**提权**）。
〔C. theory 文案体检（只改文案、零逻辑）〕① 逐条落实 §3.8-16 的"三问 + 四块"（考情块放**最后**、≤2 行）；
  ② `tmp_t11_theory_harness.js`（**提权**）报 `结构不合格=0 考情块超2行=0` + 理论区截图目视；
  ③ 与 B 同：`doc_check.py` 全绿 + `readme_audit check` 0 不一致 + `t6` 96 条零报错；
  ④ 若动过公式，再跑 `tmp_t10_harness.js`；⑤ 改了模块 js 就要 `v+1`（§3.3-17）。

收尾（顺序即优先级）：① 回写并通读 handover（§1.7-5：过时条目就地改写、模型坑归 §3.6、经验署窗号）；
② 跑 tools/doc_check.py 与 tools/exam_index.py --stats；③ 覆盖更新 §6.5【窗口建议】、§6.6.0 摘要表追加一行，
**并把本段提示词整段贴给用户**（§3.7-6③）。
```

---



## §3.6.1 逐条实测原文（窗20 从 §3.6.1 压缩时搬出；**只搬不改**，逐字保留）

#### 3.6.1 DeepSeek-V4.1-Flash（依据：窗0 ds-dfs-bfs、窗1 ds-btree、窗2 ds-bst-avl、窗3 net-window、窗4 ds-heap、窗5 coa-mainmem、窗6 README/handover 对账、窗7 net-dns+net-http 八窗复盘、窗12 COA theory 体检九窗复盘、窗14 DS theory 体检十窗复盘、窗16 net-csma 十一窗复盘、窗17 skill 基建 十二窗复盘）
- **可观测的长上下文劣化**：八窗的失误都集中在"多次工具调用之后"——窗0 并发跑两次自检、把 4 个芯片
  看成 5 个、把手算常量当真理；窗1 的三个真 bug 里**两个是自己算的坐标/边距出错**（"根"标签越界 4.7px、
  叶链箭头压住叶框）；窗2 的唯一真 bug 也是**自己写的渲染语义错**（双旋第 2 步沿用型判定时的 y 去上色）；
  窗3 的 1 个真 bug 又是**自己算的图元宽度**（ACK 片比数据片宽 10px → 压住面板 2px，见 §3.3-20）；
  窗5 的 3 个真 bug 里的 2 个仍是**自己算的版面常量**（行标签文字超宽压邻列、画布右边界算小）。
  **对策**：把"每模块一收尾"当硬边界（§3.7-4 熔断线），别拿"感觉上下文还够"当继续的理由。
- **窗3 的正面证据（要保留的好习惯）**：窗3 的**三个逻辑 bug 全部是 Node 冒烟在开浏览器之前抓到的**
  （§3.2-5 的 ②③ 与"窗口填满后误判为无事可做"），**一个都没漏到浏览器阶段**——
  印证 §3.5-2"先 Node 冒烟再开浏览器"确实省事；而**唯一的渲染 bug（压盖）又是截图抓到的**。
  两头分工清楚：**逻辑靠冒烟、版面靠截图**。
- **窗5 的分工再确认（与窗3 一致，且更极端）**：窗5 的**逻辑/数值 bug（量纲错）全部是 Node 冒烟抓的**
  （红过 5 轮 4.6 万条，都在开浏览器之前），**三个版面/类型 bug 全部是浏览器的几何断言抓的**
  （`[object Object]`、文字超宽、画布越界，冒烟全绿）。**"先冒烟、再浏览器几何断言、最后原分辨率截图"三步一个都不能省。**
- **该模型的具体倾向 → 对应通用对策**（对策本体写在通用节，此处只做索引）：
  | 倾向 | 实测表现 | 对策 |
  | --- | --- | --- |
  | 把手算的常量/序列当真理 | 断言"退栈序列应该是 FEDCBA""init 帧就该有层号"，两次误报模块 bug（窗0） | §3.8-1 |
  | 把"自己写的文案"当成事实 | 预设标签写"借位 → 合并 → 根降高"，而那条删除序列**从没出现 shrink 帧**（窗1） | §3.8-6 |
  | 把过渡帧当合法状态套公式 | 208 条红全部来自对 overflow/underflow 帧套"高度上下界"公式（窗1） | §3.8-2 |
  | 重复/并发跑同一个自检 | harness 自动跑一次 + CDP 又跑一次 → 误报 40 条"交互卡被隐藏"（窗0） | §3.8-4 |
  | 用缩略图下结论 | 把 4 个芯片看成 5 个（窗0）；窗3 又从缩略图误判"接收方把没收到的 F1 涂成紫色"——用脚本回读 `fill` 才证明**配色是对的、是预览分辨率骗人** | §3.8-3③ |
  | **把"断言全绿"当成"渲染没问题"** | 双旋第 2 步把 y 标错人，13 万项断言毫无反应（窗2）；窗3 的 ACK 压盖同样全绿（窗3） | §3.8-8 / §3.8-9 / §3.8-10 |
  | **自己算的图元尺寸不核对** | ACK 片宽 `flyW+10` 却沿用数据片的行程 → 越界 2px（窗3） | §3.3-20 |
  | **环境一次没通就下结论"改了没生效"** | harness 被 `file://` 缓存住，旧异常照旧报（窗2）；窗3 又两次误判 `clip` 截图（其实是 clip 的坐标语义问题，不是代码） | §3.5-10② / §3.5-21③ |
  | 文档攒到最后才改 | 中途被 "file changed since it was read" 拒绝，只能重读（窗0） | §3.5-17 |
  | **"非算法"的表单/契约细节容易漏** | `ins` 输入框漏写 `default` → 默认视图弹"输入有误"；87 场景 77 万断言**全绿**没抓到（窗4） | §3.1-6 / §3.8-11 |
  | **量纲/单位换算会连续错好几轮** | 窗5 主存芯片扩展模块把"片容量 K 值"既当字节又当单元，**同一个换算错了三次**（漏乘、多乘、又漏乘），红过 3 轮共 4.6 万条，全是模块自己的；渲染端一度打出 `NaNK×undefined`（窗5） | §3.8-12 |
  | **把"快照里的数组字段"当标量用** | 窗5 渲染端写 `s.rows`（本意是"行数"）→ 实为**行区间数组**，译码器文字渲染成 `4 线 — [object Object] 线译码器`；**Node 冒烟只查 undefined/NaN，查不出 `[object Object]`**，是浏览器几何断言报"文字超宽 2315px"才暴露（窗5） | §3.8-13 |
  | **用手写几何常量摆版面，不按最长文字校验** | 窗5 行标签写 `CS0: 0x0000 ~ 0x07FF` 一行 → 在 74px 芯片 + 172px 列距下**横向压到下一列芯片**；把 SVG 缩到 1100px 后按 1:1 字号更明显（窗5） | §3.3-21 |
  | **"生成的脚本/模板字符串里"乱用转义/反引号** | 窗6：harness 页面脚本里 `\[object Object\]` 退化成字符类 → 在正常 HTML 上报红一轮；同一文件注释里写反引号又把模板字符串截断一次。**窗8 复发**：net-switch 的 **theory 模板字符串**里用裸反引号写 Markdown 行内代码 → 整个模块文件 `SyntaxError`。**窗9 第三次复发**：改 `ds-hash.js` 的 theory 时又写裸反引号 → `SyntaxError`；**更说明问题的是，本窗写"修复脚本"时又栽两次**。**窗16 第四次复发**：写"handover 减重器"时把新文本里的行内代码（`default`、`stage err ds-heap` 等）**裸写进模板字面量** → `SyntaxError` 卡在第一行就停住了 | §3.5-10⑤ / §3.2-11 / **§6.2.7⑦（含"用 ``` 占位符 + `String.fromCharCode(96)` 替换"的可照做写法）** |
    | **"文档里的数字"凭印象照抄，不做机器对账** | 窗6 实测：README 26 行还在用 17 年分母、模块总览表 12 行错值、handover §2 十处错值、`exam-history.js` 注释 20 处分母 + 8 处计数错、火苗漏标 1 条——**全是历窗手打、被 5~6 个窗口的人眼放过**。**窗12 在同一天换了个位置复发**：12 条 `coa` theory 里 **9 条**数字错（`14 年` / `11 年` / `10 年` / `13 年`…）**+ 7 个头注释**同错，**全是照抄旧文案**；连 handover §2.2 也留着一条"窗9 回滚了、行没跟着改回"的旧结论 | §3.5-24 |
     | （同上，**窗13 第三次复发：theory 与头注释**） | 窗13 的 net 批又实测 **4 处**：`net-subnet`"14 年"→**15 年**（且大题年份把 **2014 写成 2022**）、`net-tcp-conn`"7 年"→**8 年**、`net-tcp-cc`"8 年"→**9 年**（theory 与头注释同错）、`net-shannon` 头注释把 **2010** 当考点却漏 **2013 / 2026**——**OS / COA / NET 三批全靠"逐条重写时现算"才发现，四模式对账与 `doc_check.py` 一处都查不到** | §3.5-24 / §3.8-16 |
    | **手里有规则却不照它判（窗9 新增）** | 窗9 裁决"选择题重复登记"时，`coa-baseconv` 自己的登记规则（"属**嵌入型**考点：机器数书写 / **地址展开** / 机器码写十六进制，第一步都要做进制转换"）就在**同一屏的注释里**，却按"还有没有更专的模块"把 **2022-15 误删**（该题解析明写"查找时要先将虚页号转换为十进制数"），事后回滚、白改 3 处文档 | §6.2.7④ |
    | **扩展"文档核对脚本"时先写判据、不先打印中间量（窗9 新增）** | 窗9 给 `tmp_readme_audit.js` 加 `years` 模式（比对年份清单）时**连续 5 轮假红**（49→32→28→6→3→0 行），每轮都是"解析散文单元格"的坑。**根因与 §3.5-22 同源**：应先写十几行把 `selPart/bigPart/iSel/iBig` **打印出来对一眼**再写判据。**这条不只适用于"单位换算"，也适用于"解析别人写的散文"** | §3.5-22 / §6.2.7⑤ |
    | **"共享内核的渲染路径"从没被自己人验证过（窗10 新增）** | `js/framework.js` 的 KaTeX 定界符写成 `'\\`('（JS 无效转义 → 就是裸括号），**九个窗口、每次收尾都跑"96 条遍历零报错"却一次没发现**——因为那个检查只断言"没报错/没 NaN"，**而公式显示错误既不报错也不产生 NaN**；九窗所有浏览器实测**都只看 `#stage`（展示区）**，**theory 区（理论区）的排版从来没人目视过**。用户一看截图就抓到了。**对策：把"理论区公式"也纳入收尾检查**（窗10 的 `tmp_t10_harness.js` 装桩抓定界符 + 数学节点数），**判据：改了 `framework.js`/`RC408.md`/`renderMath` 任一处，必须跑一次** | §6.2.8 |
     | **"theory 里写裸尖括号"会被浏览器当 HTML 标签吞掉（窗14 新增）** | 窗14 写 ds-dijkstra 时把"松弛条件"写成 `dist[u]+w<dist[v]`（数学环境里一个裸 `<` + 字母）：`RC408.md()` 不转义 `<`，浏览器把 `<dist[v]...` 当标签，**理论区整块文字消失**、t11 报 `数学=8(不配对)`；ds-topo 的 `<i,j>` 同病（`<i` 被当斜体标签） | §3.5-18「窗14 新增」的写盘前判据 + §0.3 检索表 |
    | **"同一行里纵向堆叠的图元"只算坐标、不算占位** | 窗7：net-http 一个 RTT 行内 3 条箭头，按公式把 y 铺开"看着合理"，实际 10.5px 字体行高 11~12px > 10px 间距 → 相邻标签压盖；**Node 冒烟 14124 条断言全绿**，只有逐帧几何断言（`labelOverlap=16`）抓到 | §3.8-14 |
  | **沿用旧文档里的考情结论，不当场回缓存复核** | 窗7 开工时 app.js 的 `net-http` 旧 note 写着"含 2010-40（Web 时延）"与"2011 持久非流水线 7 RTT"——**两条都错**（2010-40 是纯 DNS 题；2011-47(3) 官方答案是 6 RTT、且旧 note 还漏登了这道大题）。子代理回 `考情缓存/` 逐题核 + 主线程复核原文后改正（§6.2.5①） | §3.5-12 / §3.5-20 |
     | **"生成的页面脚本"里把变量名当值用（窗15 新增）** | 窗15 的 harness 截图脚本写成 `setv('edges', E4J)`（`E4J` 是 Node 侧常量），页面里 `E4J` **未定义** → 输入框被填成字面量字符串、`parse` 报"格式错误"，**截图却照旧出图**（画面上是报错卡 / 上一个知识点），差点误判成"模块没问题"。**对策**：往"生成的页面脚本"里塞的值一律在 Node 侧**插值成字面量**（`${JSON.stringify(v)}`），生成后过一遍 `node --check`；**截图必须走 UI**（`#in-<key>` → `#ctl-load` → `#ctl-seek`），别直接改 `Runner.model/snaps` 再 `paint()`（`def` 还是上一个知识点的） | §6.2.13⑦ |
      | **该 `.join('')` 的数组直接插进模板字符串（窗16 新增）** | 窗16 写 net-csma 的时空图时，刻度元素的变量名是 `tickEls`，SVG 模板里却插成了 `${ticks}`（**数组**）→ 渲染出 `[object Object],[object Object],[object Object]`、三条刻度线全丢。**是同一轮 Node 冒烟报的 89 条"脏文本"抓到的**（断言里本来就有 `[object Object]`，见 §3.8-13）——**教训：`render()` 里插的到底是字符串还是数组，要靠冒烟那条断言兜底，别靠眼睛**（浏览器侧只看得到"少了几条线"） | §3.8-13 |
      | **派生量函数"只返回算出来的量"，解说文案却引用原始量（窗16 新增）** | 窗16 的 `_csDerive()` 只返回 τ / 2τ / 最小帧长等派生量，**没把 `rate`/`dist`/`hubDelay` 带出来**，而解说文案要写"单程传播时延 = 距离/速率 + 设备延时 = 25.6 + **NaN**" → 快照 `desc` 里出现 NaN。同一轮冒烟的"快照/文案无脏值"断言抓到。**对策：渲染与文案要用到的每个量，要么进快照、要么进派生量对象——不许"以为它在里面"**（与 §3.1-1 快照自包含同源） | §3.1-1 / §3.8-13 |
      | **同一个状态在两处各判一次（窗16 新增）** | 窗16 退避表的高亮写成 `r.i === s.curI`（**渲染端又自己算了一遍**"谁是当前行"），而快照里已经给了 `state: 'cur'/'done'/'future'`；两者在 done 帧不一致 → **done 帧仍高亮某一行**。被冒烟"done 帧不该再高亮"的断言抓到。**对策：渲染只读快照给的状态，不在渲染端重算"当前是谁"**（§1.3"渲染只读快照"的具体形态） | §1.3 / §3.8-15 |
       | **照抄 backlog / 旧文案里的"实现形式"，不先探本机环境（窗17 新增）** | §6.2.14⑩① 原文写 `scripts/gate.ps1`，照写之后连踩两次 `ParserError`：本机 `pwsh` 实为 **PowerShell 5.1**，**无 BOM 的 `.ps1` 按 ANSI(GBK) 解码、中文字符串会把紧随的引号吃掉**，而 Edit 工具重写文件又剥掉 BOM。**对策：原文给的是"形式建议"（那里本就写了"`.ps1` 或 `.js`"）时，先探一眼本机环境再定形式；本机工具脚本用 `.js`** | §3.5-9⑥ |
- **各窗"红"的归因（可核对）**：**红色绝大多数出在自己写的断言上，先怀疑测试**——但窗4/窗5 是例外年
  （窗4：冒烟 77 万断言全绿也没抓到"input 缺 `default`"；窗5：红的大头是模块自己的量纲错）。
  逐窗明细见下表，过程与证据见各窗的 §6.2.N（**本节不再复述**）。

  | 窗 | 真 bug | 红 / 返工的归因（哪一类） | 详见 |
  | --- | --- | --- | --- |
  | 窗0 | 3 | 5 条红里 4 条是测试脚本问题 | §6.2 |
  | 窗1 | 3 | 208 条红里 **0 条**是模块 bug | §6.2 |
  | 窗2 | 1 | 截图发现（非断言）；唯一那条断言红是测试脚本自己的期望写错 | §6.2 |
  | 窗3 | 4 | 3 个逻辑 bug 由冒烟抓、1 个压盖由截图抓；冒烟红 3 轮 1900 余条、**0 条模块 bug** | §6.2 |
  | 窗4 | 1 | input 缺 `default`：**冒烟 77 万断言全绿没抓到**，浏览器全站遍历报 `stage err ds-heap` 才暴露；冒烟红 2 轮 9.7 万条全是测试脚本自己的 | §6.2 |
  | 窗5 | 3 | **全是模块自己的**（`s.rows` 当标量、行标签超宽、画布右边界算小），且**全部由浏览器几何断言抓到**；冒烟红 5 轮约 4.6 万条（约 4.3 万条是模块量纲错） | §6.2.1 |
  | 窗6 | 0 | 零新模块零真 bug；红全来自**机器对账脚本**（火苗漏标 1 + 题数当年数 1 + 注释 28 处 + README 与 §2 共 22 处） | §6.2.2 |
  | 窗7 | 1 | 版面（一行 3 箭头压盖）：**冒烟 14124 全绿查不出**，harness 几何断言报 `labelOverlap=16`；冒烟红 3 次 406+315+280 全是测试脚本自己的 | §6.2.5 |
  | 窗8 | 1 | 编译期 `SyntaxError`（theory 里裸反引号，require 一次即暴露）；**零断言红**（冒烟 18696 全过、harness 160 帧 0 违规） | §6.2.6 |
  | 窗9 | 0 | 零新模块；红全出在脚本自己的解析（`years` 5 轮假红）；模块只有 1 处自己改错（裸反引号）；唯一真错是**判断错**（2022-15 误删后回滚） | §6.2.7 |
  | 窗10 | 0 | 既不是算错常量、也不是测试写错，而是**"有一条渲染路径从来没人验过"**（KaTeX 定界符，九个窗口都没发现） | §6.2.8 |
  | 窗11 | 1 | **不在模块里**（handover 被上一个窗口写成三份重复）；教训是把"对账脚本的绿"当成"文档全对" | §6.2.9 |
  | 窗12 | 0 | 模块逻辑 0、版面 0；出问题全在"别人留下的文本 / 我自己写的检查器" | §6.2.10 |
  | 窗13 | 0 | 模块逻辑 0；文本 4 处考情数字错 + 判据把正确公式判红 1 处；**判据报红先问它在这批数据上成不成立** | §6.2.11 |
  | 窗14 | 0 | 模块逻辑 0；文本 6 处错值 + 内容错 1（2026-8 答案 5 → 2）+ 数据误登 1（2024-42）+ 新形态渲染坑（裸尖括号吞掉理论区） | §6.2.12 |
  | 窗15 | 3 + 版面 1 | 测试脚本返工 3 次；harness 截图脚本把 Node 侧常量名当值用（截图却照旧出图） | §6.2.13 |
  | 窗16 | 4 | **全是自己写的**（3 个渲染/文案读出脏值 + 1 个建模口径），3 个被同一轮冒烟抓到、1 个在写断言前发现；冒烟与 harness 各 1 轮全绿。**另有 3 处不是模块的**：手改 `OV`/`KEYS` 时漏看覆盖（见 §3.5-24）、减重后 `tmp_tmp_audit` 误报 6 个模板"引用 0 次"、减重器自己踩了裸反引号老坑 | §6.2.14 |
- **该模型值得保持的强项**：子代理并行做真题核对、主线程同时写模块（窗3 用它换回"3 误登 + 3 漏登"，
  见 §3.5-20）；先写 Node 冒烟再开浏览器（窗1 靠它一次抓到"删除帧里同一关键字出现两处"；
  窗3 靠它一次抓到三个死循环/伪确认 bug）；接线五件套一次配齐、只跑一遍全站遍历；
  用假 `rt` 跑全部 `quickActions` 自驱动覆盖预设（§3.5-15）；
  **批量改写文本时用"数据文件 + 锚点写入器 + 统一转义"（窗12 新增，见 §3.5-18 窗12 条）**。
- **窗5 的时间/成本复盘（窗5 写、窗16 压缩）**：本窗明显比窗1–窗4 费 token，四笔大头（按代价排序）——
  ① **量纲返工 5 轮**（根因：先跑全量再反推期望值；更值得记的是**第一版冒烟"全绿"其实是错的**）；
  ② **构造场景手试十余组**（应用"解等式或小脚本枚举打表"，不是手试）；
  ③ **6 轮 harness 复跑**（3 个版面 bug 分开发现，其中 1 轮还是提权前白跑）；
  ④ **PowerShell 改 `index.html` 编码损坏、只能整篇重写**（自检与 `doc_check` 都查不出编码问题）。
  **一句话**：耗时不是"读文档 / 查真题"这类固定成本造成的，而是**"验证顺序错" + "工具选错"**——
  这两条恰好都能模板化，故沉淀为 §3.5-22、§3.5-23、§3.5-9⑤。

## §6.5 逐窗窗口建议原文（窗23 从 §6.5 搬出；只搬不改，逐字保留）

> 规矩：`§6.5` **只留最新一窗的完整建议**（顶部标 `【窗N】`），更早各窗搬到这里并按窗号 grep；
> §6.6.0 摘要表那一行仍保留每窗的"该窗建议（一句话）"。


**【窗22】**（2026-09-25 · 修全站公式显示（CDN + 定界符两颗地雷）+ 新增 `net-udp` 理论卡 · 详见 **§6.2.20**）

- **本窗交付（两条用户报的问题都不是"表面那一个模块"）**：
  ① **公式显示**：根因 A = **KaTeX 走 CDN，断网即全站退化**（`RC408.renderMath` 在 KaTeX 缺席时静默 return）；
  根因 B = **`js/app.js` 81 处行内公式的定界符只写了单反斜杠**，被 JS 模板字面量当无效转义吃掉，
  **运行时定界符根本不存在（联网也不排版）**。修法 = `tmp_fix_delim.js` 补双反斜杠
  + `framework.js` 新增**不依赖外网的兜底渲染器**（27 种命令 / 281 处公式全覆盖）。
  **改动面**：`js/framework.js` + `js/app.js` + `css/style.css` + `index.html`（`v=59 → 60`）
  + `tools/doc_check.py`（第 1 项锚点去掉了写死的"96 条目"）。
  ② **UDP 缺口**：`app.js` 第 5 章新增 `net-udp` 卡片（33 号理论卡）+ `exam-history.js` 登记 **4/18 年（选 5）**
  + README 两张表 + `tmp_readme_audit.js` 三处同步。
- **信号**：真 bug **2**、判据失效 **1**、测试脚本自身返工 **多次**（同一族坑连踩 4 次，见 §6.2.20③）：
  ① 判据失误：`doc_check` 第 1 项把 `96 条目` 写进正则锚点 ⟹ 条目数一变就静默失效（§3.8-17 第六次复发）；
  ② 方法失误：**拿正则扫源码文本来断言"数据里的字符串"**，把源码写法当成运行时写法，白烧十几轮
  （已沉淀为 **§3.8-19**，并新增 §3.6.1 三条本模型倾向）。
- **验收**：`tmp_math_smoke.js` **1568 条断言 0 不合格**｜`tmp_math_browser.js`（**离线**）
  **遍历 97/97、`frac=4`、`known=0`、残留反斜杠 0、几何越界 0、真实 console 错误 0** + 理论区原分辨率截图目视｜
  `tmp_t21_harness.js --run`：`traverse=97 bad=0`、`theoryCards=33 结构不合格=0 考情块超2行=0`｜
  开工与收尾 `gate.js` 三条硬门全绿、`doc_check` 15 项全绿、`readme_audit check` 四模式 0 不一致。
  **计数变动**：条目 96 → **97**、纯理论 32 → **33**、exam-history 55 → **56**、`v` 59 → **60**（hot 仍 43）。
- **下一窗建议（首选）：支线 §6.7 第 2 块（`§0` 入口，≈2~3K/窗）**——已连挂四窗未做，成本与风险都最低。
  若继续模块轨，首选 **`ds-extsort`（外部排序 · 败者树）**（复用 `ds-heap` / `ds-sort` 的表格与槽位做法，
  不动内核），次选基数排序 / 动态分区 / 位示图 / 分层模型 / 编码调制（§6.3-8 的 live 清单）。
  其余备选：**"漏登"专项**（§6.3-3，本窗又给它添了一条证据：UDP 此前**全站零登记**，
  正是"该登记却没登记"的又一例——所以那个专项确实值得做）。
- **照抄**：① 写"要做断网兜底的渲染器"照 `js/framework.js` 的 `RC408.mathFallback` + `tmp_math_smoke.js`
  （**从真源取数**：在 `vm` 里跑源文件接 `registerModule` / `RC408.books`，别拿正则扫源码）；
  ② 跑离线浏览器实测照 `tmp_math_browser.js`（**`Network.setCacheDisabled` + `--disable-http-cache`
   + 注入脚本用字符串拼**，三个坑都写在头注释里）；
  ③ 批量改文案/定界符照 `tmp_fix_delim.js`（带"改前改后不变量核对 + dry-run + 逐条 diff"）。

**【窗24】**（2026-09-25 · 理论区「手动标注」：拖选变红 / 再选恢复 / 刷新保留 · 详见 **§6.2.22**）

> 下面的"【窗口建议】"是**收尾最终版**（模板见 §3.7-6）。
```
【窗口建议】建议：换窗
- 本窗已完成：0 个模块（**平台级功能轨**）＝ 新增 `js/annotate.js`（理论区手动标注）+ 3 个渲染分支挂 hook
  + 本地 CSS（`.kp-anno` 只改颜色）+ `index.html` 两个按钮（`v=62 → 63`）；**同窗第二批**＝"全库快捷预设"
  体检与修复（`js/framework.js` 的 `setInput` 护栏 + 5 个模块文件，`v=63 → 64`）
- 信号：真 bug **7 处**——1 处是标注功能自身的（占位页标注会挂到上一个知识点 id 上，**收尾自审发现、已修并配断言**），
  另 6 处是**预设/图面**问题（5 个模块：2 处"只剩 0 / 1"、2 处"一点预设就报错"、1 处曲线起点与丢包峰值口径、
  1 处荒谬的溢出文案；**均由新写的"预设体检两件套"扫出来、逐条修掉并复验**）｜
  **用户可见事故 1 起**（我按进程名杀浏览器，把用户正在用的 Edge 全关了——已改写 §3.5-10 并同步 skill）｜
  测试脚本（harness）自身返工 6 次（滚动后坐标失效 / 按钮被滚出视口 / 跨块目标挑错 / 公式目标挑错 /
  日志只取头部导致断言假红 / **模板字面量注释里的反引号**）｜判据失效 0 次｜文档状态返工 1 次（误删 §6.3 节头空行）
- 判定：**命中 §3.7-3 "必须 1 个/窗"**（本窗动了 `js/framework.js` + `css/style.css` ＝ 共享内核；
  第二批是**用户中途插需求**引出的修 bug，按 §1.7-1 当场修，不再另开新材料）⟹ 同窗不再开第三批；未触 §3.7-4 熔断线
- 下一窗建议（首选）：**§6.7 支线第 2 块（`§0` 入口减重，≈2~3K/窗）**——已连挂六窗；或模块轨 `ds-extsort`
- 次选（同一功能的小增量，风险低）：给标注补**导出/导入**与**触屏 / 键盘选区**（§6.2.22⑨）
- 若继续本窗：不建议——本窗已改 `framework.js`/`style.css` 与 5 个模块文件，再加改动会让
  "标注 66/66、预设 32/32（Edge + Chrome 各一遍）"那批证据过期
```

- **本窗交付明细（用户原话："给每条纯理论都加上历年考察分布行"）**：
  ① **数据**：33 张 theory 卡**全部登记**（原来只有 6 张有数据 ⟹ 其余 27 张打开时整条分布条**不渲染**）。
  总数 56 → **83 条**，其中 2 个是**空数组**（`ds-unionfind` / `ds-rbtree`：考纲新增、真题暂无 ⟹ 18 格全灰）。
  ② **取证与勘误（★ 不许照抄卡片文案）**：四科各派一个子代理按 `考情缓存/` 逐条核 + **一个对抗式复核**，
  我再抽查关键条目。**删 5 处误登**（`coa-hierarchy` 2018-12 = "为什么用二进制"；`coa-bus` 2017-19 =
  五段流水线数据通路；`net-swap` 2022-33 = OSI 各层流量控制 ⟹ 改挂 `net-layers`、2024-33 = 带宽 vs 吞吐量
  ⟹ 改挂 `net-perf`；`net-tcp-reliable` 2019-39 / 2022-39 = 三次握手确认号 / 连接释放 ⟹ 归 `net-tcp-conn`），
  **改挂 4 处**（2017-18 → `coa-microprog`、2025-16 → `coa-hierarchy`、2021-32 → `os-intro`、
  2026-21 → `os-ioctl`），**补漏 35 处**。
  ③ **`framework.js` 两条新口径**（也写进 `exam-history.js` 头注释）：**空数组 = 0 年条**（照样渲染
  18 格全灰 + "考过 0 年 · 共 0 题"）、`n: ''` = 该年考过但**题号查不到**（格内显示 `—`）。
  ④ **20 张卡的考情块数字全部按现算重写**（旧文案普遍偏低：`net-encode` 3 → **8** 年、`os-classic` 5 → **15** 年）。
  ⑤ `v=61 → 62`（`index.html` 54 处 `?v=` 同步）。
- **验收**：`node tmp_t23_harness.js --run` ⟹ **`theoryStripCheck: cards=33 分布条不合格=0 结构不合格=0
  考情块超2行=0 0年卡=2(格数/配色不合格 0)`**、`traverse=97 bad=0`、CDP 零异常 + 4 张截图目视
  （33 条并排总览 / `ds-unionfind` 0 年条 / `os-ipc` 只记年份 / `coa-bus` 最密）；Node 侧四件套
  `tmp_t23_recon.js`（文案声明 ↔ 数据现算 **33/33 ✔**）、`tmp_t23_data.js`（重复键 / 年份区间）、
  `tmp_t23_strip_smoke.js`（18 条断言）、`tmp_t23_keys.js`（`doc_check` 口径的键数）。
  开工与收尾 `gate.js` 三条硬门全绿、`doc_check` 15 项全绿、`readme_audit check` 四模式 0 不一致。
  **计数变动**：exam-history 56 → **83**、`v` 61 → **62**（条目 97 / 纯理论 33 / hot 43 **不变**）。
  ⚠ **hot 口径**：登记后 **10 张纯理论卡达 hot 口径**（`coa-bus` 11 年、`os-classic` 15 年…），
  但侧栏对 theory 卡**只显示「📖 纯理论」、不显示 🔥** ⟹ `hot:true` 仍 43，**不是漏标**。
- **照抄**：① 核考情照本窗四件套——`tmp_t23_batch.js`（按 `年份-题号` 打缓存题块）、
  `tmp_t23_paper.js <年> <题号…>`（按**行首题号**切 `_真题.txt` 原文，专治 `index.json` 缺块/错位）、
  `tmp_t23_find.js <年> <词…>`（全文关键词定位）、`tmp_t23_claims.js` + `tmp_t23_recon.js`
  （**卡片文案 ↔ 数据现算**对账）；② 改完数据必跑 `tmp_t23_data.js` + `tmp_t23_recon.js`
  （**同 id 重复键**、`'选大'` 分段记法都在里面）；③ 浏览器实测照 `tmp_t23_harness.js`
  （**离线 harness 里 Tailwind CDN 不加载 ⟹ 克隆出来的分布条会竖排成"一列 18 格"**，要显式补
  `.exam-strip > .flex { display:flex; flex-wrap:wrap }`；这条已写进脚本注释）。

