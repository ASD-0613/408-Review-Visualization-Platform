# 清单 · 新增模块（12 步，逐条勾）

> **读者**：窗口｜**必读**：否（做模块时对照勾）｜**相关**：`docs/handover.md` §3.9 / §4.2 / §3.5-18
> **纪律（`docs/handover.md` §6.2.14⑩③）**：本文件**只给"命令 + 判据 + 原文节号"**，
> 任何一条规则的原文都**不许**写进来——规则的家在 handover，抄一份就等于造出第二份会漂移的正文（§8.3）。
> 每步的"判据"= 这一步做完的最低要求；不满足就别往下走（闸门编号见 §3.5.0）。

- [ ] **1. 定窗号与批次** —— 判据：窗号 = `§6.6.0` 摘要表**最上一行 + 1**；批次按 §3.7-1 判"几个/窗"。
  · 节号：`§1.7-4`（窗号制度）｜`§3.7`（粒度）
- [ ] **2. 三条硬门全绿** —— 命令：`node .dsh/skills/408-handover/scripts/gate.js`；判据：**exit 0**。
  · 节号：`§0.2` 第 6 步（三条门各自的口径）
- [ ] **3. 核考情（子代理并行）** —— 判据：回报 ≤30 行、每条含 `年份-题号｜题型｜≤25 字｜文件:行号`。
  · 节号：`§3.5-1`（先查缓存）｜`§3.5-12`（八个坑）｜`§3.5-20`（子代理提示词）
- [ ] **4. 抄一个同族模块** —— 判据：只读"契约与关键手法"三处（`inputs` / `parse` / `render` 惯用法），不整读。
  · 节号：`§4.2`（模块契约）｜`§3.5-18`（哪类活抄哪个脚本）
- [ ] **5. 先把数据模型与不变量写进注释** —— 判据：单位、稳定帧、派生量三样都写清；`inputs` 每项都有 `default`。
  · 节号：`§1.3`（快照铁律）｜`§3.8-11`（默认输入）
- [ ] **6. 编译期自检** —— 命令：`node --check <模块文件>`（或直接跑第 2 步的 gate，它的门 1 含这一项）。
  · 节号：`§3.5-9④`｜`§0.3`「SyntaxError / 裸反引号」两行
- [ ] **7. Node 冒烟** —— 命令：`node tmp_<模块>_smoke.js`；判据：**先打中间量并与手算对齐**，再让全场景全绿。
  · 节号：`§3.8-11`（默认输入）｜`§3.8-12`（量纲）｜`§3.8-13`（类型串味 / `[object Object]`）｜`§3.5-22`（顺序）
- [ ] **8. 浏览器实测（提权）** —— 判据：看到 `shot: …` 与 `==== 完成 ====`，且 `traverse=96 bad=0`、`geomBad=0`、CDP 零异常。
  · 节号：`§3.5-10`（harness 五个坑）｜`§6.4`（提权判据）｜`§3.8-3`
- [ ] **9. 截图逐张目视** —— 判据：**原分辨率**看；缩略图下的结论不算（§3.8-3③ 有过反例）。
  · 节号：`§3.8-8`｜`§3.8-3③`
- [ ] **10. 接线五件套一次配齐** —— 判据：模块文件 / `app.js` 转 `ready` / `js/exam-history.js` / `index.html` 的 `?v=N` 递增 / README，五处齐。
  · 节号：`§3.9` P5（`v+1` 必须在 harness 之后）｜`§3.3-17`
- [ ] **11. 改完文档再跑一遍硬门** —— 命令：gate.js + `node tmp_dup_audit.js hot`；判据：全绿，且火苗数与 `hot:true` 实测一致。
  · 节号：`§1.5`（火苗口径）｜`§6.4`
- [ ] **12. 收尾** —— 判据：handover 回写 → `doc_check.py` + `exam_index.py --stats` → `§6.5` 覆盖 + `§6.6.0` 追加一行 → 临时文件按**显式清单**删。
  · 节号：`§3.9` P6｜`§1.7-5`（handover 第一优先级）｜`§3.5-18`（显式清单清理）
