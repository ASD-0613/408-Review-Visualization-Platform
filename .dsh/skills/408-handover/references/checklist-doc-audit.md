# 清单 · 纯文档 / 数据对账（8 步，逐条勾）

> **读者**：窗口｜**必读**：否（做文档轨时对照勾）｜**相关**：`docs/handover.md` §3.9 的"纯文档对账"行 / §3.5-24
> **纪律（`docs/handover.md` §6.2.14⑩③）**：本文件**只给"命令 + 判据 + 原文节号"**，规则原文一律在 handover。
> ⚠ 本轨道**没有 P2/P3**（不写代码、不写模块冒烟），但 **P1 的对账脚本要当回归测试留着**。

- [ ] **1. 定窗号** —— 判据：`§6.6.0` 摘要表最上一行 + 1；不夹带模块（§3.7-5）。
  · 节号：`§1.7-4`｜`§3.7-5`
- [ ] **2. 三条硬门全绿** —— 命令：`node .dsh/skills/408-handover/scripts/gate.js`；判据：exit 0（红的那条是上一窗的遗留）。
  · 节号：`§0.2` 第 6 步
- [ ] **3. 先确认"唯一数据源"** —— 判据：要改的数字都能从 `js/exam-history.js` 或模块文件现算出来。
  · 节号：`§3.5-24`（唯一数据源 + 对账脚本）
- [ ] **4. 现算，不照抄** —— 命令：`node tmp_readme_audit.js check` 先看现状；判据：**每个数字都要有出处**（算不出来就不写）。
  · 节号：`§3.5-24`｜`§0.5`（凭印象写数字）
- [ ] **5. 改文本只用 Edit / write** —— 判据：不用 PowerShell 改中文文件；批量改走 `node` / python 显式 utf-8。
  · 节号：`§3.5-9⑤`｜`§3.5.0` 闸门 6
- [ ] **6. 对账 + 硬门复跑** —— 判据：四模式 0 不一致、`doc_check.py` 全绿（改过 handover 就必须复跑）。
  · 节号：`§3.5-24`｜`§8.9` 第 4 步
- [ ] **7. 全站遍历零报错** —— 命令：`node tmp_t6_harness.js --run`（**提权**）；判据：`traverse=96 bad=0` + CDP 零异常 + 看到 `==== 完成 ====`。
  · 节号：`§6.4`（提权判据）｜`§3.9` P4 的"只跑那一栏"
- [ ] **8. 收尾** —— 判据：`§6.2.N` 台账 + `§6.5` 覆盖 + `§6.6.0` 追加一行；新增的坑同步进 `§0.3`；临时文件按显式清单删。
  · 节号：`§3.9` P6｜`§8.4`（新坑必须进 §0.3）｜`§3.5-18`

## 附：改 handover 本身时额外两条

- [ ] **A. 排版法** —— 判据：`§8.2` 三行节头、`§8.3` 条目格式、`§8.8` 度量衡；`doc_check` 第 12/13/14 项管这个。
  · 节号：`§8`｜`§8.9`（五步动作清单）
- [ ] **B. 改了 §3.5.0 闸门表** —— 判据：**必须同步** `.dsh/skills/408-handover/SKILL.md` 里那张摘要表；不同步会被 `doc_check` 第 15 项 (b) 判红。
  · 节号：`§3.5.0` 末尾那条注｜`§6.2.14⑩②`
