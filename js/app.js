'use strict';
/* ============================================================================
 * app.js —— 四本书的目录树 + 主导航 + 启动
 * ----------------------------------------------------------------------------
 * 章节结构对照《25 考研 408 大纲》考查范围组织（见 考纲/25考研408大纲.pdf），
 * 各条目考频依据 2009-2026 共 18 年真题统计（详见 README「真题考情分析」）。
 *
 * 后续扩充新知识点只需两步：
 *   ① 在 js/modules/ 下新建模块文件（参照现有模块契约）并在 index.html 引入；
 *   ② 在下方对应章节添加 { id, label, status:'ready' } 条目。
 * status:'wip' 自动显示占位页；hot:true → 🔥 高频考点（真题统计支撑）；
 * plan 字段描述规划中的交互形式（显示在占位页）。
 * ========================================================================== */

RC408.books = [
  {
    id: 'ds', name: '数据结构', icon: '🧩', accent: '#6366f1',
    chapters: [
      {
        name: '第1章 基本概念',
        topics: [
          { id: 'ds-complexity', hot: true, label: '算法时间复杂度分析', status: 'wip', plan: '代码逐行高亮 + 执行次数 f(n) 实时累计、常见量级 O(·) 的判定过程（每年第1题）' },
        ],
      },
      {
        name: '第2章 线性表',
        topics: [
          { id: 'ds-linkedlist', hot: true, label: '单链表的插入与删除', status: 'wip', plan: '指针 p/q 逐步移动、断链接链高亮、头插/尾插对比（链表是算法大题常客）' },
          { id: 'ds-seqlist', label: '顺序表 vs 链表', status: 'theory', note: `
> **为什么要有它**：线性表既可以用一段连续单元也可以用一串结点表示，选错了会让插入删除或随机访问付出成倍代价——**顺序表 vs 链表**就是按操作特征挑存储结构的判据。
> **怎么实现**：顺序表用连续空间加表长，靠下标直接定位到元素；链表用指针把结点串起来，靠顺着指针走才能定位。
> **记住什么**：**按位序存取只有顺序表是 O(1)**；插入删除的代价看下表，"哪种结构更省"要先看操作在表头还是表尾、是否已经定位。

## 关键机制
| 比较项 | 顺序表 | 链表 |
| --- | --- | --- |
| 存取 | 随机存取 \\(O(1)\\) | 顺序存取 \\(O(n)\\) |
| 插入/删除 | 移动元素 \\(O(n)\\) | 改指针 \\(O(1)\\)（已定位时） |
| 空间 | 连续、需预估；存储密度高 | 按需申请；每结点多指针开销 |
| 适用 | 查多改少、长度稳定 | 频繁插删、长度未知 |

- **顺序表插入删除的移动量**（表长 n）：表头插入移动 n 个元素、表头删除移动 n−1 个元素，**一定移动多个元素**；表尾插入与表尾删除都不移动其他元素，代价 \\(O(1)\\)（2026-1）；
- **链表归并的代价**：两个升序链表合并，每比较一次就确定一个结点的链接位置，最坏情况每个元素都参与比较，时间 \\(O(m+n)\\)（2013-1）。

## 考点提醒（易错点）
- "链表插入删除快"只在**已经定位到插入位置**时成立；定位本身仍要 \\(O(n)\\)（顺序表定位 \\(O(1)\\) 但移动 \\(O(n)\\)，两者是一对镜像代价）；
- "表头插入 \\(O(n)\\)"与"表尾插入 \\(O(1)\\)"是同一张表上的两个结论，别互相套用；
- 顺序表可能因预估过大而浪费空间、过小而溢出（需扩容搬移）；链表每结点多出指针域，存储密度小于 1。

> **真题考情**：> **2/18 年（选 2）**：2013-1（升序链表合并的代价）、2026-1（表头/表尾插删的移动量）。` },
        ],
      },
      {
        name: '第3章 栈、队列和数组',
        topics: [
          { id: 'ds-stack-queue', hot: true, label: '栈与队列出入演示（序列判定）', status: 'ready' },
          { id: 'ds-expression', label: '中缀 → 后缀表达式求值', status: 'ready' },
          { id: 'ds-matrix', label: '特殊矩阵压缩存储', status: 'theory', note: `
> **为什么要有它**：特殊矩阵里大量元素是重复值或 0，直接开二维数组会把成倍的存储浪费在已知内容上——**压缩存储**就是把它们映射进一维数组。
> **怎么实现**：先规定只存哪部分、按什么次序存（行优先/列优先），再数出目标元素**之前**有多少个元素，加上数组起始下标即得压缩下标。
> **记住什么**：两条主线是"对称矩阵存下三角"与"三对角矩阵存三条对角线"；**同一个元素的下标随起始下标是 0 还是 1 整体差 1**。

## 关键机制
| 矩阵 | 存法 | 下标公式（数组为 0 基） |
| --- | --- | --- |
| n 阶对称矩阵 | 只存下三角，按行优先 | \\(i\ge j\\) 时 \\(k=i(i-1)/2+j-1\\)；上三角元素先按 \\(a[i][j]=a[j][i]\\) 换成下三角再代入 |
| 三对角矩阵 | 只存 3 条对角线，按行优先 | 矩阵下标 1 基时 \\(k=2i+j-3\\)；第 1 行与第 n 行各 2 个元素，其余每行 3 个 |

- **通用模板**：数清目标元素**之前**的元素个数（按行求和）再加数组起始下标——三对角矩阵的 \\(m_{30,30}\\) 前面是"首行 2 个 + 中间 28 行每行 3 个 + 本行 1 个"，故下标为 87（2016-4）；
- **上三角按行优先**时每行长度递减：12 阶矩阵的上三角第 1 行 12 个、第 2 行 11 个、第 3 行 10 个……元素 \\(m_{6,6}\\) 是第 12+11+10+9+8+1 = 51 个，0 基下标为 50（2018-3）；
- **多维数组按行优先**存放时，后面各维的尺寸会以乘积的形式出现在地址计算里（2025 二维数组地址计算）。

## 考点提醒（易错点）
- **先确认 0 基还是 1 基**：矩阵下标从 1 起、数组下标从 0 起时，公式要整体调一次，这是最常错的 1 分；
- 对称矩阵公式只对**下三角（\\(i\ge j\\)）**成立，遇到上三角元素必须先按对称性换位，别把 i、j 直接代入；
- 三对角矩阵"第 1 行和第 n 行只有 2 个非零"最容易漏，逐行求和时不要按"每行 3 个"算满。

> **真题考情**：> **5/18 年（选 4 + 大 1）**：大 2011-41（上三角按行优先压缩）、选 2016-4（三对角求下标）、> 选 2018-3（上三角行优先）、2020-1（对称矩阵按列优先）、2021-3（二维数组行优先地址）。` },
        ],
      },
      {
        name: '第4章 树与二叉树',
        topics: [
          { id: 'ds-bintree-traversal', hot: true, label: '二叉树的遍历（前/中/后/层序）', status: 'ready' },
          { id: 'ds-huffman', hot: true, label: '哈夫曼树与哈夫曼编码', status: 'ready' },
          { id: 'ds-bst-avl', hot: true, label: '二叉排序树与 AVL 旋转', status: 'ready' },
          { id: 'ds-clue', label: '线索二叉树', status: 'theory', note: `
> **为什么要有它**：二叉链表里大量空指针被浪费，而且不借栈或不递归就找不到结点的前驱后继——**线索二叉树**用这些空链域把遍历序列直接串起来。
> **怎么实现**：选定一种遍历次序，把空的左指针改指该次序下的**前驱**、空的右指针改指**后继**，并用标志位区分"指向孩子"还是"是线索"。
> **记住什么**：n 个结点的二叉链表恰有 n+1 个空链域；**线索指向的是"该遍历序列"里的前驱/后继**，判定时先写出序列再连线，不要凭树形猜。

## 关键机制
| 项 | 结论 |
| --- | --- |
| 空链域数 | n 个结点有 n+1 个空指针，正好容纳 n+1 条线索 |
| 标志位 | ltag/rtag 为 0 表示指向孩子、为 1 表示是线索 |
| 中序线索求后继 | 有右子树 → 右子树的**最左**结点；无右子树 → 右指针已是线索，直接指向后继 |
| 中序线索求前驱 | 有左子树 → 左子树的**最右**结点；无左子树 → 左指针已是线索，直接指向前驱 |
| 后序线索 | 找后继仍**需要父指针**；例外是叶子结点，其后序后继就是父结点 |

- **优点**：遍历不用栈、找前驱后继快；**缺点**：插入删除时线索的维护更繁琐；
- **判定流程**：按"左-根-右"写出中序序列 → 找目标结点在序列中的位置 → 只有序列中相邻的结点才互为前驱/后继（2014-4）。

## 考点提醒（易错点）
- 叶子结点的左右线索分别指向**该遍历序列**的前驱与后继，不是树形上的父结点或孩子；
- 后序线索二叉树中叶子结点若是右孩子，则其后序后继就是父结点、右线索指向父结点（2013-5）；但**非叶子**结点求后序后继仍需父指针（或三叉链表）；
- 中序序列里"两个结点相邻"不能只看树形：当 q 是 p 的右兄弟时，中序一定先 p、再父结点、最后 q，两者**不可能相邻**；举反例即可判（2022-3）；
- 中序片段为 p, v, q 时，p 在 v 的左子树、q 在 v 的右子树：左子树中最后访问的 p **没有右孩子**，右子树中最先访问的 q **没有左孩子**（2024-3）。

> **真题考情**：> **5/18 年（选 5）**：2010-3（后序线索化的前驱/后继）、2013-5、2014-4（线索指向该遍历序列的前驱/后继）、
> 2022-3、2024-3（由中序片段判有无孩子）。` },
          { id: 'ds-heap', hot: true, label: '堆与优先队列（建堆 / 插入 / 删除堆顶）', status: 'ready' },
          { id: 'ds-unionfind', label: '并查集', status: 'theory', note: `
> **为什么要有它**：Kruskal 判环、等价类划分、连通分量计数都要反复回答"这两个元素是不是同一组"——**并查集**就是只做合并与查询、单次代价极低的集合结构。
> **怎么实现**：每个集合用一棵树表示，**双亲表示法**存 parent 数组、根指向自己；查就顺着 parent 爬到根，合并就把一棵树的根挂到另一棵的根上。
> **记住什么**：同集合等价于根相同；Find 用于判环、Union 用于合并；**按秩合并 + 路径压缩**后单次操作近常数。

## 关键机制
| 优化 | 做法 | 效果 |
| --- | --- | --- |
| 按秩（或按大小）合并 | 把矮树的根挂到高树的根下 | 树高控制在 \\(O(\log n)\\) |
| 路径压缩 | Find 途中把沿途结点直接挂到根上 | 单次查询近 \\(O(1)\\)，与按秩合并同用则近乎线性 |

- **Find(x)**：沿 parent 一路上溯直到 parent[x] = x，该结点就是根；**Union(a,b)**：先各求一次根，根不同才合并，根相同说明已在同一集合（Kruskal 里就是"成环"，该边丢弃）；
- **判同集合**：两次 Find 的根相同；**数连通分量**：统计 parent[i] = i 的结点个数；
- **应用**：Kruskal 判环、等价类划分、连通分量计数、动态连通性。

## 考点提醒（易错点）
- 合并的是**根**，不是把 b 直接挂到 a 下面；不按秩、固定方向连挂会让树退化成链，Find 变 \\(O(n)\\)；
- 路径压缩改的是**沿途所有结点**的 parent，压缩后树高变小、但原来的层次信息不再保留；
- "根"既是集合的代表元也是树的根：判断两个元素同集合必须比较各自的根，绝不能只比较 parent 数组里的值。

> **真题考情**：> **0/18 年（真题暂无）**：2025 考纲新增"并查集及其应用"，真题尚未单独出题；
> 本平台 Kruskal 模块已演示合并动画。` },
        ],
      },
      {
        name: '第5章 图',
        topics: [
          { id: 'ds-graph-storage', label: '图的存储（邻接矩阵/表）', status: 'wip', plan: '两种存储互转、边表挂接、度数计算' },
          { id: 'ds-dfs-bfs', label: '图的遍历 DFS / BFS', status: 'ready' },
          { id: 'ds-kruskal', hot: true, label: 'Kruskal 最小生成树', status: 'ready' },
          { id: 'ds-prim', hot: true, label: 'Prim 最小生成树（对比 Kruskal）', status: 'ready' },
          { id: 'ds-dijkstra', label: 'Dijkstra 最短路径', status: 'ready' },
          { id: 'ds-topo', hot: true, label: '拓扑排序与关键路径', status: 'ready' },
        ],
      },
      {
        name: '第6章 查找',
        topics: [
          { id: 'ds-binsearch', label: '二分查找（折半查找）与判定树', status: 'ready' },
          { id: 'ds-hash', hot: true, label: '散列表与冲突处理（ASL）', status: 'ready' },
          { id: 'ds-blocksearch', label: '分块查找（索引顺序查找）', status: 'wip', plan: '块间有序+块内查找两级动画、每块最优元素数 √n（2025 真题）' },
          { id: 'ds-rbtree', label: '红黑树基本概念', status: 'theory', note: `
> **为什么要有它**：AVL 要求左右子树高度差不超过 1，插入删除时旋转过于频繁——**红黑树**把平衡条件放宽成"最长路径不超过最短路径的 2 倍"，用更少的旋转换取整体插删效率。
> **怎么实现**：在普通二叉排序树上给每个结点加红/黑颜色，插入删除后用**旋转 + 重新着色**恢复五条性质，从而把树高锁在 \\(O(\log n)\\)。
> **记住什么**：五条性质（根黑、叶黑、无连续红、黑高相同）+ 与 AVL 的分工：查找 AVL 略优，插入删除红黑树更省。

## 关键机制
| # | 性质 |
| --- | --- |
| 1 | 每个结点或是红色、或是黑色 |
| 2 | 根结点是黑色 |
| 3 | 叶结点（NULL，即空指针）视为黑色 |
| 4 | 红结点的两个孩子必为黑色（不出现两个连续的红结点） |
| 5 | 从任一结点到其所有叶结点的路径上黑结点数目（黑高）相同 |

- **高度上界**：n 个内部结点的红黑树高度 \\(h\le 2\log_2(n+1)\\)，故查找、插入、删除都是 \\(O(\log n)\\)；
- **与 AVL 对比**：AVL 严格平衡（左右子树高度差不超过 1），查找略快；红黑树是**近似平衡**，插删所需旋转更少，综合性能更好——C++ 的 map、Linux 的 CFS 调度器都用红黑树；
- **性质 4 与性质 5 配合**才推出高度受限：一条路径上红结点不能连续，故最长路径至多是最短路径的两倍；
- **插入**：新结点先染红（这样只会破坏性质 4），再按叔结点的颜色决定是"重新着色上溯"还是"旋转 + 着色"。

## 考点提醒（易错点）
- 性质 3 的"叶结点"指**空指针 NULL**，不是普通的叶子结点——红黑树真实的叶子（无孩子的结点）可以是红色，只有空链域才算黑叶；
- 由"根黑 + 无连续红 + 黑高相同"可推出**最长路径不超过最短路径的 2 倍**，但红黑树**不是**严格平衡树，查找不一定比 AVL 快；
- 黑高相同是对**每个结点**到其所有叶结点而言的，不是整棵树所有叶结点深度相同；
- 构造与插入调整的完整过程真题暂未考，备考以五条性质与 AVL 的辨析为主。

> **真题考情**：> **0/18 年（真题暂无）**：2024/2025 考纲新增"红黑树"，真题暂未考构造，备考以性质辨析为主。` },
          { id: 'ds-btree', hot: true, label: 'B 树与 B+ 树', status: 'ready' },
          { id: 'ds-kmp', label: 'KMP 与 next/nextval 数组', status: 'ready' },
        ],
      },
      {
        name: '第7章 排序',
        topics: [
          { id: 'ds-sort', hot: true, label: '排序算法全家桶（含每趟结果）', status: 'ready' },
          { id: 'ds-extsort', hot: true, label: '外部排序（初始归并段 / 败者树 / 最佳归并树）', status: 'ready' },
          { id: 'ds-radix', label: '基数排序', status: 'wip', plan: '按位分配/收集的队列动画、稳定性验证' },
        ],
      },
    ],
  },
  {
    id: 'coa', name: '计算机组成原理', icon: '⚙️', accent: '#0ea5e9',
    chapters: [
      {
        name: '第1章 计算机系统概述',
        topics: [
          { id: 'coa-hierarchy', label: '层次结构与冯·诺依曼机', status: 'theory', note: `
> **为什么要有它**：裸硬件只认机器码，人不可能直接指挥门电路——层次结构把"人写的语言"逐级翻译成"机器能执行的信号"，没有它就没有可编程的通用计算机。
> **怎么实现**：靠**存储程序**——指令和数据一起以二进制存进同一存储器，CPU 按 PC 给出的地址自动逐条取指、译码、执行；层与层之间用编译器/汇编器一次翻译，或用解释方式逐条解释。
> **记住什么**：冯·诺依曼五大部分 + "存储程序"的两条推论（指令与数据**同存一器**、只靠**取指/执行阶段**区分）+ 五层结构的名称与顺序 + 软硬件**逻辑等价**。

## 关键机制
| 层 | 名称 | 说明 |
| --- | --- | --- |
| M4 | 高级语言级 | C/C++ 等，靠**编译**一次翻译到下层 |
| M3 | 汇编语言级 | 靠**汇编程序**翻译成机器语言 |
| M2 | 操作系统级 | 管理资源、提供系统调用 |
| M1 | 传统机器语言级 | 机器指令，由**微程序解释**执行 |
| M0 | 微程序机器级 | 微指令直接控制硬件 |

- **五大部分**：运算器、控制器、存储器、输入设备、输出设备（运算器 + 控制器合称 CPU）。
- **存储程序**：程序（指令序列）与数据**都**以二进制存于**同一**存储器，机器按地址自动逐条取指执行——这是通用计算机的前提。
- **指令与数据怎么分**：两者形式相同、都按地址访问，唯一依据是**指令周期的阶段**——取指阶段取出的是指令，执行阶段取出的是数据。
- **中心的演化**：早期以**运算器**为中心（I/O 与主存之间的数据要经运算器中转），现代机器已演化为以**存储器**为中心。
- **软硬件逻辑等价**：同一功能既可用硬件实现也可用软件实现（如乘法可用硬件乘法器，也可用软件移位相加），分界随器件速度与价格浮动。
- **翻译与解释**：编译/汇编把高级语言**一次**全部变成机器语言（生成目标程序，执行快）；解释是**逐条**边翻译边执行（不生成目标程序）。

## 考点提醒（易错点）
1. "区分指令与数据的依据"不是"存在不同存储器里"或"格式不同"，而是**取指阶段 / 执行阶段不同**（2009-11）。
2. 层次结构是**功能（虚拟机）分层**，不是硬件的物理分层；下层为上层提供支持，靠翻译或解释衔接。
3. "软硬件逻辑等价"只指**逻辑功能**等价，不等于性能与成本可以互换。
4. "以存储器为中心"是**现代**机器；早期冯·诺依曼机是**以运算器为中心**，别记反。
5. 五层顺序别颠倒：微程序机器级（最低）→ 传统机器语言级 → 操作系统级 → 汇编语言级 → 高级语言级（最高）。

> **真题考情**：> **4/18 年（选 4）**：2009-11（指令与数据都存于存储器，靠指令周期阶段区分）、
> 2019-12（冯·诺依曼基本思想）、2025-16（ISA 规定什么）、2026-12（系统层次与 ISA）。` },
          { id: 'coa-perf', label: '性能指标（CPI/MIPS/FLOPS）', status: 'ready' },
        ],
      },
      {
        name: '第2章 数据的表示和运算',
        topics: [
          { id: 'coa-fixadd', hot: true, label: '补码加减运算与溢出判断', status: 'ready' },
          { id: 'coa-baseconv', hot: true, label: '进位计数制与转换（R 进制互转 · 除基取余 / 乘基取整）', status: 'ready' },
          { id: 'coa-float754', hot: true, label: 'IEEE 754 浮点数表示', status: 'ready' },
          { id: 'coa-floatadd', label: '浮点数的加/减运算（对阶 · 规格化 · 舍入）', status: 'ready' },
          { id: 'coa-muldiv', hot: true, label: '乘除运算电路（原码一位乘 / 加减交替除法）', status: 'ready' },
          { id: 'coa-hamming', label: '海明码与校验', status: 'wip', legacy: '现行考纲"数据的表示和运算"章已不含校验码（CRC 校验仍属于计算机网络链路层的差错控制）；2013 年真题曾考"纠 1 位错最少校验位数"。' },
        ],
      },
      {
        name: '第3章 存储器层次结构',
        topics: [
          { id: 'coa-cache-direct', hot: true, label: 'Cache 直接映射', status: 'ready' },
          { id: 'coa-cache-assoc', hot: true, label: '组相联与全相联映射', status: 'ready' },
          { id: 'coa-mainmem', label: '主存与 CPU 连接（芯片扩展）', status: 'ready', hot: true },
          { id: 'coa-sram-dram', label: 'SRAM / DRAM / Flash 特点对比', status: 'theory', note: `
> **为什么要有它**：没有一种器件能同时做到又快又大又便宜——快而贵的 SRAM 只能做 Cache，慢而便宜的 DRAM 做主存，掉电不丢的 Flash 做 SSD/U 盘，三者组合才凑出"接近 Cache 速度、接近磁盘容量"的存储层次。
> **怎么实现**：SRAM 用**触发器**存一位（不必刷新），DRAM 用**电容电荷**存一位（必须定时刷新、地址行列复用引脚），Flash 用**浮栅晶体管**存电荷（非易失，写入前必须先擦除）。
> **记住什么**：三者的**速度 / 刷新 / 易失 / 用途**对照表 + DRAM 的**行列地址复用**与"**刷新对 CPU 透明**" + "随机存取方式"不等于"随机存储器"。

## 关键机制
| 特性 | SRAM | DRAM | Flash |
| --- | --- | --- | --- |
| 存储原理 | 触发器 | 电容电荷 | 浮栅晶体管 |
| 速度 | 快 | 较慢 | 读快、**写慢**（写前先擦除） |
| 刷新 | 不需要 | **需定时刷新** | 不需要 |
| 易失性 | 易失 | 易失 | **非易失** |
| 集成度 / 价格 | 低 / 贵 | 高 / 便宜 | 高 / 便宜 |
| 典型用途 | Cache | 主存 | SSD、U 盘 |

- **DRAM 行列地址复用**：地址分两次（行、列）送同一组引脚，**地址引脚数减半**，代价是存取时间变长——这就是 DRAM 引脚少的原因。
- **刷新**：以**行**为单位按行地址逐个进行，占用存储器带宽但**对 CPU 透明**（由存储器控制器自动完成，CPU 不参与）。
- **破坏性读出**：DRAM 读电容后电荷被破坏，读出后必须**再生（重写）**。
- **RAM 与 ROM**：RAM（含 DRAM、SRAM）**断电即失**，ROM 断电不丢；两者**都采用随机存取方式**，但"随机存取"不等于"随机存储器"。Cache 只能用高速的 **SRAM**，ROM 只可读、不能作 Cache。
- **CD-ROM 属串行存取**：光盘按道顺序寻找，存取时间与物理位置有关，因此不是随机存取；且 CD-ROM 是只读光盘，不等于 ROM。

## 考点提醒（易错点）
1. "随机存取"说的是**存取方式**（存取时间与存储单元物理位置无关），不要与"随机存储器 RAM"混为一谈；CD-ROM 是**串行（顺序）存取**。
2. 刷新**对 CPU 透明**，别写成"CPU 定期刷新"；刷新以**行**为单位，不是逐个单元。
3. "ROM 断电不丢"不等于"ROM 不能改"：Flash 是 EEPROM 的进一步发展，可读可写，但**写入前必须先擦除**，所以写比读慢得多。
4. SRAM 与 DRAM 都易失，DRAM 只多一条"需定时刷新"；别记成 SRAM 不需要供电。
5. 引脚少的是 DRAM（行列地址复用），速度快的是 SRAM；ROM 不需要刷新，DRAM 需要。
6. 用"采用随机存取方式"去反推"是 RAM"是常见错项——ROM 也采用随机存取方式。

> **真题考情**：> **4/18 年（选 4）**：2010-16（RAM 易失、都采随机存取、ROM 不能作 Cache）、2011-14（CD-ROM 属串行存取）、
> 2012-16（Flash 写前擦除、非易失）、2020-15（Cache 用 SRAM、DRAM 需不断刷新）。` },
          { id: 'coa-disk', label: '磁盘存储器性能计算器', status: 'ready', hot: true },
          { id: 'coa-optical', label: '光盘存储器 / CD-ROM', status: 'wip', legacy: '现行考纲外部存储器仅含磁盘与 SSD，光盘相关内容已删；2011 年真题（不采用随机存取方式的存储器）、2013 年真题（CD-ROM 视频随机播放的文件物理结构）曾考。' },
          { id: 'coa-vm', label: '页式虚拟存储器', status: 'theory', note: `
> **为什么要有它**：程序的逻辑地址空间比实际主存大得多，多个进程还要互不干扰地"独占"内存——虚拟存储器把主存当 Cache 用，靠页表 + 缺页异常在磁盘与主存间自动搬页，让每个进程看到连续、独立、可大于主存的地址空间。
> **怎么实现**：CPU 发出虚拟地址 → MMU 用**虚页号查 TLB（或查页表）**得到页框号 → 与**页内偏移**拼接成物理地址；页不在主存时触发**缺页异常**，由 OS 从磁盘调入。
> **记住什么**：地址划分（虚页号 ‖ 页内偏移）+ **TLB 表项 = 有效位 + Tag + 页框号** + **MMU 只检测 TLB 缺失、缺页、越权（不含 Cache 缺失）** + 大题里推位数的模板。

## 关键机制
- **地址划分**：虚拟地址 = 虚页号 ‖ 页内偏移，**页内偏移位数 = log₂(页大小)**；物理地址 = 页框号 ‖ 页内偏移。虚地址位数由虚拟地址空间大小定，物理地址位数由主存容量定，两者不能互抄。
- **转换三步**：① 用虚页号查 **TLB**（在 CPU 内、由 SRAM 组成，速度快）；② TLB 未命中则查**页表**（在主存）并回填 TLB；③ 取出页框号，与页内偏移拼成物理地址再访问 Cache / 主存。
- **TLB 表项**：有效位 + **Tag（虚页号的一部分）** + 页框号（另有脏位、权限位等）。组相联 TLB 与组相联 Cache 同法划分：**组数 = 表项数 ÷ 路数**，组号位数 = log₂(组数)，**Tag 位数 = 虚页号位数 − 组号位数**。
- **MMU 负责的三件事**：TLB 缺失（硬件自动查页表）、页面缺失（缺页异常，交 OS 处理）、越权访问（保护异常）；**Cache 缺失发生在访问物理内存之后，不由 MMU 检测**。
- **页表项常见字段**：有效位（页是否在主存）、修改位（脏位）、访问位、访问权限、物理页号。
- **缺页处理流程**：缺页异常 → OS 查外存地址 → 调页入主存 → 更新页表与 TLB → 重新执行被中断的指令（OS 视角的完整流程见 OS 书的页式虚拟内存·地址转换模块）。

## 考点提醒（易错点）
1. TLB 是**硬件**（在 CPU 内、SRAM、容量小、全相联或组相联），页表在主存由 OS 维护；TLB 命中不等于该页一定在 Cache 中。
2. TLB 表项里存的是**页框号**而不是完整物理地址；Tag 是**虚页号的高位**，不是物理地址的 Tag。
3. **Cache 缺失不由 MMU 检测**——别把它算进 MMU 的检查项（2024-18）。
4. 推位数先定**页内偏移**（由页大小决定），再定页号位数；只用高位部分做 Tag 或组号，漏掉有效位、算错组数是常见失分点（2024-17）。
5. TLB 未命中时页**不一定**不在主存（可能只是表项被换出）；**缺页**才说明页不在主存——两者不能混。
6. 虚页号 = 虚拟地址位数 − 页内偏移位数，页框号 = 物理地址位数 − 页内偏移位数，别用虚页号去查页框号。

> **真题考情**：> **7/18 年（选 5 + 大 4）**：大 2011-44、2016-45、2018-44、2023-43（地址划分与字段位数）；
> 选 2013-16（页号拼物理地址）、2024-17、2024-18（MMU 检测范围）、2026-19、2026-24。` },
        ],
      },
      {
        name: '第4章 指令系统',
        topics: [
          { id: 'coa-addressing', hot: true, label: '指令寻址方式（EA 的形成）', status: 'ready' },
          { id: 'coa-inst-format', label: '指令格式与扩展操作码', status: 'theory', note: `
> **为什么要有它**：机器只认二进制，必须把"做什么 + 对谁做"编码成固定格式；而指令条数少了不够用、多了操作码太长，于是有了**变长操作码**——用短操作码挤进更多指令，靠牺牲译码复杂度换编码空间。
> **怎么实现**：指令 = **操作码 + 地址码**；扩展操作码从短到长逐级扩展，每扩展一级就把一个地址字段让给操作码，并把上一级**用掉的编码从前缀池里扣除**。
> **记住什么**：操作码位数 = ⌈log₂指令条数⌉（定长）+ 扩展操作码的**前缀扣除法** + 指令字长要取**字节的整数倍** + ISA 规定指令格式而不是微架构。

## 关键机制
- **指令格式的组成**：操作码 ‖ 地址码（按地址码个数分三地址 / 二地址 / 一地址 / 零地址指令）；**指令字长 = 操作码位数 + 各地址字段位数之和**，指令字长 ≠ 机器字长 ≠ 存储字长。
- **地址码位数决定寻址范围**：直接寻址范围 = 2 的"地址码位数"次方（按字节编址时即字节数）；定长操作码位数 = ⌈log₂指令条数⌉。
- **扩展操作码的做法**：操作码从短到长逐级扩展，**短操作码不能与长操作码前缀冲突**（用掉的编码必须从"前缀池"扣除，否则译码歧义）。设最短一级操作码 k 位、地址字段 A 位、各级条数依次为 n₃、n₂、n₁…：
  1. 三地址级最多 2^k 条，用掉 n₃ 条后**剩 2^k − n₃ 个前缀**；
  2. 二地址级操作码 k + A 位，最多 (2^k − n₃) × 2^A 条，用掉 n₂ 条后剩余再作下一级前缀；
  3. 一地址、零地址逐级再乘 2^A，依此类推。
- **真题算式一**（三地址 29 条 + 二地址 107 条）：29 条要求操作码至少 5 位，剩 32 − 29 = 3 个前缀 → 二地址最多 3 × 2^6 = 192 条 ≥ 107 条 ✓ → 指令字长 = 5 + 3 × 6 = 23 位 → 按字节编址需取 8 的倍数 → **24 位**（2017-16）。
- **真题算式二**（指令字长 16 位、地址码 6 位、二地址 12 条、一地址 254 条）：二地址操作码 4 位，12 条用掉 12 个前缀剩 4 个 → 一地址操作码 10 位，最多 4 × 2^6 = 256 条，给 254 条后剩 2 个前缀 → 零地址操作码 16 位，最多 2 × 2^6 = **128 条**（2022-19）。
- **ISA 与微架构的分界**：ISA（指令集体系结构）规定指令格式、操作码、寻址方式、通用寄存器等**程序员可见**的内容；数据通路、微程序控制器、阵列乘法器属于微架构实现细节，不由 ISA 规定（2025-16）。

## 考点提醒（易错点）
1. 扩展操作码必须**从短到长逐级扣除**已用编码；忘记扣除、或让短操作码成为长操作码的前缀，都会造成译码歧义。
2. 算出的指令字长要取**字节的整数倍**：23 位必须进到 **24 位**（2017-16 的坑），这是把位数答案和选项区分开的关键一步。
3. 操作码位数按**指令条数**取 ⌈log₂N⌉，不要用地址字段个数或寄存器个数代替。
4. **直接寻址范围 = 2 的地址码位数次方**，而寄存器间接寻址范围由机器字长决定；两者别套同一个公式。
5. "零地址指令"不等于"无操作数指令"：操作数可能在栈顶或隐含寄存器中。
6. 问"哪种内容由 ISA 规定"时，微程序控制器、单总线数据通路、阵列乘法器都属于**实现**，不是 ISA 规定的内容（2025-16）。

> **真题考情**：> **6/18 年（选 4 + 大 2）**：选 2014-17、2017-16（扩展操作码与指令字长）、2022-19（编码扣除）、
> 2026-16（ISA 规定什么）；大 2010-43（操作码位数）、2024-43（字段位数）。` },
          { id: 'coa-endian', label: '数据的对齐与大小端存放', status: 'theory', note: `
> **为什么要有它**：一个字占多个字节而主存按字节编址，"多字节数据在内存里怎么摆"必须有统一约定；约定不同，同一串字节的含义就不同，读汇编、看内存快照、推结构体地址都会错。
> **怎么实现**：**大端**把高位字节放低地址（网络字节序），**小端**把低位字节放低地址（x86 采用）；**按边界对齐**让每个成员的起始地址是自身大小（或对齐模数）的整数倍，不足就填充。
> **记住什么**：大小端定义 + x86 是小端；结构体大小 = 成员按对齐规则排布后的总长（取最大对齐值的整数倍）；解题就三步：**画地址格 → 按声明顺序放 → 不对齐补空洞**。

## 关键机制
- **定义（只记这一句）**：一个多字节数据中，**高位字节存低地址 = 大端**；**低位字节存低地址 = 小端**；x86 用**小端**。
- **示例**：0x12345678 从地址 0x100 起存放——小端依次为 0x100 存 78H、0x101 存 56H、0x102 存 34H、0x103 存 12H；大端恰好相反（0x100 存 12H）。
- **边界对齐**：成员偏移必须是该成员大小（题目另给"对齐模数"时取较小者）的整数倍，不足则**填充**；结构体总大小 = **最大成员对齐值**的整数倍，末尾也要补齐。
- **解题模板**（给起始地址求某字节地址）：① 画地址格并标出每个成员的起止；② 按声明顺序安放，遇不对齐就空出填充字节；③ 数组元素按"元素大小 × 下标"整体平移（元素大小含填充）；④ 在小端下把目标值的字节序列按**低字节在前**铺进该元素的地址格，再数到目标字节的地址。
- **元素大小的算法**：如 record 含 int（4B）+ char[10] + int（4B）：char[10] 占 10B，为让后面的 int 按 4 字节对齐要补 2B 到 12B，故元素大小 = 4 + 12 + 4 = **20B**（不是 18B），数组第 2 个元素的起始地址 = 首地址 + 20B。

## 考点提醒（易错点）
1. 大小端只影响**字节顺序**，不影响位序；别把它和"字内位编号方向"混为一谈。
2. 小端机里 0x12345678 的**低**字节 78H 在**低**地址——方向记反是本考点最常见的错（2016-14）。
3. 对齐会产生**空洞**：成员声明顺序不同，同一个结构体的大小可以不同；末尾也要补到最大对齐值的整数倍。
4. 数组元素大小是**含填充后的元素大小**，不是成员大小直接相加（2025-15 的 record 是 20B 而非 18B）。
5. 题目问"某地址存放的是哪个字节"时，先定位**所属成员的起始地址**，再按小端顺序数偏移，别从结构体首地址一路数。
6. 只给"未知"字样的格子是填充字节，不参与数据，但它**占地址**，数偏移时必须算上（2020-14）。

> **真题考情**：> **6/18 年（选 6）**：2012-15（结构体边界对齐 + 小端）、2016-14（大小端定义与字节存放）、
> 2018-15（小端字节序）、2019-15（大端 + 基址寻址求 LSB 地址）、2020-14、2025-15。` },
          { id: 'coa-assembly', hot: true, label: '高级语言 ↔ 机器级代码', status: 'wip', plan: '循环/分支/函数调用的汇编对应、机器码逐句分析（近年大题热点）' },
          { id: 'coa-cisc-risc', label: 'CISC 与 RISC 对比', status: 'theory', note: `
> **为什么要有它**：CISC 想让"一条指令干完一件事"，指令越来越复杂、译码与流水线越难做；RISC 反过来只留常用简单指令、把复杂度推给编译器，才让**定长译码 + 流水线**成为可能——这是两种设计哲学的分水岭。
> **怎么实现**：CISC 多用**微程序控制**、指令变长、多数指令可访存；RISC 多用**硬布线（组合逻辑）控制**、指令**定长**、只有 **Load/Store 访存**、通用寄存器多、用寄存器传参。
> **记住什么**：4 个高频判据——**定长指令、仅 Load/Store 访存、硬布线控制、适合流水线**；x86 是 CISC，MIPS 与经典 ARM 是 RISC。

## 关键机制
| 比较项 | CISC | RISC |
| --- | --- | --- |
| 指令系统 | 复杂庞大，指令数目多 | 简单精简，指令数目少 |
| 指令长度 | 不固定（变长） | **定长** |
| 寻址方式 | 丰富（十几种） | 较少（几种） |
| 访存指令 | 大多可访存 | **只有 Load/Store 可访存** |
| 通用寄存器 | 较少 | **大量** |
| 控制器 | 多用**微程序**控制 | 多用**硬布线（组合逻辑）** |
| 流水线 | 难以优化 | **适合流水线** |
| 目标代码 | 程序代码短 | 程序代码较长 |
| 代表机型 | x86 | MIPS、经典 ARM |

- **RISC 的三条硬特征**：指令**定长**、指令与数据**按边界对齐**、**仅 Load/Store 访存**——三条都直接服务于流水线（译码简单、一个存取周期取完、取指与取操作数时间固定）。
- **为什么 RISC 用硬布线**：指令少而规整，译码可用组合逻辑直接实现，比查控制存储器快；CISC 指令复杂多变，用微程序更规整、易扩展。
- **寄存器传参**：RISC 的过程调用用寄存器传参数、配合大量寄存器减少访存；CISC 常压栈传参。
- **速度来源**：RISC 的快来自定长 + 硬布线 + 流水线，不是"指令简单所以单条执行时间短"。

## 考点提醒（易错点）
1. RISC 用**硬布线**、CISC 用**微程序**——把 RISC 说成"用微程序控制器"就是错项（2009-17 就考在这）。
2. "只有 Load/Store 访存"是 RISC 特征，别扩大成"RISC 不能访存"。
3. 定长、对齐、仅 Load/Store 这三条要答出**为什么**：都是为了简化译码、便于流水线（2011-18）。
4. 目标代码长度别记反：CISC 代码**短**、RISC 代码**长**（定长指令有冗余）。
5. x86 始终是 CISC 代表；题给"经典 RISC 特征"时按上表判断，不要用后来的混合形态去推翻结论。
6. RISC 的"精简"指指令**数目少、格式规整**，不等于功能弱（复杂功能交给多条简单指令组合）。

> **真题考情**：> **3/18 年（选 3）**：2009-17（指令条数少、定长、仅取数/存数访存、以硬布线为主）、
> 2011-18（定长 / 对齐 / 仅 Load-Store ⟹ 利于流水线）、2025-17（硬连线、Load/Store、易流水线）。` },
        ],
      },
      {
        name: '第5章 中央处理器',
        topics: [
          { id: 'coa-datapath', hot: true, label: '数据通路与指令执行', status: 'wip', plan: '取指/译码/执行阶段数据流、控制信号逐个点亮' },
          { id: 'coa-pipeline', hot: true, label: '五段流水线与冒险处理', status: 'ready' },
          { id: 'coa-interrupt', hot: true, label: '异常与中断机制', status: 'wip', plan: '中断隐指令、多重中断嵌套、fault/trap 辨析' },
          { id: 'coa-microprog', label: '微程序控制器', status: 'theory', note: `
> **为什么要有它**：硬布线控制器的控制信号由逻辑门"焊死"，指令一改就得重做电路；微程序控制器把每条机器指令的控制信号序列**当数据存进控制存储器**，取微指令、执行微指令即可——改指令只改微程序，规整易扩展（代价是慢）。
> **怎么实现**：一条机器指令对应一段**微程序**（若干条微指令）；微指令 = **操作控制字段**（本拍发出哪些微命令）+ **下地址字段**（下一条微指令的地址），操作控制字段用直接编码、字段直接编码或字段间接编码压缩。
> **记住什么**：微指令的**两段结构** + **字段直接编码位数公式** + **下地址字段位数 = ⌈log₂微指令总条数⌉** + 控制存储器在 CPU 内、按**地址**访问且只读。

## 关键机制
- **层次关系**：机器指令（M1 层）由**微程序解释**执行，微指令（M0 层）直接控制数据通路。控制存储器 **CS** 在 CPU 内、只读，按**地址**访问（不是按内容访问）。
- **微指令格式**：操作控制字段 ‖ 顺序控制（下地址）字段。
- **操作控制字段的三种编码**：
  | 编码方式 | 做法 | 特点 |
  | --- | --- | --- |
  | 直接编码（水平型） | 一位对应一个微命令 | 最快、并行性最好、**字长最长** |
  | 字段直接编码 | 互斥的微命令归入同一字段，相容的分在不同字段 | 字长较短、需译码，**最常考** |
  | 字段间接编码 | 一个字段的含义再由另一字段解释 | 最短、最慢、最复杂 |
- **字段直接编码位数公式**：位数 = Σ 各互斥类 ⌈log₂(类内微命令数 + 1)⌉；**每组多留的 1 个状态表示"本字段不发任何微命令"**。例：33 个微命令分 5 个互斥类（7、3、12、5、6 个）→ 3 + 2 + 4 + 3 + 3 = **15 位**（2012-18）。
- **下地址字段位数**：由**微指令总条数** N 决定，取 ⌈log₂N⌉ 位。例：32 条机器指令 × 平均 4 条微指令 = 128 条，加 2 条公共微指令共 130 条 → ⌈log₂130⌉ = **8 位**（2014-18）。
- **微程序控制器慢的根因**：每执行一条机器指令都要**访问一次以上控制存储器**（取微指令），所以比硬布线慢；但它采用"**存储程序**"原理，修改与扩充容易、灵活性好（2009-19）。
- **入口映射**：机器指令的操作码经映射产生该微程序的首地址，再由下地址字段与判定字段决定后续微指令。

## 考点提醒（易错点）
1. 字段直接编码是 ⌈log₂(**类内数 + 1**)⌉，那个 + 1 是"不发微命令"的状态：类内数恰好是 2 的幂时会差一位（4 个微命令要 **3 位**，不是 2 位）。
2. 公式里的类内数是**互斥类内部**的微命令个数，不是全部微命令数；分组判据是"同一拍能否并行发出"（互斥才能同字段）。
3. 下地址字段位数由**微指令总条数**决定，不是机器指令条数：先算"机器指令数 × 每条平均微指令数 + 公共微指令数"（2014-18 的 130 条）。
4. 控制存储器按**地址**访问且只读，微程序不能在运行时改写；别与主存（可读可写）的属性混。
5. 直接编码最快但字长最长；字段直接/间接编码字长短、并行性受限，需要额外译码——这是"编码方式换字长"的取舍。
6. 微程序控制器用"存储程序"原理，不等于"用软件实现控制"，它是固化在只读存储器里的微指令序列（2009-19）。

> **真题考情**：> **4/18 年（选 4）**：2009-19（"存储程序"原理、慢于硬布线）、2012-18（字段直接编码位数）、
> 2014-18（下地址字段位数）、2017-18（控制存储器存放微指令）。` },
          { id: 'coa-multi', label: '多处理器基本概念', status: 'theory', note: `
> **为什么要有它**：单核主频撞墙后只能"堆核"，但核之间怎么分工、怎么共享数据，决定了程序能不能真的变快——Flynn 分类、硬件多线程、SMP 就是描述"并行度从哪来、数据怎么共享"的基本词汇。
> **怎么实现**：按**指令流 / 数据流**的多少分成 SISD、SIMD、MISD、MIMD；多核与多处理器都属 MIMD；硬件多线程让**一个核**交错执行多个线程以掩盖访存延迟；SMP 让多个对等核共享同一物理地址空间。
> **记住什么**：四种 Flynn 类型的判据（尤其 MIMD 的两个子类）+ **硬件多线程可以出现在单核** + **SMP 的"核间对等 + 共享单一物理地址空间"**。

## 关键机制
| 类型 | 指令流 | 数据流 | 典型代表 |
| --- | --- | --- | --- |
| SISD | 单 | 单 | 传统单处理器（顺序执行） |
| SIMD | 单 | 多 | 向量处理器、阵列处理器，一条指令对一组数据同时运算 |
| MISD | 多 | 单 | 实际很少使用 |
| MIMD | 多 | 多 | 多核处理器、多处理器系统 |

- **MIMD 的两个子类**：**多计算机系统**（各结点有独立地址空间，靠消息传递）与**多处理器系统**（共享内存，SMP 属于此类）；多核属于多处理器系统。
- **硬件多线程**：在**一个核**内交错执行多个线程，用来**掩盖访存延迟**（某线程等访存时换另一线程执行），所以**单核处理器也可以有多线程**。
- **SMP（对称多处理）**：所有核**对等**（无主从之分）、共享**单一物理地址空间**与主存，各核都可用存取指令访问同一片主存地址空间。
- **多核与多处理器的差别**：多核是**一个芯片上**集成多个独立 CPU 核（共享片内 Cache 与互连），多处理器是多个芯片/插槽；两者的并行层次都属于 MIMD。
- **SIMD 与 GPU**：GPU 侧重**吞吐率**，用大量简单核做 SIMD/SIMT 式同构运算，单线程性能并不突出。

## 考点提醒（易错点）
1. **硬件多线程不要求多核**：一个核交错执行多个线程即可，"必须多核才能硬件多线程"是错项（2022-22）。
2. 向量处理器是 **SIMD** 的变体，不要归到 MIMD。
3. SMP 的判据是"**核间对等 + 共享单一物理地址空间**"，与核数多少无关；多计算机系统各有独立地址空间，不算 SMP。
4. MIMD 覆盖面最大（多核、多处理器都在内），SISD 是单处理器顺序执行，MISD 基本没有实际产品。
5. 多核提升的是**并行吞吐**，单线程性能不一定变快（受程序可并行比例与访存带宽限制）。
6. 本章 2022 年才进入考纲，概念辨析为主，别去背具体型号参数。

> **真题考情**：> **1/18 年（选 1）**：2022-22（Flynn 分类、MIMD 两子类、硬件多线程、
> SMP 共享单一物理地址空间）。` },
        ],
      },
      {
        name: '第6章 总线和输入/输出系统',
        topics: [
          { id: 'coa-bus', label: '总线事务与定时', status: 'theory', note: `
> **为什么要有它**：CPU、主存、I/O 若各拉一根专线互连，线数会爆炸且无法仲裁——总线用**一组共享的信号线 + 一套定时规则**定下"谁在什么时候用线、数据怎么传"，成为各部件通信的公共通道。
> **怎么实现**：一次总线事务按**请求 → 仲裁 → 寻址 → 传输 → 释放**的顺序占用总线；传输用同步（统一时钟）或异步（握手应答）定时；带宽 = 工作频率 × 每周期传输次数 × 数据线宽度。
> **记住什么**：**带宽公式与单位换算**（B/s）+ 同步 / 异步 / 半同步的取舍 + **突发传输**（一次地址连传多个数据）+ 串行不一定慢、并行不一定快。

## 关键机制
- **总线事务五阶段**：① 请求总线（某部件提出请求）② 总线仲裁（集中式或分布式决定使用权）③ 寻址（主设备送出目标地址）④ 数据传输（可含多个数据周期）⑤ 状态返回与释放总线。
- **一次总线传输的时间**：总线周期可由**多个时钟周期**构成（2009-20 中 1 个总线周期 = 2 个时钟周期，传 4B）。
- **带宽公式**：总线带宽 = 工作频率 × 每周期传输次数 × 每次传输字节数。
  | 真题场景 | 算式 | 结果 |
  | --- | --- | --- |
  | 10MHz，1 总线周期 = 2 时钟周期，传 4B（2009-20） | 4B ÷ (2 × 0.1µs) | 20 MB/s |
  | 66MHz，32 位（4B），双沿（2014-19） | 66M × 2 × 4B | 528 MB/s |
  | 2.4GHz，16 位（2B），双沿且双向全双工（2020-19） | 2.4G × 2 × 2B × 2 | 19.2 GB/s |
  | 1333MHz，64 位（8B），每周期传 4 次（2025-20） | 1333M × 4 × 8B | 42.66 GB/s（旧文案写 10.66，已按缓存原文更正） |
- **突发传输**：一次给出首地址后连续传送**多个地址连续的数据**（一批数据作为一个独立传输处理），省掉重复寻址开销，提高存储器读写效率与吞吐量（2017-20、2024-20）。注意区分"最大带宽（按周期算）"与"含突发开销的有效带宽（按整次事务算）"——2024-20 两者分别得 6.72 GB/s 与 3.84 GB/s。
- **定时方式**：
  | 方式 | 做法 | 特点 |
  | --- | --- | --- |
  | 同步 | 统一时钟、定宽定时 | 快、控制简单；必须按最慢设备设计 |
  | 异步 | 握手（请求 / 应答） | 慢但灵活，可兼容速度不同的设备 |
  | 半同步 | 时钟 + 等待响应信号 | 兼顾两者 |
- **总线复用**：同一组信号线在不同时间传输不同信息（如地址 / 数据分时复用），用较少的线传更多信息，节省空间与成本。
- **串行与并行**：低频时并行总线更快，但频率高时线间干扰严重、数据无法恢复；串行总线线少、干扰易控，可不断提高时钟频率（PCIe 等高速总线采用串行数据包传输），所以**并行不一定比串行快**。

## 考点提醒（易错点）
1. 带宽单位是 **B/s（字节每秒）**：题干给的是位宽要先除以 8，再乘"每周期传输次数"（双沿 ×2、每周期 4 次 ×4）——漏乘倍数是最高频错误（2025-20 要 ×4 得 42.66 GB/s）。
2. "并行总线一定比串行快""串行总线频率低"都是错的：高频下并行线间干扰使其无法恢复，串行反而能提频（2016-21、2017-20）。
3. **总线周期 ≠ 时钟周期**：一次总线周期可能占多个时钟周期（2009-20 是 2 个），别把时钟频率直接当传输次数。
4. 含**突发传输**时看清问的是"最大带宽"还是"整次事务的有效带宽"：2024-20 两个算法分别得 6.72 GB/s 与 3.84 GB/s，答案取前者。
5. **数据通路 ≠ 总线**：数据通路是各子系统间数据传送的路径（含 PC、ALU、通用寄存器、取指部件），**不包括控制部件**——2017-19 考的是这个，别按总线带宽去算。
6. 同步定时快但必须迁就最慢设备；设备速度差异大时用异步握手更合适。

> **真题考情**：> **11/18 年（选 11 + 大 1）**：选 2009-20、2014-19·20（突发传输定义）、2015-19（同步定时）、
> 2016-21、2017-20、2018-21、2020-19、2021-19、2024-20（突发 6.72 / 3.84 GB/s）、2025-20（42.66 GB/s）；大 2013-43。` },
          { id: 'coa-bus-standard', label: '总线标准（PCI/ISA/USB…）', status: 'wip', legacy: '现行考纲总线部分仅含基本概念、组成及性能指标、总线事务和定时，具体总线标准条目已删；2010 年（PCI/ISA/EISA）、2012 年（USB）、2013 年（PCI/USB/AGP/PCI-E）真题曾考。' },
          { id: 'coa-io', label: 'I/O 方式与 CPU 时间占比计算器', status: 'ready', hot: true },
        ],
      },
    ],
  },
  {
    id: 'os', name: '操作系统', icon: '🖥️', accent: '#10b981',
    chapters: [
      {
        name: '第1章 操作系统概述',
        topics: [
          { id: 'os-intro', label: '运行环境：中断/异常/系统调用', status: 'theory', note: `
> **为什么要有它**：CPU 不能把机器全交给用户程序（会互相破坏、也无法直接操作设备），所以要**划分用户态与内核态**，再用**中断 / 异常 / 系统调用**这三条通道来回切换——它是"受控地提供服务"的入口。
> **怎么实现**：CPU 用状态位区分两态；用户程序通过 trap（自陷）指令主动请求服务，外设与出错分别通过中断、异常被动进入内核，处理完由**中断返回指令**回到用户态。
> **记住什么**：三态切换通道 + **特权指令只能在内核态执行** + 用户态转内核态靠硬件、内核态转用户态靠中断返回指令。

## 三态切换的通道
| 通道 | 触发者 | 例子 |
| --- | --- | --- |
| 中断（外中断） | 外部设备 | I/O 完成、时钟 |
| 异常（内中断） | 当前指令 | 缺页、整数除零、访管/陷入 |
| 系统调用 | 用户程序主动 | trap 指令请求 OS 服务 |

- **仅内核态**：I/O 指令、关中断、置时钟、清内存等；**trap（系统调用/自陷）本身不是特权指令**；
- 用户态到内核态由**硬件**完成切换；内核态到用户态由 OS 执行**中断返回指令**完成。
- 系统调用流程：传参 → trap 进入内核 → 执行服务例程 → 返回。**凡涉及资源/设备的操作必走系统调用**。

## 考点提醒（易错点）
1. **trap 不是特权指令**（2014-25 直接考）：trap、跳转、压栈都能在用户态执行，关中断才是特权指令；
2. 函数调用不等于系统调用：sin() 这类库函数在用户态就能跑（2013-28 判"需要在内核态执行的操作"）；
3. 编译、链接、命令解释程序的运行都在用户态；缺页处理、时钟中断、进程切换在内核态；
4. 判"能不能在用户态执行"先问：**它要不要碰硬件/全局资源**——要碰就必在内核态。

> **真题考情**：> **8/18 年（选 8）**：2010-23、2013-28、2014-25（trap 可在用户态、
> 关中断是特权指令）、2021-32（哪些操作由系统调用完成）、2022-27、2023-26、2025-23、2026-23（内核态执行的操作）。` },
          { id: 'os-structure', label: '操作系统结构（宏内核/微内核）', status: 'theory', note: `
> **为什么要有它**：全部功能塞进内核则一处出错整机崩溃、改动牵一发动全身，全部搬到用户态又慢得不能用——**OS 结构**就是在"性能 ↔ 可靠/可扩展/安全"之间选一个折中。
> **怎么实现**：按"哪些服务留在内核态"来分：宏内核全留内核态；微内核只留中断/调度/基本 IPC，其余服务放用户态进程、彼此用消息传递。
> **记住什么**：微内核的取舍（可靠、安全、可扩展更好，**性能较差**）+ 四条结构线（宏内核/微内核/外核/层次）各自特征。

## 四种结构（2023 真题考微内核特征）
| 结构 | 做法 | 特征 |
| --- | --- | --- |
| 宏内核（单体） | 全部系统服务都在内核态 | 性能好、耦合高（Linux 典型） |
| 微内核 | 内核只留中断/调度/基本 IPC，其余服务放用户态进程 | **可靠性、安全性、可扩展性更好**，消息传递带来性能开销 |
| 外核 | 资源分配与使用分离，库操作系统复用内核缓冲 | 为应用定制、减少抽象层次 |
| 层次结构 | 逐层搭建、单向调用（只能调下层） | 结构清晰、易验证，效率低 |

- 微内核为什么可靠：服务进程崩了只影响自己，不必重启整个内核；为什么慢：一次服务要**多次消息传递 + 多次态切换**。

## 考点提醒（易错点）
1. **微内核的优点与缺点必须配对记**：可靠/安全/可扩展是优点，**性能较差是缺点**，不能只挑一半（2023 真题答案）；
2. 别把"宏内核性能好"记成"宏内核各方面都好"——它的耦合度高、一处崩溃波及整机；
3. 层次结构的判据是**单向调用**：上层只能调下层，反向调用即违反结构定义。

> **真题考情**：> **1/18 年（选 1）**：2023-23（微内核特征：可靠 / 安全 / 可扩展较好，性能较差）。` },
          { id: 'os-boot', label: '操作系统引导与虚拟机', status: 'theory', note: `
> **为什么要有它**：关机后内存与寄存器全是空的，而"把 OS 装进内存"这件事本身也要程序来做——**引导**就是这条从固件到内核的自举链；**虚拟机**再在同一硬件上虚拟出多台可装 OS 的机器。
> **怎么实现**：CPU 从 ROM 的 BIOS/POST 起，依次读 **MBR（0 号扇区）** → 分区引导扇区 → 启动管理器/根目录 → 装载内核；虚拟机由 **VMM** 截获并翻译特权操作来虚拟硬件。
> **记住什么**：引导链的固定顺序 + 安装 OS 三步（物理格式化 → 分区 → 逻辑格式化）+ VMM 特权级高于 OS。

## 引导流程（顺序不能乱）
ROM 的 **BIOS/POST** → 读 **MBR**（0 号扇区：磁盘引导程序 + 分区表）→ 分区引导扇区 PBR → 启动管理器/根目录 → 装载 OS 内核 → 初始化。

## 安装 OS 三步（2021 真题）
**物理格式化**（划分扇区/校验）→ **分区** → **逻辑格式化**（建根目录、空闲管理结构如 FAT/位图）。

## 虚拟机
- **VMM（虚拟机监视器）的特权级高于操作系统**，负责管理硬件资源（2025-24）；
- 一台主机可支持多个虚拟机、可模拟多种指令集；虚拟机里照常能装并运行 OS。

## 考点提醒（易错点）
1. **逻辑格式化不做物理划分**：划分扇区属于物理格式化，"建根目录/空闲表"才是逻辑格式化（2021 真题的判型点）；
2. MBR 固定在 **0 号扇区**（固件第一条就按这个约定去读），分区表也在其中；
3. 虚拟机辨析的关键那句：**VMM 的特权级比 OS 更高**——所以 OS 在虚拟机里跑的是"降级后的特权指令"。

> **真题考情**：> **2/18 年（选 1 + 大 1）**：2021-46（多阶段引导与格式化顺序）、
> 2025-24（VMM 特权级高于 OS）。` },
        ],
      },
      {
        name: '第2章 进程与线程',
        topics: [
          { id: 'os-procstate', hot: true, label: '进程状态转换（五态模型）', status: 'ready' },
          { id: 'os-ipc', label: '进程间通信（共享内存/消息/管道/信号）', status: 'theory', note: `
> **为什么要有它**：进程各有独立地址空间、彼此看不见对方的内存，可并发任务又必须交换数据——**进程间通信**就是内核提供的那套"跨地址空间传数据"的机制。
> **怎么实现**：四条路线——共享内存（两个页表映射同一物理页，最快但同步自理）、消息传递（内核中转）、管道（半双工字节流）、信号（异步事件通知）。
> **记住什么**：四种方式的定位 + **管道读写都可能阻塞** + 共享内存必须自己配同步互斥。

## 四种方式（判型靠这张表）
| 方式 | 特点 |
| --- | --- |
| 共享内存 | 最快；两个进程页表映射**同一物理页**（虚地址可不同）；**必须配同步互斥** |
| 消息传递 | 内核中转（直接通信 / 信箱间接通信） |
| 管道 | 半双工**字节流**；**读写都可能阻塞**（2014-31）；匿名管道用于父子进程 |
| 信号 | 异步事件通知（kill、Ctrl+C） |

- 管道为什么可能阻塞：管道是**固定大小的内核缓冲区**——写满则写者阻塞，读空则读者阻塞；
- 共享内存为什么最快：数据只在内存里搬一次、不经内核中转；代价是**同步要自己写**（用 PV 操作）。

## 考点提醒（易错点）
1. **共享内存最快 ≠ 可以不用互斥**：并发读写同一片内存一样要 PV 操作保护；
2. 管道是**字节流不是消息队列**，一次 write 不保证原样一次 read 读到；
3. 匿名管道只能用于有亲缘关系（父子/兄弟）的进程，命名管道才能给无亲缘进程用；
4. 消息传递由**内核中转**——对用户透明，但一次通信要两次拷贝。

> **真题考情**：> **4/18 年（选 4）**：2014-31（管道读写阻塞）、2025-30（内存映射文件与共享内存）、2026-30（共享文件的页表项）；
> 2022-24（卷面无文本层只记题号）。` },
          { id: 'os-pv', hot: true, label: '同步与互斥 · PV 操作（生产者-消费者）', status: 'ready' },
          { id: 'os-classic', label: '经典同步问题（读者写者/哲学家）', status: 'theory', note: `
> **为什么要有它**：并发程序的正确性全靠同步机制，而"从零设计一套 PV 方案"每道题都要重新想——**经典同步问题**把常考的三种交互（生产-消费、读-写、争资源）固化成了可套用的模板。
> **怎么实现**：每个互斥资源配一个初值 1 的 mutex，每个前后依赖配一个初值等于可用数的同步信号量，再写进程的 P/V 次序。
> **记住什么**：三大问题的信号量分工 + **先同步 P 后互斥 P（颠倒即死锁）** + 哲学家三种破死锁改法。

## 三大经典问题设计要点
1. **生产者-消费者**：empty/full（同步）+ mutex（互斥）；**先同步 P 后互斥 P**，顺序颠倒会死锁（本平台 PV 模块可演示）；
2. **读者-写者**：读者计数器 cnt + rmutex 保护 cnt + wmutex 写互斥；首个读者锁写、末个读者解锁；
3. **哲学家**：5 根筷子全拿会死锁 → **最多 4 人同时拿** / 奇偶号取筷顺序相反 / 一次拿齐。

## 大题套路（固定四步）
列互斥资源 → 每资源一个 mutex（初值 1）、每个前后件一个同步信号量（初值 = 可用数）→ 写进程伪代码 → 查死锁（是否循环等待）。

## 考点提醒（易错点）
1. **信号量初值的物理意义必须写出**：mutex=1（临界区只有 1 个）、empty=N（初始 N 个空位）、full=0（初始无产品）；
2. 写者饥饿：读者源源不断时写者可能永远等不到——读者优先方案的固有缺陷；
3. **哲学家加"m 个碗"实质是加一个初值 m 的资源信号量**，不是改筷子；
4. 大题变体只在标准模型上**加一个约束**，先搭标准框架再补条件，别推翻重写。

> **真题考情**：> **15/18 年（选 6 + 大 11）**：选 2010-25、2016-27、2018-32、2020-32、2022-46、2026-26·27；
> 大 2009-45、2013-45、2014-47（连续取 10 件）、2015-45、2017-46、2019-43、2020-45、2021-45、2024-46、2025-45。` },
          { id: 'os-monitor', label: '管程', status: 'wip', legacy: '现行考纲同步互斥部分以锁、信号量、条件变量表述，管程条目已不再出现；2016 年真题（管程叙述）、2018 年真题（条件变量 x.wait() 的行为）曾考。' },
          { id: 'os-sched', hot: true, label: '处理机调度（FCFS/SJF/HRRN/RR）', status: 'ready' },
          { id: 'os-deadlock', label: '死锁与银行家算法', status: 'ready' },
        ],
      },
      {
        name: '第3章 内存管理',
        topics: [
          { id: 'os-mem-alloc', label: '连续分配与动态分区', status: 'theory', note: `
> **为什么要有它**：多道程序的内存容量不够把所有进程一次装完，只能**按需分配**；分配策略选得差，空闲区就会被切得越来越碎——连续分配与动态分区就是"怎么分、怎么收"的规则。
> **怎么实现**：把空闲区按一定次序组织成链/表，作业到来时按某种规则挑一个够大的分区切给它，回收时与相邻空闲区合并。
> **记住什么**：四种算法的取舍 + **最佳适应外部碎片最多** + 回收必合并相邻空闲区。

## 关键机制
| 算法 | 规则 | 特点 |
| --- | --- | --- |
| 首次适应 | 空闲链按**地址有序**，取第一个够大 | 开销小；高地址大分区得以保留（2017-25） |
| 邻近适应 | 从**上次位置**继续找 | 检索短；大分区易被耗尽 |
| 最佳适应 | 取**最小**够大分区 | **外部碎片最多**（2019-32） |
| 最坏适应 | 取**最大**分区 | 中小作业友好，大分区很快消失 |

## 碎片口径
**内部碎片** = 固定分区/分页（分给进程的分区内部用不完）；**外部碎片** = 动态分区/分段（空闲区太小谁也用不上，可紧凑解决）。

## 考点提醒（易错点）
1. **"最佳"名不副实**：最佳适应留下的碎块最小、最难再利用，**外部碎片最多**（2019-32）；
2. 回收一定要**合并相邻空闲区**（2017-25 就是考合并后的起址与大小）；
3. 邻近适应检索快，代价是**高地址大分区容易被反复切割**——它破坏的正是首次适应的优点；
4. 判内部还是外部碎片看**分配单位是否固定**：固定分区/页有内部碎片，动态分区/段只有外部碎片。

> **真题考情**：> **3/18 年（选 3）**：2010-28（最佳适应）、2017-25（回收合并）、2019-32（碎片）。` },
          { id: 'os-vm-paging', hot: true, label: '页式虚拟内存 · 地址转换（TLB+缺页）', status: 'ready' },
          { id: 'os-lru', hot: true, label: '页面置换 · LRU（含推导表）', status: 'ready' },
          { id: 'os-page-replace', label: 'FIFO / OPT / CLOCK 置换对比', status: 'theory', note: `
> **为什么要有它**：请求分页下进程只占少量物理块，要访问的页不在内存而块又用满时，**必须挑一页换出**——挑得差就频繁缺页，置换算法决定挑谁。
> **怎么实现**：FIFO 淘汰最早进入的页；OPT 淘汰未来最久不用的页（做基准）；LRU 淘汰最近最久未用的页；CLOCK 用访问位 A 循环扫描近似 LRU，改进型 CLOCK 再引入修改位 M。
> **记住什么**：三种规则 + **Belady 异常只有 FIFO 会犯** + 改进型 CLOCK 的 (A,M) 淘汰顺序。

## 关键机制
| 算法 | 淘汰规则 | 特点 |
| --- | --- | --- |
| FIFO | 最早进入内存的页 | 实现最简单；可能出现 **Belady 异常**（框数增、缺页反增，2014-30） |
| OPT | 以后最长时间不被访问的页 | 理论下界，**不可实现**，只作评价基准 |
| LRU | 最近最久未使用的页 | 堆栈类算法，**无 Belady 异常**；硬件开销较大 |
| CLOCK | 循环扫描访问位 A，A=1 则给第二次机会并清 0 | 近似 LRU，开销小 |
| 改进型 CLOCK | 按 **(A,M)** 顺序淘汰：(0,0) → (0,1) → (1,0) → (1,1) | 扫描中把 A 清 0，优先淘汰"未访问且未修改"的页（2016-26） |

## 两条计算类结论
- **缺页率 = 缺页次数 ÷ 访问总次数**；降缺页率的手段：增大页框/工作集、页面缓冲；
- **EAT（有效访问时间）**受缺页率、磁盘读写时间、访存时间、缺页处理 CPU 时间共同影响（2020-28）；
- 多级页表**省空间但增加访存次数**（2026-29）。

## 考点提醒（易错点）
1. **Belady 异常只有 FIFO 会犯**（2014-30 直接考"只有 FIFO 导致 Belady 异常"）；LRU/OPT 属堆栈类算法，不会犯；
2. **命中的页也要更新标记**——漏更新就把 LRU 做成了 FIFO；
3. 改进型 CLOCK 扫描中**只清 A、不动 M**，否则"已修改"信息丢失，被换出的脏页就无法正确回写；
4. 手算题最稳的做法：画"块 × 访问序列"表，逐列标命中/缺页与淘汰对象。

> **真题考情**：> **8/18 年（选 7 + 大 2）**：选 2014-30（Belady）、2016-26（改进型 CLOCK）、2020-28、2022-29·30、
> 2025-26（LRU 缺页 7 次）、2026-29（多级页表）、2018-45；大 2010-46、2025-28（虚拟文件系统）。` },
        ],
      },
      {
        name: '第4章 文件管理',
        topics: [
          { id: 'os-file-alloc', hot: true, label: '文件物理结构与混合索引计算', status: 'ready' },
          { id: 'os-bitmap', label: '外存空闲空间管理（位图/FAT）', status: 'theory', note: `
> **为什么要有它**：外存有成千上万个盘块，必须随时回答"哪块空着"并能快速分配/回收——**位图与 FAT** 就是外存空闲空间的两种登记结构（位图按位标记，FAT 按链表串联文件块）。
> **怎么实现**：位图每个盘块占 1 位（0 空闲 1 占用）；FAT 每个盘块一个表项，存同文件下一块的块号。
> **记住什么**：位示图的字号/位号换算 + **位图大小只由总块数决定，与当前空闲数无关**。

## 位示图换算（先确认 0 基还是 1 基！）
- 每字 **n** 位，盘块号 **b**：**0 基**口径 字号 = \\(\lfloor b \div n \rfloor\\)、位号 = \\(b \bmod n\\)；
- **1 基**口径（教材常用）：字号 \\(i = \lfloor (b-1) \div n \rfloor + 1\\)、位号 \\(j = (b-1) \bmod n + 1\\)；
- 位图总字节数 = 总块数 \\(\div 8\\)——**与当前空闲块数无关**（2024-26）；
- 位图自身也占盘块（2015-31 的位图就存在固定块号区间里）。

## FAT 与位图的定位
- **FAT 记录外存块的使用情况**（2025-31）：表项串成链表表达"同文件的下一块"，所以文件块号可以离散；
- 目录项记录文件元信息、系统打开文件表记录打开状态、FCB 保存文件控制信息——**都替代不了位图/FAT 的空闲登记作用**。

## 考点提醒（易错点）
1. **位图大小只由总块数算**（总块数 ÷ 8），**别用"当前空闲块数"**（2024-26）；
2. 求字号必须先减 1 再整除（1 基）——否则落错字、差一个盘块（2015-31 释放盘块的定位就是它）；
3. 0 基/1 基题目必先声明口径，两种算法结果可以不同；
4. 位图本身占用盘块，计算"系统总共花多少空间"时不能漏。

> **真题考情**：> **5/18 年（选 4 + 大 1）**：大 2010-45（2KB 位图 → 16384 块）、选 2014-27（位图所需簇数）、
> 2015-31（释放盘块定位）、2024-26（占用空间与空闲块数无关）、2025-31（FAT 记录外存块）。` },
        ],
      },
      {
        name: '第5章 输入/输出管理',
        topics: [
          { id: 'os-disk-sched', label: '磁盘调度 SSTF / SCAN / C-SCAN', status: 'ready', hot: true },
          { id: 'os-buffer', label: '缓冲区管理（单/双缓冲）', status: 'theory', note: `
> **为什么要有它**：CPU 与 I/O 设备速度差几个数量级，若每块数据都"读一块—算一块"串起来做，CPU 有大半时间在干等——**缓冲区**就是插在中间、让读入与处理并行的内存区。
> **怎么实现**：单缓冲只有一块缓冲，读入与传送/分析不能完全重叠；双缓冲设两块，一块在传数据时另一块已在读入下一块。
> **记住什么**：单缓冲与双缓冲的两个时间公式 + 双缓冲用 max(T, C+M) 而不是 max(T, C)。

## 缓冲时间公式（T 读入、M 传送、C 分析）
- **单缓冲**：每块 = max(T, C) + M；n 块 = n × max(T, C) + T + M（首块多读入一次）；
- **双缓冲**：读入与传送并行 → **n 块 = n × max(T, C+M) + T**；
- 套真题：单缓冲 100/5/90 两块 = 2 × max(90,5) + 100 + 5 = **295μs**（2013-27）；单缓冲 1550μs → 双缓冲 **1100μs**（2011-31）。

## 其他要点
- 缓冲属于**设备独立性软件层**（不是驱动程序），对用户透明；
- 目的 = 缓和 CPU 与设备的速度差 + 减少 I/O 次数；进一步还有循环缓冲、缓冲池。

## 考点提醒（易错点）
1. 单缓冲 n 块为什么**多加 T + M**：最后一块的读入与传送没有下一块来重叠，必须单独算；
2. 双缓冲为什么是 max(T, C+M)：**读入下一块（T）与"分析当前块 C + 传送当前块 M"重叠**，两者取长；
3. 别把 M 漏掉或与 C 相加错位——C 与 M 是同一块上先后发生的两步，加起来才是"处理完一块"；
4. 看清题目问的是"**1 块**"还是"**n 块**"的时间，公式不一样。

> **真题考情**：> **2/18 年（选 2）**：2011-31（单缓冲 1550 → 双缓冲 1100μs）、
> 2013-27（单缓冲 2 块的时间）。` },
          { id: 'os-ioctl', label: 'I/O 控制方式与软件层次', status: 'theory', note: `
> **为什么要有它**：I/O 设备型号千差万别，若让用户程序或上层软件直接面对寄存器与设备细节，OS 就无从移植——**I/O 软件分层 + 三种控制方式**把"通用逻辑"与"硬件细节"隔开。
> **怎么实现**：自下而上分四层——中断处理程序 → 设备驱动程序 → 设备独立性软件 → 用户层 I/O 软件，各自只跟相邻层打交道。
> **记住什么**：四层的固定次序 + 驱动程序算 CHS + 中断处理程序把数据放进内核缓冲区。

## I/O 软件四层（自下而上，2012-26 考排序）
中断处理程序 → **设备驱动程序** → **设备独立性软件** → **用户层 I/O 软件**（+ 硬件在最底）。
| 层 | 管什么 |
| --- | --- |
| 中断处理程序 | 被中断唤醒；把设备数据寄存器内容取到**内核缓冲区**（2024-31）；解析含义、唤醒等待进程 |
| 设备驱动程序 | 厂家按硬件写；**计算 CHS（柱面/磁头/扇区）**、设置寄存器启动设备（2013-25、2022-32） |
| 设备独立性软件 | 命名、保护、缓冲、分配、SPOOLing；向用户提供统一接口 |
| 用户层 I/O 软件 | 库函数与系统调用接口（open/read/write 的库包装） |

## 控制方式的定位（判型）
程序直接控制（轮询）→ 中断驱动 → DMA → 通道：**越往后 CPU 干预越少、并行度越高**。

## 考点提醒（易错点）
1. **层次次序不能颠倒**：用户层 I/O 软件在最上、中断处理程序在最下（2012-26 就是给四层排序）；
2. **CHS 由驱动程序算**（2013-25）：它因设备硬件而异，所以归厂家提供的驱动程序，而不是独立软件层；
3. **中断处理程序把数据取到内核缓冲区**（2024-31），解析、同步用户缓冲都在中断返回之后做——分清"取数"与"解析"的先后；
4. 用户态到内核态只能靠**中断 / 系统调用**；设备分配要考虑类型、状态、权限与映射（2023-32）。

> **真题考情**：> **9/18 年（选 10 + 大 1）**：选 2009-32（设备独立性）、2011-26（四层排序）、2013-25（驱动算 CHS）、
> 2019-24、2022-32、2024-31、2026-21·31；大 2012-26、2019-44（簇号转物理地址由什么程序完成）。` },
        ],
      },
    ],
  },
  {
    id: 'net', name: '计算机网络', icon: '🌐', accent: '#f59e0b',
    chapters: [
      {
        name: '第1章 计算机网络概述',
        topics: [
          { id: 'net-layers', label: '分层模型（OSI vs TCP/IP）', status: 'theory', note: `
> **为什么要有它**：一次跨网通信要同时解决比特传输、寻址、路由、可靠、应用语义等一堆问题——**分层把"大而杂"拆成"各管一段"的若干层**，否则任一层换技术就得全网重做。
> **怎么实现**：每层只在同层之间用协议对话，并向紧邻的上一层提供服务（服务通过层间接口调用，上层不必知道下层怎么实现）。
> **记住什么**：**OSI 七层与 TCP/IP 四层的对应 + 每层的协议数据单元与代表协议 + 常见设备的最高层号**。

## 一、OSI 七层 ↔ TCP/IP 四层 ↔ 五层教学模型
| OSI | TCP/IP | 五层 | 这一层干什么 | 代表协议/设备 |
| --- | --- | --- | --- | --- |
| 应用 / 表示 / 会话 | 应用 | 应用 | 用户服务；表示层管数据格式与加密，会话层管会话 | HTTP、DNS、FTP、SMTP、POP3 |
| 传输 | 传输 | 传输 | **端到端**（进程到进程，用端口区分进程） | TCP、UDP |
| 网络 | 网络 | 网络 | 分组从源到目的的转发与路由选择 | IP、ICMP、ARP、路由协议；路由器 |
| 数据链路 | 网络接口 | 数据链路 | 相邻结点之间的成帧、差错控制、介质访问 | 以太网、PPP；交换机、网桥 |
| 物理 | 网络接口 | 物理 | 比特流传输、机械电气接口 | 集线器、中继器、双绞线 |

## 二、三条必背判据
1. **第一个提供端到端（进程间）服务的是传输层**，网络层及以下都是点到点/主机到主机（2009-33）；
2. **TCP/IP 的网络层只提供无连接、尽最大努力交付的数据报服务**——IP 首部里没有"面向连接"的连接字段（2011-33）；
3. **IP 直接为其上层协议提供服务**：ICMP 报文被封装在 IP 分组里，所以直接为 ICMP 服务的是 IP（2012-33）。

## 三、设备看"最高层"（2016-33 的考法）
- 路由器工作到**网络层（第 3 层）**，交换机/网桥工作到**数据链路层（第 2 层）**，集线器/中继器只在**物理层（第 1 层）**。

## 考点提醒（易错点）
1. **层次越多越好的说法是错的**（2026-33）：层数越多，封装/解封装的额外开销越大，效率反而降低；
2. OSI 里应用层的相邻层是**表示层**（2013-33），别答成会话层——会话层在表示层**下面**；
3. **"端到端"专指传输层**，链路层是相邻结点之间、网络层是整个网络范围；
4. TCP/IP 的网络接口层**不规定具体协议**，凡是能传 IP 分组的物理网（以太网、WiFi、PPP）都算，不要背成只有以太网。

> **真题考情**：> **9/18 年（选 9）**：2009-33、2011-33、2012-33、2013-33、2014-33（相邻层服务）、
> 2016-33、2017-33（PDU 额外开销）、2022-33（相邻结点间流量控制）、2026-33。` },
          { id: 'net-model', label: '网络应用模型（C/S 与 P2P）', status: 'theory', note: `
> **为什么要有它**：网络上的程序总要有一方"等着被找"、另一方"主动去找"，否则两个进程无从建立通信——**应用模型就是约定这个角色分工**，它决定服务能力由谁承担、扩展性能做到多大。
> **怎么实现**：C/S 让服务集中在少数常驻的服务器进程上，客户进程按需发起请求；P2P 不设固定服务方，每个结点同时跑客户与服务器两种角色，彼此直接交换资源。
> **记住什么**：**两种模型"谁是服务方/客户方"的分工 + 缺点归谁 + BT/文件分发这类场景该判给谁**。

## 两种模型对照
| 比较项 | C/S（客户/服务器） | P2P（对等） |
| --- | --- | --- |
| 角色 | 服务器**常驻**、被动等待；客户主动发起 | 每个结点**既是客户又是服务器** |
| 客户之间 | **不直接通信**，一切经服务器转发 | 可直接互相通信、互相提供资源 |
| 提供服务的能力 | 集中于服务器，服务器是瓶颈、也易受攻击 | 分散在各结点，**结点越多服务能力越强（自扩展）** |
| 管理 | 集中管理容易 | 分散、管理难、安全性差 |

## 三条必背判据
1. **C/S 中客户之间不直接通信**（2019-40 的判定点）；
2. 同一台主机上可以同时运行多个服务器/客户进程，靠**端口号**区分；
3. 判型：只要"用户越多下载越快、彼此互为资源提供者"，就是 P2P（BT、文件分发）。

## 考点提醒（易错点）
1. **C/S 的"不是"要背清**：客户之间不直接通信、服务不由客户提供、服务器常驻而非按需启动；
2. **P2P 不等于没有服务器**：可以有索引/目录服务器（如 BT 的 Tracker），但**文件内容不经服务器转发**，那才是关键区别；
3. 一个"客户程序"与一个"服务器程序"只是角色分工，**同一进程在不同场景里可以换角色**，不要绑定到具体机器。

> **真题考情**：> **1/18 年（选 1）**：2019-40（网络应用模型辨析）。` },
          { id: 'net-perf', label: '性能指标与时延计算', status: 'theory', note: `
> **为什么要有它**：说"网慢"必须能拆开量化到底慢在哪一段——**性能指标把一次传输的耗时分解成发送、传播、处理、排队四部分**，否则无法判断该提带宽还是该缩距离。
> **怎么实现**：发送时延由分组长度与发送速率算出，传播时延由链路长度与信号传播速率算出，两者**互相独立**；把各段的这四类时延相加即得端到端时延。
> **记住什么**：**时延带宽积的含义与单位 + 吞吐量由瓶颈链路带宽决定 + 发送时延与传播时延彼此独立**。

## 一、时延四项（逐项会算）
| 时延 | 公式 | 由什么决定 |
| --- | --- | --- |
| 发送时延（传输时延） | \\(T_d = \frac{L}{R}\\) | 分组长度 L 与**发送速率** R，与距离无关 |
| 传播时延 | \\(T_p = \frac{D}{V}\\) | 链路长度 D 与**信号传播速率** V，与带宽无关（铜/光纤约 \\(2\times 10^8\\) m/s） |
| 处理时延 | 查表/校验耗时 | 结点处理能力 |
| 排队时延 | 取决于排队长度 | 网络拥塞程度 |

\\(L\\) 是分组长度（bit），\\(R\\) 是发送速率（b/s），\\(D\\) 是链路长度（m），\\(V\\) 是信号传播速率（m/s）。

## 二、三个必背指标
1. **时延带宽积 = 传播时延 × 带宽**，单位是**比特**，含义是"链路上正在传播的比特数"，即**以比特为单位的链路长度**；
2. **往返时间 RTT**：从发出到收到确认的总时间，既含双向传播时延也含中间结点的处理与排队；
3. **吞吐量 = 瓶颈链路带宽**：端到端路径上实际能通过的速率由**最慢的那一段**决定，其他链路再快也没用（2024-33）。

## 三、分组交换的端到端时延（与 net-swap 同源）
路径有 \\(k\\) 段链路、每段发送速率为 \\(R\\)、分组长度为 \\(P\\)、单个分组的发送时延为 \\(T_d = P/R\\)、每段传播时延为 \\(T_p\\)：
\\(T = k \cdot T_d + k \cdot T_p = \frac{P}{R} + (k-1)\frac{P}{R} + k \cdot T_p\\)
**为什么**：分组可以流水线转发，只有最后一个分组要排队等前 \\((k-1)\\) 个分组先经各结点转发完；传播时延不随分组数变化，只按链路的段数与长度算。
例：8 Mb 文件、分组 1 KB、\\(R=100\\) Mbps、经 2 个分组交换机 ⟹ \\(T_d=80\\) ms，再加 \\(2\times 0.08\\) ms 转发的 \\(0.16\\) ms，共 **80.16 ms**（2010-34）；1500 B 在 2 Mbps 上发送时延为 **6 ms**（2026-34）。

## 考点提醒（易错点）
1. **发送时延与传播时延互不影响**：带宽变大只缩短发送时延，传播时延由距离与介质决定，不会变小；
2. **传播时延与链路带宽无关**，但**发送时延与链路带宽有关**——这两个"无关"是常考的反向陷阱；
3. 算总时延别漏**中间结点的转发时延**（每个中间结点一个 \\(T_d\\)，共有 \\(k-1\\) 个）；
4. 传播时延若题中未给就按"忽略"处理，但**建链（电路交换的建立时间）不能漏**——那是另一类时延，判型见 net-swap；
5. **吞吐量不是带宽**：带宽是链路的最大能力，吞吐量是实测通过量，有瓶颈或拥塞时吞吐量低于带宽（2024-33）。

> **真题考情**：> **4/18 年（选 4）**：2010-34（分组交换时延）、2013-35（报文 vs 分组）、
> 2024-33（吞吐量 = 瓶颈带宽）、2026-34（发送时延）。` },
        ],
      },
      {
        name: '第2章 物理层',
        topics: [
          { id: 'net-shannon', hot: true, label: '奈奎斯特定理与香农定理', status: 'ready' },
          { id: 'net-encode', label: '编码与调制', status: 'theory', note: `
> **为什么要有它**：数字数据要送上模拟信道（或要让接收方从波形里恢复出比特），必须规定"什么波形代表 1、什么代表 0"——**编码与调制就是这套映射规则**，映射选得不好接收方就找不准比特边界。
> **怎么实现**：基带传输直接把比特映射成电平波形（NRZ、NRZI、曼彻斯特），带通传输则用数字信号去改变载波的幅度/频率/相位（ASK、FSK、PSK、QAM）。
> **记住什么**：**四种编码的规则 + 曼彻斯特的波特率是比特率的 2 倍 + 由最小码距判检错纠错位数**。

## 一、四种编码对照（无自同步能力的最容易考）
| 编码 | 规则 | 自同步 | 典型用途 |
| --- | --- | --- | --- |
| NRZ 不归零 | 高电平 = 1、低电平 = 0，一个码元一个比特 | **不能**（长串同值会失去边界） | 简单基带传输 |
| NRZI 不归零倒置 | **电平翻转表示 0、不翻转表示 1**（差分思想） | 不能 | USB 等 |
| 曼彻斯特 | 每个比特的**中间必有一次跳变**；由中间跳变的方向定比特值 | **能** | 10BaseT 传统以太网 |
| 差分曼彻斯特 | 每个比特**中间必跳变**（只作时钟）；**比特开始处有无跳变定值**：有跳变 = 0、无跳变 = 1 | 能，且抗极性反接 | 令牌环 |

## 二、波特率与比特率
- 曼彻斯特与差分曼彻斯特的**波特率 = 2 × 比特率**（一个比特被拆成两个码元，中间那次跳变被占用）；
- 若一个码元携带 \\(n\\) 个比特，则比特率 = 波特率 \\(\times n\\)。

## 三、最小码距 \\(d\\) 的两条结论（2025-34）
| 能力 | 上限 |
| --- | --- |
| 检错 | 可检出 \\(d-1\\) 位错 |
| 纠错 | 可纠正 \\(\lfloor (d-1)/2 \rfloor\\) 位错 |

## 考点提醒（易错点）
1. **曼彻斯特是"每比特中间必跳变"**，读图先按中间跳变切出比特边界，再按起始处有无跳变定 0/1（差分曼彻斯特）；
2. **差分曼彻斯特与曼彻斯特的区别只在"值"放在哪里**：曼彻斯特用中间跳变定值，差分用起始跳变定值，两者中间那一次跳变都只为同步；
3. **NRZ/NRZI 不能自同步**，只有曼彻斯特族能——"自同步"是判断题的高频点；
4. 检错与纠错别混：\\(d=3\\) 时可检 2 位、纠 1 位；**纠错能力总是约为检错能力的一半**。

> **真题考情**：> **8/18 年（选 8）**：2009-34（QAM + 奈奎斯特）、2011-34（波特率与比特率）、2013-34（曼彻斯特读比特串）、
> 2015-34（NRZ/NRZI/曼彻斯特）、2021-34（差分曼彻斯特还原）、2022-34、2024-34（FSK）、2025-34（最小码距）。` },
          { id: 'net-swap', label: '电路/报文/分组交换对比', status: 'theory', note: `
> **为什么要有它**：一条链路不能只给一对用户用，中间结点必须先"收下来再发出去"或"先建好通路再发"——**交换方式决定资源以何种粒度被占用**，直接决定总时延、实时性和带宽浪费程度。
> **怎么实现**：电路交换先端到端建立独占通路再传数据；报文交换把整个报文在各结点存储转发；分组交换把报文切小，各结点可流水线地收一个转一个。
> **记住什么**：**三种交换的总时延公式与大小关系 + 数据报与虚电路的区别点（虚电路不预分配带宽）**。

## 一、三种交换方式对照
| 方式 | 建立阶段 | 总时延（路径 \\(k\\) 段、每段速率 \\(R\\)、报文/分组长度 \\(L\\)） | 特点 |
| --- | --- | --- | --- |
| 电路交换 | 需要**建立时间 \\(T_s\\)**，通路独占 | \\(T_s + \frac{L}{R} + k \cdot T_p\\) | 时延小、实时性好；空闲通路也白占带宽 |
| 报文交换 | 无 | 整个报文在每个结点都要完整收发一遍，约 \\(k \cdot \frac{L}{R}\\) | 无需建立；**存储转发时延最大** |
| 分组交换 | 无 | \\(\frac{L}{R} + (k-1)\frac{P}{R} + k \cdot T_p\\)（\\(P\\) 为分组长度） | 流水转发，**总时延最小**；有额外首部开销 |

## 二、判据（先算三个总时延再排序）
1. 分组长度 \\(P\\)、报文长度 \\(L\\)、报文被切成 \\(L/P\\) 个分组时，**只有最后一个分组要等前面各结点转发完**，故附加项为 \\((k-1)\frac{P}{R}\\)；
2. 2013-35 实测：8 Mb 报文、\\(R=10\\) Mbps，不分组的报文交换总时延为 \\(800\times 2 = 1600\\) ms，而切成 800 个分组后为 \\(800 + 1 = 801\\) ms；
3. 2025-33 实测：电路交换 \\(1.6\\) s 加上 \\(32\ \mu\\)s 建立时间，报文交换更大，于是 **分组交换小于电路交换小于报文交换**。

## 三、数据报与虚电路（两种分组交换）
| 比较项 | 数据报 | 虚电路 |
| --- | --- | --- |
| 连接 | 无连接，每个分组独立选路 | 先建立一条虚连接，分组按虚电路号（VCID）转发 |
| 分组顺序 | 可能乱序、可能丢失 | 同一虚电路的分组按序到达 |
| 资源 | 不预留 | **也不预分配带宽**（与电路交换的本质区别） |
| 故障影响 | 结点失效可绕行 | 虚电路断则整条失效 |

## 考点提醒（易错点）
1. **电路交换的总时延必须加上建立时间**（2025-33 的 \\(32\ \mu\\)s），漏了它就选错；
2. 分组交换的快**只来自流水转发**，不是因为它传得更快——单个分组的发送时延与报文按同样的 \\(L/R\\) 处理；
3. **虚电路不预分配带宽**：它只是先建一条逻辑通路，带宽仍是共享的，别与电路交换的独占通路混为一谈；
4. 报文交换的"存储转发"是**整个报文**收完才转发，所以时延最大；分组交换收到一个分组就能转发。

> **真题考情**：> **4/18 年（选 4）**：2010-34、2013-35（报文 vs 分组时延）、
> 2020-34（虚电路网络）、2025-33（电路 vs 分组总时间）。` },
        ],
      },
      {
        name: '第3章 数据链路层',
        topics: [
          { id: 'net-crc', label: 'CRC 循环冗余校验', status: 'ready' },
          { id: 'net-hdlc', label: 'HDLC 组帧（广域网）', status: 'wip', legacy: '现行考纲广域网部分仅含 PPP，HDLC 已删；2013 年真题曾考零比特填充组帧。' },
          { id: 'net-window', hot: true, label: '滑动窗口可靠传输（停等/GBN/SR）', status: 'ready' },
          { id: 'net-csma', hot: true, label: 'CSMA/CD 与最小帧长', status: 'ready' },
          { id: 'net-switch', hot: true, label: '以太网交换机自学习', status: 'ready' },
        ],
      },
      {
        name: '第4章 网络层',
        topics: [
          { id: 'net-subnet', hot: true, label: 'IPv4 地址与子网划分', status: 'ready' },
          { id: 'net-fragment', label: 'IP 分片与重组', status: 'ready' },
          { id: 'net-routing', hot: true, label: '路由算法与协议（RIP/OSPF/BGP）', status: 'ready' },
          { id: 'net-ipv6', label: 'IPv6（地址与特点）', status: 'theory', note: `
> **为什么要有它**：IPv4 只有 32 位地址、早已分配殆尽，且首部字段多、路由器处理慢——**IPv6 用 128 位地址把空间放大，同时把首部简化、取消首部校验和**，让路由器转发更快。
> **怎么实现**：地址从 32 位扩到 128 位；基本首部固定为 40 字节、只有 8 个字段；可选项全部移到扩展首部，由路由器按需处理。
> **记住什么**：**地址位数与首部定长 + :: 只能出现一次 + 过渡技术只有双协议栈与隧道两类 + HopLimit 等价于 TTL**。

## 一、IPv6 与 IPv4 的关键差别（对比题就考这些）
| 比较项 | IPv4 | IPv6 |
| --- | --- | --- |
| 地址长度 | 32 位 | **128 位**，地址空间是 IPv4 的约 \\(2^{96}\\) 倍 |
| 基本首部 | 20~60 字节，**变长** | **固定 40 字节**，8 个字段 |
| 首部校验和 | 有 | **取消**（交给传输层保证） |
| 分片 | 路由器可分片 | **只有源主机分片**，中间路由器不分片 |
| 生存时间 | TTL | **HopLimit 跳数限制，作用等价于 TTL** |
| 其他 | — | 流标号可用来预分配资源、支持 QoS |

## 二、地址表示的三条规则
1. 写成 **8 组 16 位十六进制数**，组间用冒号分隔；
2. 每组内的**前导 0 可以省略**（中间与末尾的 0 不能省）；
3. **连续的全 0 组可以用 :: 压缩，但 :: 在一整个地址里只能出现一次**（出现两次就无法判断各自代表几组 0）。

## 三、从 IPv4 过渡到 IPv6 的两种技术
- **双协议栈**：一台主机同时装 IPv4 和 IPv6 两套协议栈，按目的地址选用；
- **隧道技术**：把 IPv6 数据报整体封装进 IPv4 数据报的数据部分，穿过只支持 IPv4 的路由器。

## 考点提醒（易错点）
1. **首部"定长 40 字节"是 IPv6 的标签**，但可选项并没有被删除，而是挪进了**扩展首部**；
2. **:: 只能出现一次**、前导 0 可省是两条独立的规则，别把"可省"用成"可以省中间或末尾的 0"；
3. **HopLimit 不是新增功能**，它就是把 TTL 换了名字，作用一致（2023-40 的判定点之一）；
4. **隧道与双栈是两个不同的选项**，隧道是"IPv6 装进 IPv4 里传"，不是"把地址翻译成 IPv4"。

> **真题考情**：> **1/18 年（选 1）**：2023-40（IPv4 与 IPv6 的基本首部与特点对比）。` },
          { id: 'net-arp', label: 'ARP / DHCP / ICMP / NAT', status: 'theory', note: `
> **为什么要有它**：IP 地址是给路由用的逻辑地址、MAC 地址才是链路真正认的物理地址，且主机往往没有静态 IP——**这四个协议分别解决"IP 到 MAC 怎么问""IP 从哪来""出错怎么报""私网怎么上网"**，缺一个主机就上不了网。
> **怎么实现**：ARP 在同网段广播询问并单播应答；DHCP 用四步交互把地址等配置租给主机；ICMP 把差错与询问封装进 IP 分组回送；NAT 在出口路由器上改写地址与端口并记表。
> **记住什么**：**DHCP 四步各自的源/目的地址 + ARP 请求是广播而应答是单播 + NAT 只改源地址/端口且校验和要重算**。

## 一、四个协议对照
| 协议 | 干什么 | 层次 | 关键点 |
| --- | --- | --- | --- |
| ARP | 由 IP 地址求同一网段内目的主机的 MAC 地址 | 网络层 | 请求**广播**（目的 MAC 全 1）、应答**单播**；有 ARP 高速缓存 |
| DHCP | 动态为主机分配 IP 地址、掩码、网关、DNS 等 | 应用层，基于 **UDP** | 四步：Discover、Offer、Request、ACK |
| ICMP | 差错报告（终点不可达、超时、参数问题等）与询问（回送请求/应答） | 网络层，**封装在 IP 分组中** | 由 IP 直接为其提供服务，不是传输层协议 |
| NAT | 让私网主机共用少量公网地址访问互联网 | 出口路由器上 | 出方向替换**源 IP 与源端口**并记入 NAT 表 |

## 二、DHCP 四步的地址用法（高频，逐格背）
| 报文 | 源 IP | 目的 IP | 说明 |
| --- | --- | --- | --- |
| Discover | 0.0.0.0 | 255.255.255.255 | 主机还没有 IP，只能广播找服务器 |
| Offer | 服务器地址 | 255.255.255.255 | 服务器提出可用的地址 |
| **Request** | **0.0.0.0** | **255.255.255.255** | 主机仍未正式获得地址，**仍用广播**（2022-47、2025-36） |
| ACK | 服务器地址 | 255.255.255.255 | 确认租用，主机此后才可用该地址 |

## 三、ARP 与 NAT 的必背细节
1. **跨网段时 ARP 问的是默认网关的 MAC**，而不是最终目的主机的 MAC——因为目的主机不在本网段，帧必须先交给网关；
2. ARP 请求帧是**广播**（目的 MAC 全 1）、应答帧是**单播**；
3. **NAT 出方向改源 IP，可能同时改源端口**（多个内网主机复用一个公网地址时靠端口区分），**目的端口与总长度不变**；由于 IP 首部和 UDP 首部都被改动，**IP 首部校验和与 UDP 校验和都必须重新计算**（2025-37）。

## 考点提醒（易错点）
1. **DHCP 的 Request 仍是广播**（源 0.0.0.0、目的 255.255.255.255）——它既能确认选中的服务器，也让其他服务器撤回自己的 Offer，别答成单播；
2. **ICMP 不是传输层协议**，它是网络层协议且数据被封装在 IP 分组里；ping 用的是 ICMP 回送请求/应答，traceroute 靠 TTL 逐跳超时；
3. **NAT 改写的是源地址**（出方向），入方向才对应改写目的地址；改端口的原因是**端口复用**，不是"必须改端口"；
4. 跨网段通信时**源与目的 MAC 都会随链路改变，IP 地址不变**。

> **真题考情**：> **5/18 年（选 2 + 大 4）**：大 2011-47、2015-47、2020-47（NAT）、
> 2022-47（DHCP）；选 2025-36（Request 源 0.0.0.0）、2025-37（NAT）。` },
        ],
      },
      {
        name: '第5章 传输层',
        topics: [
          { id: 'net-tcp-conn', hot: true, label: 'TCP 连接建立与释放（三次握手/四次挥手）', status: 'ready' },
          { id: 'net-tcp-cc', hot: true, label: 'TCP 拥塞控制（慢启动/拥塞避免/快重传）', status: 'ready' },
          { id: 'net-tcp-reliable', label: 'TCP 可靠传输与流量控制', status: 'theory', note: `
> **为什么要有它**：IP 只提供尽最大努力交付，分组会丢、会乱序、会重复，而应用要的是"一字不差的字节流"；同时接收方缓存有限——**TCP 用序号/确认号加滑动窗口，把不可靠的 IP 变成可靠且不淹没接收方的字节流**。
> **怎么实现**：发送方为每个字节编号，靠**累积确认**推进发送窗口；超时或收到三个重复确认就重传；接收方用接收窗口通告自己还能收多少。
> **记住什么**：**确认号 = 期望收到的下一个字节 + 发送窗口 = min(接收窗口, 拥塞窗口) + 零窗口要发探测**。

## 一、序号与确认号（推算题的公式）
- \\(seq\\) = 本报文段**数据部分第一个字节**的编号；
- \\(ack\\) = **期望收到对方的下一个字节的编号** = 最后连续收到的字节编号 + 1，且是**累积确认**：ack 之前的字节全部已收到；
- **SYN 与 FIN 各占 1 个序号**（不携带数据也要消耗一个序号），纯 ACK 段不占序号。

## 二、窗口
\\(W = \min(rwnd,\ cwnd)\\)
其中 \\(rwnd\\) 是接收方通告的接收窗口，\\(cwnd\\) 是发送方的拥塞窗口；可发送区间为 \\([ack,\ ack + W - 1]\\)。

## 三、模板（照这个式子套就能得分）
1. 收到 seq=1913、ack=2046、载荷 100 B 的段 ⟹ 己方回复 **seq=2046**（即**照抄对方的 ack 当自己的 seq**）、**ack=1913+100=2013**（2013-39）；
2. 连续收到 300 B 与 500 B 两段 ⟹ 返回的确认号 = 起始号 200 加两段长度，即 **1000**（2009-38）；
3. 建立连接时己方初始序号为 1000 ⟹ 对方回的 SYN+ACK 里 **ack=1001**（**SYN 占 1 个序号**，2026-47）；
4. 窗口通告 ack=501、rwnd=500 ⟹ 下一次可发送 **501 到 1000**（2021-40）。

## 四、流量控制
- 接收方靠**通告窗口**限制发送速率，实现端到端的流量控制（不是链路层的相邻结点流量控制）；
- **rwnd = 0** 时发送方停发并启动**持续计时器**，到点发**零窗口探测**报文，等对方通告新窗口后再发。

## 考点提醒（易错点）
1. **ack 是"我期待的下一个字节"，不是"我最后收到的字节"**，两者恰好差 1，写答案时最容易差这个 1；
2. **SYN 与 FIN 各占 1 个序号**：初始序号 1000 时 SYN 的确认号是 1001；FIN 段同理要占一个序号；
3. **ack 与 rwnd 同时到达时，两者对应的是不同的东西**：ack 决定从哪一字节开始发，rwnd 决定还能发多少字节（括号区间别算错端点）；
4. **发送窗口取接收窗口与拥塞窗口的较小值**，只写 rwnd 会漏掉拥塞控制这一路限制；
5. 零窗口时**必须发探测**：接收方从 0 窗口恢复后不会主动通知，靠探测才不会永久停发。

> **真题考情**：> **6/18 年（选 5 + 大 1）**：选 2009-38、2011-40、2013-39、2019-38（快重传）、2021-40（ack / rwnd 求可发区间）；
> 大 2026-47（拥塞窗口与发送窗口）。` },
          { id: 'net-udp', label: 'UDP 用户数据报协议（首部 / 校验和 / 与 TCP 对比）', status: 'theory', note: `
> **为什么要有它**：不是所有应用都要"一字不差的字节流"——实时音视频宁可丢一帧也不要等重传，DNS 查询一问一答只要一个来回；**TCP 的建连、确认、重传、拥塞控制这些开销对它们是纯负担**，于是传输层另有一条极简路线：UDP 只做"复用/分用 + 差错检测"，把可靠性交还给应用自己。
> **怎么实现**：UDP 在 IP 之上只加 **8 字节固定首部**（源端口 / 目的端口 / 长度 / 校验和），发出去就不管了；**没有连接、没有确认、没有重传、没有拥塞控制**，端口号负责复用与分用。
> **记住什么**：**首部 8 字节且长度字段含首部** + **校验和只"检错丢弃"、不保证可靠** + **伪首部只参与校验和计算、不随报文发送**。

## 一、首部只有 4 个字段（共 8 字节）
| 字段 | 长度 | 作用 |
| --- | --- | --- |
| 源端口号 | 2 字节 | 复用：发送方靠它区分是哪个进程发出的 |
| 目的端口号 | 2 字节 | 分用：接收方靠它把数据交给正确的进程 |
| 长度 | 2 字节 | **首部 + 数据**的总字节数（最小 8，全 0 表示 65535） |
| 校验和 | 2 字节 | 检出首部与数据里的差错；**算出 0 要写成全 1** |

**效率换算**：应用层数据 M 字节走 UDP，开销恒为 8 字节 ⟹ 效率 = \\(M \\) ÷ \\((M + 8)\\)。
例：M = 12 B ⟹ 12 ÷ 20 = **60%**（对比 TCP 固定首部 20 B：12 ÷ 32 = **37.5%**，2021-39）。

## 二、与 TCP 的对比（考的几乎都是这张表）
| 对比项 | UDP | TCP |
| --- | --- | --- |
| 连接 | **无连接**，直接发 | 面向连接，先三次握手 |
| 可靠性 | 不可靠（丢包不重传），只检错 | 可靠（确认 + 超时重传 + 序号排序） |
| 首部开销 | **8 字节固定** | 20 字节固定（可带选项） |
| 传输单位 | 面向报文（一次发一个数据报） | 面向字节流 |
| 流量 / 拥塞控制 | **都没有** | 滑动窗口 + 慢启动 / 拥塞避免 / 快重传 |
| 一对多 | 支持广播 / 组播 | 只能一对一 |

**选它还是选 TCP**：要实时、要一问一答、要广播 ⟹ UDP（实时音视频、DNS、DHCP、TFTP、SNMP）；要文件、要网页、要邮件 ⟹ TCP。

## 三、校验和（唯一要动笔算的题型）
**计算三步**（2024-39 就是这么问的）：
1. **字块求和**：把伪首部 + UDP 首部 + 数据按 **16 位字块**划分，逐个做**二进制加法**（不是异或），
   数据为奇数个字节时**末尾补一个全 0 字节**；
2. **进位回卷**：把结果高位溢出的进位加到低位（反复做，直到不再溢出）；
3. **按位取反**：得到校验和——**若结果为 0 则写成全 1**（全 0 表示"不校验"）。

**伪首部**：12 字节，含**源 IP / 目的 IP / 全 0 / 协议号 17 / UDP 长度**——
**只在计算校验和时临时拼出来，不随报文发送**，所以它增加的是计算开销、不是传输开销。

## 考点提醒（易错点）
1. **"有校验和"不等于"可靠"**——这是 2014-39 的题眼：UDP 校验和只**检错、丢弃**，不重传也不排序，
   所以"通过差错校验保障可靠数据传输"是错的；
2. **长度字段含 8 字节首部**，求数据长度要减 8；也别忘了它最小是 8（没有数据的空报也要发首部）；
3. **伪首部不发送**：算校验和要用它，算"传输开销 / 效率"时**绝不能算进去**；
4. **端口号的作用是复用与分用**，不是"提供可靠性"；NAT 转发时会改**源端口**、
   于是**UDP 校验和必须重算**，但**总长度与目的端口不变**（2025-37）；
5. **UDP 适合的场景与"要不要可靠"是两件事**：DNS 可以在应用层自己做超时重发，所以它用 UDP 也够用。

> **真题考情**：**4/18 年（选 5）**：2014-39（三条叙述判断，答"仅无连接 + 复用/分用"）、2021-39（12 B 的效率）、
> 2024-39（校验和回卷取反）、2025-37（NAT 改源端口与校验和）、2025-39（无连接 1 个往返）。` },
        ],
      },
      {
        name: '第6章 应用层',
        topics: [
          { id: 'net-dns', label: 'DNS 域名解析过程（递归 / 迭代 / 缓存）', status: 'ready' },
          { id: 'net-http', label: 'HTTP 请求与持久连接（三种连接的 RTT 计数）', status: 'ready' },
          { id: 'net-mail', hot: true, label: '电子邮件与 FTP（SMTP/POP3）', status: 'ready' },
        ],
      },
    ],
  },
];

/* ============================================================================
 * 导航渲染与启动
 * ========================================================================== */
(function boot() {
  let curBook = 'ds';
  let curTopic = null;
  let curFilter = 'all';   // 侧栏筛选：all | ready | other

  const tabsEl = document.getElementById('main-tabs');
  const sidebarEl = document.getElementById('sidebar');
  const mobileEl = document.getElementById('mobile-nav');

  /** 顶部四大主 Tab */
  function renderTabs() {
    tabsEl.innerHTML = RC408.books.map(b => {
      const ready = b.chapters.flatMap(c => c.topics).filter(t => t.status === 'ready').length;
      const total = b.chapters.reduce((n, c) => n + c.topics.length, 0);
      return `<button data-book="${b.id}" class="tab-btn ${b.id === curBook ? 'tab-active' : ''}">
        <span>${b.icon}</span><span>${b.name}</span>
        <span class="tab-badge" title="已实现 / 总数">${ready}/${total}</span>
      </button>`;
    }).join('');
    tabsEl.querySelectorAll('[data-book]').forEach(btn => {
      btn.addEventListener('click', () => selectBook(btn.dataset.book));
    });
  }

  /** 左侧子导航树（章节分组 → 知识点条目；支持 可交互/未上线 筛选） */
  function renderSidebar() {
    const book = RC408.books.find(b => b.id === curBook);
    const matchFilter = t => curFilter === 'all'
      || (curFilter === 'ready' && t.status === 'ready')
      || (curFilter === 'theory' && t.status === 'theory')
      || (curFilter === 'wip' && t.status !== 'ready' && t.status !== 'theory');
    const filterBar = `
      <div class="sticky top-0 z-10 bg-white/95 backdrop-blur px-3 pt-3 pb-2 border-b border-slate-100">
        <div class="flex gap-1">
          ${[['all', '全部'], ['ready', '✓ 可交互'], ['theory', '纯理论'], ['wip', '建设中']].map(([v, t2]) =>
            `<button data-filter="${v}" class="flex-1 text-[11px] font-bold py-1.5 rounded-lg transition-colors ${curFilter === v ? 'bg-indigo-500 text-white shadow-sm' : 'bg-slate-100 text-slate-500 hover:bg-slate-200'}">${t2}</button>`).join('')}
        </div>
      </div>`;
    sidebarEl.innerHTML = filterBar + book.chapters.map(ch => {
      const topics = ch.topics.filter(matchFilter);
      if (!topics.length) return '';
      return `
      <div class="px-4 pt-4 pb-1 text-[11px] font-bold text-slate-400 tracking-wider">${ch.name}</div>
      ${topics.map(t => t.status === 'ready'
        ? `<button data-topic="${t.id}" class="topic ${t.id === curTopic ? 'topic-active' : ''}">
             <span class="dot"></span><span>${t.label}</span>${t.hot ? '<span class="wip-badge" style="background:#fff1f2;color:#be123c" title="真题高频考点">🔥</span>' : ''}
           </button>`
        /* ⚠ 三种"非 ready"状态要用**两套**外观，别一律 topic-wip（用户 2026-09-25 反馈）：
             · theory（纯理论速记卡）：**内容已完成**，只是没有交互动画 ⟹ 标题用正常黑字、圆点用蓝点，
               与"可交互"齐平（用户原话："标题文字可以变成黑色（和可交互一样），然后前面灰点改成蓝点"）；
             · wip（建设中 / 考纲外）：确实还没有内容 ⟹ 保持灰色弱化。
           判据：**已完工的绝不做视觉弱化**——这与 §1.5 那条"已完工内容绝不显示敬请期待"是同一条纪律。 */
        : `<button data-topic="${t.id}" class="topic ${t.status === 'theory' ? 'topic-theory' : 'topic-wip'} ${t.id === curTopic ? 'topic-active' : ''}"
             title="${t.legacy ? '该考点已从现行考纲删除，点击查看说明' : t.status === 'theory' ? '纯理论速记卡（已完成，无交互动画）' : '点击查看规划中的内容'}">
             <span class="${t.status === 'theory' ? 'dot dot-theory' : 'w-1.5 h-1.5 rounded-full bg-slate-300 shrink-0'}"></span>
             <span>${t.label}</span>${t.legacy
              ? '<span class="wip-badge" style="background:#fff1f2;color:#9f1239" title="真题曾考，现行考纲已删">考纲外</span>'
              : t.status === 'theory' ? '<span class="wip-badge" style="background:#e0f2fe;color:#0369a1" title="纯理论速记卡（已完成）">📖 纯理论</span>' : t.hot ? '<span class="wip-badge" style="background:#fff1f2;color:#be123c" title="真题高频考点">🔥</span>' : '<span class="wip-badge">敬请期待</span>'}
           </button>`).join('')}`;
    }).join('');
    sidebarEl.querySelectorAll('[data-topic]').forEach(btn => {
      btn.addEventListener('click', () => openTopic(btn.dataset.topic));
    });
    sidebarEl.querySelectorAll('[data-filter]').forEach(btn => {
      btn.addEventListener('click', () => { curFilter = btn.dataset.filter; renderSidebar(); });
    });

    // 移动端横向导航
    mobileEl.innerHTML = book.chapters.flatMap(c => c.topics).map(t =>
      `<button data-topic="${t.id}" class="shrink-0 px-3 py-1.5 rounded-lg text-xs font-bold ${t.id === curTopic ? 'bg-indigo-500 text-white' : t.status === 'ready' ? 'bg-slate-800 text-indigo-200' : 'bg-slate-800/60 text-slate-500'}">${t.label}${t.status === 'ready' ? '' : ' 🚧'}</button>`
    ).join('');
    mobileEl.querySelectorAll('[data-topic]').forEach(btn => {
      btn.addEventListener('click', () => openTopic(btn.dataset.topic));
    });
  }

  /** 切换主 Tab（书籍），自动打开该书第一个可用知识点 */
  function selectBook(bookId) {
    curBook = bookId;
    const book = RC408.books.find(b => b.id === bookId);
    const all = book.chapters.flatMap(c => c.topics);
    const first = all.find(t => t.status === 'ready') || all[0];
    renderTabs();
    openTopic(first.id);
  }

  /** 打开某个知识点（更新侧栏高亮 → 交给 Runner 渲染工作区） */
  function openTopic(topicId) {
    curTopic = topicId;
    renderSidebar();
    Runner.open(topicId);
  }
  // 供专注模式下拉选择器调用（同步侧栏高亮）
  RC408.openTopicFromUI = openTopic;

  /* ---- 启动 ---- */
  document.addEventListener('DOMContentLoaded', () => {
    Runner.mount();
    renderTabs();
    selectBook('ds');          // 默认打开【数据结构】的第一个可视化
  });
})();
